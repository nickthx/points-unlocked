---
phase: 02-redemption-database
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/data/programs.ts
  - src/data/transfers.ts
  - src/data/redemptions.ts
  - src/data/redemptions-flights.ts
  - src/data/redemptions-hotels.ts
  - src/data/index.ts
autonomous: true
requirements: [DATA-01, DATA-03, DATA-04]
must_haves:
  truths:
    - "All 8 enterable programs plus their transfer-partner programs exist as typed seed data"
    - "The three edge-case routes exist structurally: Marriott 1:3 with 5K-per-60K block bonus, Amex MR→Hilton 2:1, Bilt 1:1"
    - "35-40 drafted redemptions exist, every one with verifiedAt: null and a sourceNote naming what Nick must check"
    - "validateDataset passes over the full real dataset — every cross-reference resolves"
  artifacts:
    - path: "src/data/programs.ts"
      provides: "8 enterable + partner programs, satisfies ProgramSeed[]"
      contains: "isUserEnterable: true"
    - path: "src/data/transfers.ts"
      provides: "Transfer routes (structural) + dated draft transfer bonuses"
      contains: "bonusMilesPerBlock"
    - path: "src/data/redemptions.ts"
      provides: "35-40 draft redemption entries (or barrel over category-split files)"
    - path: "src/data/index.ts"
      provides: "Barrel re-exporting all seed arrays + types for scripts/tests"
  key_links:
    - from: "src/data/transfers.ts"
      to: "src/data/programs.ts"
      via: "fromProgramSlug/toProgramSlug referencing program slugs"
    - from: "src/data/redemptions.ts"
      to: "src/data/programs.ts"
      via: "partnerProgramSlug referencing program slugs"
    - from: "src/data/index.ts"
      to: "src/data/types.ts"
      via: "export * re-export consumed by scripts/seed.ts and tests"
---

<objective>
Author the curated seed dataset as typed TypeScript: the 8 enterable programs plus partner programs, the structural transfer-route graph (including the three named edge cases), draft dated transfer bonuses, and 35-40 draft redemption entries — all Claude-drafted with verifiedAt: null, structured for Nick's DATA-04 verification pass in plan 02-05.

Purpose: This is the product's substance (DATA-01) in draft form. Drafts are scaffolding for Nick, never facts — training-lag makes drafted numbers stale by construction (2026 devaluations), so every entry carries a sourceNote and no entry claims verification.
Output: src/data/programs.ts, transfers.ts, redemptions*.ts, index.ts — Zod-clean via validateDataset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-redemption-database/02-RESEARCH.md
@.planning/phases/02-redemption-database/02-PATTERNS.md
@.planning/phases/02-redemption-database/02-01-SUMMARY.md
</context>

<interfaces>
From src/data/types.ts (created in 02-01 — read it before authoring; field names are the contract):
- Types: `ProgramSeed`, `TransferRouteSeed`, `TransferBonusSeed`, `RedemptionSeed` (camelCase keys mirroring Drizzle schema exactly)
- `validateDataset({ programs, routes, bonuses, redemptions })` — throws on Zod failure, unknown slug references, or duplicates

Canonical enterable-program slugs (fixed contract for all later phases): `chase-ur`, `amex-mr`, `capital-one`, `citi-ty`, `bilt`, `world-of-hyatt`, `hilton-honors`, `marriott-bonvoy`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author programs.ts and transfers.ts (programs, structural routes, draft bonuses)</name>
  <files>src/data/programs.ts, src/data/transfers.ts</files>
  <read_first>src/data/types.ts, src/db/schema.ts, .planning/phases/02-redemption-database/02-RESEARCH.md (seed-data shape lines 410-421, Pattern 1, Assumptions A1-A5, State of the Art table — Bilt 2.0 note), .planning/research/PITFALLS.md (2026 partner/devaluation context, if present)</read_first>
  <action>
    Create src/data/programs.ts exporting `export const programs = [...] satisfies ProgramSeed[]`:
    - Exactly 8 entries with `isUserEnterable: true` using the canonical slugs chase-ur, amex-mr, capital-one, citi-ty, bilt (kind "bank") and world-of-hyatt, hilton-honors, marriott-bonvoy (kind "hotel").
    - Provisional cashOutBaselineCppX100 for banks (Chase 100, Amex 60, Capital One 50, Citi 100, Bilt low/near-zero per research A3), null for hotels — each bank entry commented `// [ASSUMED] baseline — Nick methodology sign-off (A3)`.
    - Partner programs (isUserEnterable: false, cashOutBaselineCppX100: null) for every airline/hotel program referenced by routes or redemptions — roughly 12-20 programs such as world-of-hyatt partners' targets and airline programs (e.g. ana-mileage-club, virgin-atlantic, air-france-flying-blue, united-mileageplus, air-canada-aeroplan, british-airways-avios, alaska-mileage-plan, singapore-krisflyer, avianca-lifemiles, turkish-miles-smiles, etc. — final list is whatever the drafted redemptions in Task 2 actually reference; keep the two files consistent).

    Create src/data/transfers.ts exporting `export const routes = [...] satisfies TransferRouteSeed[]` and `export const bonuses = [...] satisfies TransferBonusSeed[]`:
    - Routes from each of the 5 bank programs to the partners its drafted redemptions use, plus hotel-relevant routes (e.g. chase-ur→world-of-hyatt 1:1) — every route with integer ratioNumerator/ratioDenominator, incrementPoints (banks 1000 per A2, marked [ASSUMED] in notes), active: true, transferTimeDays best-known-draft value or null.
    - The three MANDATORY edge-case routes (DATA-02's test fixtures in real data, consumed by plan 02-03's tests): (1) marriott-bonvoy→at least one airline with ratioNumerator 1, ratioDenominator 3, incrementPoints 3000 [ASSUMED A1 — Nick confirms], bonusMilesPerBlock 5000, bonusBlockPoints 60000; (2) amex-mr→hilton-honors with ratioNumerator 2, ratioDenominator 1, incrementPoints 1000, no block bonus; (3) at least one bilt→airline route with ratioNumerator 1, ratioDenominator 1, incrementPoints 1000, no block bonus. Bilt routes must reflect the 2026 Bilt 2.0 partner reality (mostly 1:1 with exceptions like Accor 3:2) — note exceptions in the route's notes field for Nick.
    - 1-2 draft transferBonuses rows (DATA-03 proof: dated manual overrides editable without schema change) with ISO startDate/endDate and sourceNote explicitly saying it is a Claude draft requiring Nick's confirmation of the live promo (or replacement with a real current one).
    Every drafted number is provisional: notes/sourceNote fields name what must be verified. Keep each file under 500 lines with short intent-comment headers in the Phase 1 style.
  </action>
  <verify>
    <automated>npm run typecheck && npx tsx -e "Promise.all([import('./src/data/programs.ts'),import('./src/data/transfers.ts'),import('./src/data/types.ts')]).then(([p,t,ty]) => { p.programs.forEach(x=>ty.programSeedSchema.parse(x)); t.routes.forEach(x=>ty.transferRouteSeedSchema.parse(x)); t.bonuses.forEach(x=>ty.transferBonusSeedSchema.parse(x)); const e=p.programs.filter(x=>x.isUserEnterable); if(e.length!==8) throw new Error('enterable count '+e.length); console.log('programs+transfers OK:', p.programs.length, 'programs,', t.routes.length, 'routes,', t.bonuses.length, 'bonuses'); })"</automated>
  </verify>
  <acceptance_criteria>
    - The tsx one-liner exits 0 and reports exactly 8 enterable programs
    - src/data/transfers.ts contains a route with bonusMilesPerBlock: 5000 and bonusBlockPoints: 60000 from marriott-bonvoy (grep for "bonusBlockPoints: 60000" ≥ 1 after stripping comment lines)
    - A route amex-mr→hilton-honors with ratioNumerator: 2, ratioDenominator: 1 exists; a bilt route with ratioNumerator: 1, ratioDenominator: 1 exists
    - bonuses array has ≥1 entry, each with ISO startDate/endDate and non-empty sourceNote
    - Both files under 500 lines; npm run typecheck exits 0 (satisfies-clause enforces types)
  </acceptance_criteria>
  <done>Program and route graphs exist as typed, Zod-clean seed data, the three edge-case routes are present structurally, and dated draft bonuses prove the DATA-03 edit path.</done>
</task>

<task type="auto">
  <name>Task 2: Draft 35-40 redemption entries (all verifiedAt: null)</name>
  <files>src/data/redemptions.ts, src/data/redemptions-flights.ts, src/data/redemptions-hotels.ts</files>
  <read_first>src/data/types.ts, src/data/programs.ts, src/data/transfers.ts (from Task 1 — partnerProgramSlug values must exist there), .planning/phases/02-redemption-database/02-RESEARCH.md (Pattern 3 draft workflow, Pitfall 1, Pitfall 5, redemptions column list in Pattern 1)</read_first>
  <action>
    Draft 35-40 redemption entries satisfying RedemptionSeed[] — aspirational, wow-delta-friendly picks (international business class, high-value hotel nights) plus enough breadth that every enterable program is reachable: hotel-program entries use partnerProgramSlug world-of-hyatt / hilton-honors / marriott-bonvoy directly; bank programs reach entries via the routes authored in Task 1 (verify each drafted partnerProgramSlug has an inbound route from at least one bank, or is a direct hotel program).

    Hard rules for every entry (Pitfall 1 + Pitfall 5):
    - `verifiedAt: null` — no exceptions; Claude never marks an entry verified.
    - `sourceNote` names concretely what Nick must check (e.g. "check current ANA award chart via Virgin Atlantic; fare benchmarked Google Flights 2026-09; pre-devaluation risk").
    - Dynamic-priced programs (Delta, United, Aeroplan, Marriott, Hilton, Hyatt peak/off-peak) use pointsMin/pointsMax ranges; only genuinely fixed charts use pointsMax: null.
    - `methodologyNote` records the fare convention used for cashFareCents (e.g. "discounted retail business fare, not full-flex") — the convention itself is provisional until Nick's sign-off (Pitfall 5).
    - Money in integer cents (taxesFeesCents, cashFareCents); slug kebab-case and unique (e.g. "ana-business-tokyo-via-virgin"); bookingHint 2-4 concrete lines (RANK-05 consumes this verbatim later); availabilityRating honest (hard_to_find for ANA F-class-style unicorns); imageSlug a stable destination string (e.g. "tokyo") or null; category from flight|hotel|other.

    File layout (500-line rule): author flights in src/data/redemptions-flights.ts and hotels/other in src/data/redemptions-hotels.ts, with src/data/redemptions.ts exporting `export const redemptions = [...flightRedemptions, ...hotelRedemptions] satisfies RedemptionSeed[]` (concatenation barrel). If the total comfortably fits one file under 450 lines, a single redemptions.ts with the same `redemptions` export name is acceptable — the export name and array shape are the contract.
  </action>
  <verify>
    <automated>npm run typecheck && npx tsx -e "Promise.all([import('./src/data/redemptions.ts'),import('./src/data/types.ts')]).then(([r,ty]) => { r.redemptions.forEach(x=>ty.redemptionSeedSchema.parse(x)); if(r.redemptions.length<35) throw new Error('only '+r.redemptions.length+' entries'); const v=r.redemptions.filter(x=>x.verifiedAt!==null); if(v.length>0) throw new Error(v.length+' entries falsely claim verification'); console.log('redemptions OK:', r.redemptions.length, 'drafts, 0 verified'); })"</automated>
  </verify>
  <acceptance_criteria>
    - The tsx one-liner exits 0 reporting ≥35 drafts and exactly 0 verified
    - Every entry Zod-parses; every entry has non-empty sourceNote and bookingHint
    - Every partnerProgramSlug exists in src/data/programs.ts (full check lands in Task 3's validateDataset run)
    - Each source file is under 500 lines
  </acceptance_criteria>
  <done>35-40 structured draft redemptions exist covering all 8 programs' reach, all honestly marked unverified with verification instructions embedded per entry — the DATA-01 corpus at draft stage, path to 80-120 established by the file-split structure.</done>
</task>

<task type="auto">
  <name>Task 3: Create src/data/index.ts barrel and validate the full dataset end to end</name>
  <files>src/data/index.ts</files>
  <read_first>src/data/types.ts, src/data/programs.ts, src/data/transfers.ts, src/data/redemptions.ts, src/db/index.ts (barrel style analog), .planning/phases/02-redemption-database/02-PATTERNS.md (barrel section — name-collision warning)</read_first>
  <action>
    Create src/data/index.ts re-exporting everything the seed script and tests consume: `export * from "./types"`, `export * from "./programs"`, `export * from "./transfers"`, `export * from "./redemptions"` (redemptions.ts already flattens the category-split files, so the barrel must NOT also re-export redemptions-flights/redemptions-hotels — avoid duplicate symbol exports). Note the known name-collision hazard in a comment: `src/data` exports `programs`/`redemptions` arrays while `src/db` exports Drizzle tables of the same names — consumers importing both must alias (the seed script destructures with renames); never re-export data from src/db or vice versa.
    Then run the full-dataset validation to prove every cross-reference resolves.
  </action>
  <verify>
    <automated>npm run typecheck && npm run build && npx tsx -e "import('./src/data/index.ts').then(d => { d.validateDataset({ programs: d.programs, routes: d.routes, bonuses: d.bonuses, redemptions: d.redemptions }); console.log('dataset valid:', d.programs.length, 'programs,', d.routes.length, 'routes,', d.bonuses.length, 'bonuses,', d.redemptions.length, 'redemptions'); })"</automated>
  </verify>
  <acceptance_criteria>
    - The validateDataset tsx run exits 0 and prints all four counts (proves: all Zod parses pass, all route/bonus/redemption cross-references resolve, no duplicate slugs)
    - `import { programs, routes, bonuses, redemptions, validateDataset } from "../src/data"` resolves (the shape scripts/seed.ts will use in plan 02-04)
    - npm run typecheck and npm run build exit 0
  </acceptance_criteria>
  <done>The complete draft dataset is importable from a single barrel and passes full cross-referential validation — ready for the engine tests (02-03) and seed pipeline (02-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo seed files → downstream consumers | Seed data is repo-controlled source; the Zod boundary in types.ts is the input-validation control (ASVS V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering (bad/stale data) | Drafted seed values | mitigate | Every entry verifiedAt: null + mandatory sourceNote; the automated verifies in this plan fail if any draft claims verification; DATA-04 gate (plan 02-05) is the truth control |
| T-02-06 | Repudiation (untraceable data provenance) | redemptions/bonuses entries | mitigate | sourceNote required by Zod (min 1) on redemptions and bonuses — provenance is schema-enforced, not discipline-enforced |
| T-02-07 | Information disclosure | tsx validation one-liners | accept | Scripts import repo files only, print counts only; no DB connection, no env access — nothing sensitive in scope |
| T-02-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- Full-dataset validateDataset run exits 0 (all cross-refs, zero duplicates)
- ≥35 drafts, 0 falsely-verified entries
- npm run typecheck && npm run build && npm test green (existing suite unaffected)
</verification>

<success_criteria>
- Typed seed files exist for programs, routes, bonuses, and 35-40 draft redemptions — DATA-01's structure with the path to 80-120 established (category-split files)
- The three DATA-02 edge-case routes exist as real seed rows for plan 02-03's tests to consume
- DATA-03 satisfied structurally: bonuses are dated data rows, editable with zero schema change
- DATA-04 draft-stage honored: nothing claims verification; every entry tells Nick what to check
</success_criteria>

<output>
Create `.planning/phases/02-redemption-database/02-02-SUMMARY.md` when done
</output>
