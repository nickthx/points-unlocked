---
phase: 02-redemption-database
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - scripts/seed.ts
  - tests/seed-data.test.ts
autonomous: true
requirements: [DATA-01, DATA-03]
must_haves:
  truths:
    - "One command (npm run db:seed) rebuilds all four curated tables in Neon from the repo — validation throws before any write on bad data"
    - "Running the seed twice converges: identical row counts, no duplicates, no orphans"
    - "The seed script never prints the connection string — row counts and error messages only"
    - "The real seed dataset passes structural tests in CI without a DB connection"
  artifacts:
    - path: "scripts/seed.ts"
      provides: "Idempotent delete-then-insert rebuild via db.batch()"
      contains: "db.batch"
      min_lines: 40
    - path: "tests/seed-data.test.ts"
      provides: "DB-free Zod/cross-ref/edge-case-route assertions over real seed data"
      min_lines: 40
  key_links:
    - from: "scripts/seed.ts"
      to: "src/data/index.ts"
      via: "imports + validateDataset BEFORE importing src/db"
      pattern: "validateDataset"
    - from: "scripts/seed.ts"
      to: "src/db/index.ts"
      via: "db.batch delete children→parents, insert parents→children"
    - from: "tests/seed-data.test.ts"
      to: "src/data/index.ts"
      via: "in-memory import of real seed arrays (no DB)"
---

<objective>
Build the idempotent one-command seed pipeline (npm run db:seed) that rebuilds the four curated tables in Neon from the repo, and the DB-free structural test suite over the real seed data — proving phase success criterion 4 and the automated portions of DATA-01/DATA-03.

Purpose: The DB must be a pure deployment target of the repo (repo-as-CMS). Upsert-only seeding leaves deleted rows behind (Pitfall 3); full rebuild in FK-safe order converges to exactly the repo state every run.
Output: scripts/seed.ts, tests/seed-data.test.ts, seeded Neon DB (draft data live), idempotency proven.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-redemption-database/02-RESEARCH.md
@.planning/phases/02-redemption-database/02-PATTERNS.md
@.planning/phases/02-redemption-database/02-02-SUMMARY.md
</context>

<interfaces>
From src/data/index.ts (02-02): `programs: ProgramSeed[]`, `routes: TransferRouteSeed[]`, `bonuses: TransferBonusSeed[]`, `redemptions: RedemptionSeed[]`, `validateDataset(dataset)` (throws pre-write).
From src/db (barrel, 02-01): lazy `db` client (neon-http; supports `db.batch([...])`, does NOT support interactive `db.transaction()` — it throws) plus Drizzle tables `programs`, `transferRoutes`, `transferBonuses`, `redemptions`. NAME COLLISION: both barrels export `programs`/`redemptions` — alias on import (e.g. data arrays as programData/redemptionData per the RESEARCH.md Pattern 4 sketch).
FK dependency order: transferBonuses → transferRoutes → programs; redemptions → programs. Delete children first, insert parents first.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/seed.ts — validate, then rebuild via db.batch, then prove idempotency</name>
  <files>scripts/seed.ts</files>
  <read_first>scripts/db-check.ts (canonical script template: guarded loadEnvFile, DATABASE_URL check, dynamic import after env load, counts-only output, message-only errors), src/data/index.ts, src/db/index.ts, src/db/schema.ts, .planning/phases/02-redemption-database/02-RESEARCH.md (Pattern 4 lines 279-311, Assumption A6, Pitfall 3)</read_first>
  <action>
    Create scripts/seed.ts following scripts/db-check.ts's structure exactly: header comment stating purpose + run command (`npm run db:seed`), guarded `process.loadEnvFile(".env.development.local")` in try/catch, `main()` that exits 1 with a "DATABASE_URL is not set (re-run `vercel env pull .env.development.local`)" message when the env var is absent.
    Ordering inside main(): dynamically import `../src/data` FIRST and run `validateDataset({...})` — any Zod or cross-ref failure throws before src/db is ever imported, so bad data can never reach a write. Then dynamically import `../src/db`, aliasing colliding names (data arrays vs Drizzle tables).
    Rebuild in one `db.batch([...])` call — NEVER `db.transaction()` (neon-http throws on interactive transactions): deletes in child→parent order (transferBonuses, redemptions, transferRoutes, programs) followed by inserts in parent→child order (programs, transferRoutes, transferBonuses, redemptions) using `db.insert(table).values(array)`. Guard against empty arrays (drizzle's .values([]) throws — skip the insert statement for any empty array, bonuses may legitimately be empty someday).
    Output on success: one line with the four row counts plus the verified count, e.g. "seeded: N programs, N routes, N bonuses, N redemptions (N verified)" — never the connection string. Catch handler prints "seed failed:" + err.message only, exits 1. Convergence note in the header: if a batch partially applies (A6), re-running converges by construction.
  </action>
  <verify>
    <automated>npm run typecheck && npm run db:seed && npm run db:seed && npx tsx scripts/db-check.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run db:seed` exits 0 and prints the counts line matching the seed arrays' lengths (with "(0 verified)" at draft stage)
    - Running `npm run db:seed` twice in a row: second run exits 0 and prints IDENTICAL counts (idempotency, phase success criterion 4)
    - `npx tsx scripts/db-check.ts` prints a programs row count equal to the programs seed array length (data actually landed in Neon)
    - `grep -vE "^\s*//" scripts/seed.ts | grep -c "db.transaction"` reports 0; `grep -c "db.batch" scripts/seed.ts` ≥ 1
    - No output line contains "postgresql://" or any DATABASE_URL fragment
  </acceptance_criteria>
  <done>One command rebuilds Neon's curated tables from the repo, twice-run converges to identical state, validation fails before writes, and secrets never surface.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests/seed-data.test.ts — DB-free structural assertions over the real dataset</name>
  <files>tests/seed-data.test.ts</files>
  <read_first>tests/smoke.test.ts (vitest conventions), tests/transfers.test.ts (if 02-03 already merged — import-style consistency), src/data/index.ts, src/data/types.ts, .planning/phases/02-redemption-database/02-RESEARCH.md (Validation Architecture table, coverage test lines 425-438)</read_first>
  <action>
    Create tests/seed-data.test.ts with named vitest imports, importing the real seed arrays + schemas from ../src/data (no DB connection — CI has no DATABASE_URL by design). Describe blocks:
    - "dataset validity" (DATA-01): every element of programs/routes/bonuses/redemptions passes its Zod schema via safeParse with failures reported per-slug; validateDataset over the full dataset does not throw; exactly 8 programs have isUserEnterable true, matching the canonical slug set chase-ur, amex-mr, capital-one, citi-ty, bilt, world-of-hyatt, hilton-honors, marriott-bonvoy.
    - "edge-case routes present" (DATA-02 data-side): a marriott-bonvoy route exists with ratioNumerator 1, ratioDenominator 3, bonusMilesPerBlock 5000, bonusBlockPoints 60000; the amex-mr→hilton-honors route exists with ratioNumerator 2, ratioDenominator 1; a bilt route exists with ratioNumerator 1, ratioDenominator 1.
    - "transfer bonuses" (DATA-03): every bonus row has endDate >= startDate, a bonusPercent between 1 and 100, non-empty sourceNote, and its (from,to) pair matches an existing route.
    - "provenance" (DATA-04 automated portion): every redemption has a non-empty sourceNote and bookingHint; every entry with pointsMax non-null has pointsMax >= pointsMin.
    Do NOT add the ≥30-verified coverage assertion here — drafts are all verifiedAt: null by design and the suite must stay green; that assertion is added at the verification gate in plan 02-05 (leave a one-line comment saying exactly that).
  </action>
  <verify>
    <automated>npx vitest run tests/seed-data.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/seed-data.test.ts` exits 0 with all four describe blocks passing against the real seed arrays
    - The file contains no import of src/db and no DATABASE_URL access — `grep -vE "^\s*//" tests/seed-data.test.ts | grep -cE "src/db|@/db|DATABASE_URL"` reports 0
    - A comment referencing the plan 02-05 coverage assertion exists (deferred-gate breadcrumb)
    - Full `npm test` green
  </acceptance_criteria>
  <done>The real dataset's structure, cross-references, edge-case routes, bonus dating, and provenance fields are locked by CI-runnable, DB-free tests — a bad transfer ratio now fails the build, not ships.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo seed files → Neon (write path) | scripts/seed.ts is the ONLY writer of curated tables; runs locally with DATABASE_URL, never at request time |
| CI → tests | Tests are DB-free; CI never holds DATABASE_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information disclosure | scripts/seed.ts output/errors | mitigate | Counts-only success line; catch prints err.message only; acceptance criteria assert no connection-string fragments in output; .env.development.local stays untracked |
| T-02-11 | Tampering (bad data reaching DB) | Seed write path (ASVS V5) | mitigate | validateDataset runs BEFORE src/db is imported — Zod + cross-ref failure aborts with exit 1, zero writes |
| T-02-12 | Tampering (SQL injection) | seed.ts queries | mitigate | Drizzle parameterized builder only (delete/insert/values); no raw SQL interpolation |
| T-02-13 | Denial of service (inconsistent DB state) | Partial batch failure (A6) | accept | Re-running converges by construction (full rebuild); curated tables have no runtime writers to race; documented in script header |
| T-02-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `npm run db:seed` twice: both exit 0, identical counts (success criterion 4)
- `npx vitest run tests/seed-data.test.ts` green without a DB
- `npm test && npm run typecheck && npm run build` green
- Homepage (`npm run build` + deployed) now backed by real seeded tables — status line reads "0 verified redemptions live" until 02-05
</verification>

<success_criteria>
- Phase success criterion 4 proven: seed files rebuild the database idempotently with one command
- DATA-01/DATA-03 automated validations green over the real dataset
- Neon contains the full draft dataset, visible via drizzle-kit studio for Nick's verification pass
</success_criteria>

<output>
Create `.planning/phases/02-redemption-database/02-04-SUMMARY.md` when done
</output>
