---
phase: 02-redemption-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/db/schema.ts
  - src/data/types.ts
  - src/app/page.tsx
  - scripts/db-check.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "The live Neon database has programs, transfer_routes, transfer_bonuses, and redemptions tables (health_check is gone)"
    - "npm run typecheck, npm run build, and npm test all pass after the schema swap"
    - "The homepage still renders a live DB status line without exposing errors or secrets"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "4 curated pgTables + 3 pgEnums, integer-only quantities"
      contains: "transferRoutes"
    - path: "src/data/types.ts"
      provides: "Hand-written Zod seed schemas + validateDataset cross-referential checks"
      exports: ["programSeedSchema", "transferRouteSeedSchema", "transferBonusSeedSchema", "redemptionSeedSchema", "validateDataset"]
    - path: "src/app/page.tsx"
      provides: "Verified-redemptions count status line (no healthCheck import)"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/db/schema.ts"
      via: "barrel import of redemptions table from @/db"
      pattern: "from \"@/db\""
    - from: "scripts/db-check.ts"
      to: "src/db/schema.ts"
      via: "programs count query"
      pattern: "from\\(programs\\)"
    - from: "Neon database"
      to: "src/db/schema.ts"
      via: "drizzle-kit push --force"
---

<objective>
Replace the Phase 1 placeholder `health_check` schema with the four real curated tables (programs, transfer_routes, transfer_bonuses, redemptions), create the Zod seed-validation layer, migrate both healthCheck consumers atomically so the build never breaks, and push the new schema to Neon.

Purpose: This is the structural foundation of the product's substance — every later plan (data authoring, engine math, seed pipeline) builds on these table shapes and Zod types.
Output: New src/db/schema.ts, new src/data/types.ts, migrated src/app/page.tsx + scripts/db-check.ts, zod installed, live Neon schema in sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-redemption-database/02-RESEARCH.md
@.planning/phases/02-redemption-database/02-PATTERNS.md
</context>

<interfaces>
Current exports the executor replaces/consumes (from 02-PATTERNS.md, verified by read):

From src/db/schema.ts (current, 11 lines — to be REPLACED):
- `export const healthCheck = pgTable("health_check", { id: serial, status: text, checkedAt: timestamp })`

From src/db/index.ts (UNCHANGED this phase):
- `export const db` — lazy Proxy over drizzle(neon(DATABASE_URL)); importing never throws at build time
- `export * from "./schema"` (line 24) — auto-picks-up the new table exports; consumers import tables via `@/db`, never deep-import schema.ts

From src/app/page.tsx (lines 1-20, current): `import { db, healthCheck } from "@/db"`, `export const dynamic = "force-dynamic"`, `dbStatus` neutral-fallback string set before try, empty catch (T-01-07: never render caught errors).

From scripts/db-check.ts: guarded `process.loadEnvFile(".env.development.local")` in try/catch, DATABASE_URL presence check with exit(1), dynamic `await import("../src/db")` after env load, counts-only output, catch prints `err.message` only (T-01-08).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install zod, replace schema.ts with the 4 curated tables, migrate both healthCheck consumers atomically</name>
  <files>package.json, package-lock.json, src/db/schema.ts, src/app/page.tsx, scripts/db-check.ts</files>
  <read_first>src/db/schema.ts, src/db/index.ts, src/app/page.tsx, scripts/db-check.ts, package.json, .planning/phases/02-redemption-database/02-RESEARCH.md (Pattern 1, lines 154-225), .planning/phases/02-redemption-database/02-PATTERNS.md (schema + page.tsx + db-check sections)</read_first>
  <action>
    Run `npm install zod` (lands as `^4.5.4` in dependencies — slopcheck [OK], no postinstall; do NOT install drizzle-zod or drizzle-seed).

    Rewrite src/db/schema.ts, deleting the `healthCheck` export entirely and defining, exactly per RESEARCH.md Pattern 1's column model:
    - Three pgEnums: `programKind` ("program_kind": bank|airline|hotel), `availabilityRating` ("availability_rating": wide_open|plan_ahead|hard_to_find), `redemptionCategory` ("redemption_category": flight|hotel|other).
    - `programs` table: text `slug` primary key (kebab-case natural key), `name` notNull, `kind` enum notNull, `isUserEnterable` boolean notNull default false, `cashOutBaselineCppX100` nullable integer (100 = 1.0 cents/pt).
    - `transferRoutes` table: `fromProgramSlug`/`toProgramSlug` text notNull with .references to programs.slug, composite primary key on (from,to) via the `(t) => [primaryKey({ columns: [...] })]` form, `ratioNumerator`/`ratioDenominator`/`incrementPoints` integers notNull, `bonusMilesPerBlock`/`bonusBlockPoints` nullable integers (Marriott: 5000/60000), `transferTimeDays` nullable integer, `active` boolean notNull default true, `notes` nullable text.
    - `transferBonuses` table: serial `id` PK, `fromProgramSlug`/`toProgramSlug` text notNull, `bonusPercent` integer notNull (30 = +30%), `startDate`/`endDate` date notNull, `sourceNote` text notNull, plus a composite `foreignKey()` on (fromProgramSlug,toProgramSlug) referencing transferRoutes (fromProgramSlug,toProgramSlug).
    - `redemptions` table: text `slug` PK, `partnerProgramSlug` notNull referencing programs.slug, `title` notNull, `category` enum notNull, nullable `origin`/`destination`/`cabin` text, `pointsMin` integer notNull, `pointsMax` nullable integer (null = fixed price), `taxesFeesCents` integer notNull, `cashFareCents` integer notNull, `availabilityRating` enum notNull, `bookingHint` text notNull, `methodologyNote` nullable text, `sourceNote` text notNull, `verifiedAt` nullable date (NULL = draft, not shippable), `imageSlug` nullable text, `featured` boolean notNull default false, `notes` nullable text.
    Conventions: snake_case column-name strings, camelCase TS keys/exports, integer-only quantities (cents / x100 / numerator-denominator — no floats, no `numeric`), short D-xx/T-xx style intent comment header in the Phase 1 style. Do NOT create users/bookmarks tables (Phase 6) or store cpp/wow-delta columns (derived values live in the engine).

    Update src/app/page.tsx in the same change: replace the `healthCheck` import with `import { db, redemptions } from "@/db"` and `import { count, isNotNull } from "drizzle-orm"`. Replace the query block with a select of `{ n: count() }` from redemptions where `isNotNull(redemptions.verifiedAt)`; on success set dbStatus to the template `` `${n} verified redemptions live` `` (0 is a valid render in this phase); keep the neutral fallback "infrastructure: warming up" initialized before the try, keep the empty catch (T-01-07: never render the caught error), keep `export const dynamic = "force-dynamic"` and all existing JSX/layout untouched.

    Update scripts/db-check.ts in the same change: repoint from healthCheck insert+select to a read-only `db.select({ n: count() }).from(programs)` printing exactly the row count (e.g. "programs rows: N"). Keep every existing pattern verbatim: guarded loadEnvFile, DATABASE_URL check with exit(1), dynamic import after env load, counts-only output, message-only error handling. Update the header comment to describe the new behavior. Do not add an insert — curated tables have no writer besides the seed script.

    Add npm scripts to package.json matching the existing bare-name style: "db:push": "drizzle-kit push" and "db:seed": "tsx scripts/seed.ts" (seed.ts arrives in plan 02-04; the script entry is inert until then).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "healthCheck" src/db/schema.ts src/app/page.tsx scripts/db-check.ts` reports 0 matches in every file
    - `grep -c "pgEnum" src/db/schema.ts` reports 3; `grep -c "pgTable" src/db/schema.ts` reports 4
    - src/db/schema.ts contains no `real(`, `doublePrecision(`, or `numeric(` column — `grep -vE "^\s*//" src/db/schema.ts | grep -cE "real\(|doublePrecision\(|numeric\(" ` reports 0
    - package.json dependencies contain "zod" and scripts contain "db:push" and "db:seed"
    - src/app/page.tsx contains the string `verified redemptions live` and still contains `force-dynamic`
    - `npm run typecheck`, `npm run build`, `npm test` all exit 0
  </acceptance_criteria>
  <done>healthCheck is gone from the codebase, the 4 curated tables + 3 enums exist in schema.ts, both consumers compile against the new schema, and typecheck/lint/build/test are green.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/data/types.ts — seed types, hand-written Zod schemas, validateDataset</name>
  <files>src/data/types.ts</files>
  <read_first>src/db/schema.ts (as rewritten in Task 1 — Zod field names/types must mirror the Drizzle TS keys exactly so db.insert(...).values(seedArray) needs no mapping), .planning/phases/02-redemption-database/02-RESEARCH.md (Pattern 2, lines 227-271; Assumption A4)</read_first>
  <action>
    Create src/data/types.ts with hand-written Zod v4 schemas (do NOT use drizzle-zod) whose object keys exactly match the camelCase Drizzle property names from schema.ts:
    - `programSeedSchema`: slug matching regex ^[a-z0-9-]+$, name min 1, kind enum ["bank","airline","hotel"], isUserEnterable boolean, cashOutBaselineCppX100 positive int nullable.
    - `transferRouteSeedSchema`: from/to slugs, ratioNumerator/ratioDenominator/incrementPoints positive ints, bonusMilesPerBlock/bonusBlockPoints positive-int-nullable with a .refine enforcing both-null-or-both-set ("bonus fields must be set together"), transferTimeDays int min 0 nullable, active boolean, notes string nullable.
    - `transferBonusSeedSchema`: from/to slugs, bonusPercent int between 1 and 100, startDate/endDate as ISO date strings (z.iso.date() in zod v4), sourceNote min 1, with a .refine enforcing endDate >= startDate ("bonus endDate must be on or after startDate").
    - `redemptionSeedSchema`: slug regex ^[a-z0-9-]+$, partnerProgramSlug, title min 1, category enum ["flight","hotel","other"], origin/destination/cabin string nullable, pointsMin positive int, pointsMax positive int nullable with a .refine enforcing pointsMax >= pointsMin when non-null, taxesFeesCents int min 0, cashFareCents positive int, availabilityRating enum ["wide_open","plan_ahead","hard_to_find"], bookingHint min 1, methodologyNote nullable, sourceNote min 1 (Pitfall 1: non-negotiable), verifiedAt ISO date string nullable, imageSlug nullable, featured boolean, notes nullable.
    - Export inferred TS types via z.infer: ProgramSeed, TransferRouteSeed, TransferBonusSeed, RedemptionSeed.
    - Export `validateDataset({ programs, routes, bonuses, redemptions })`: Zod-parse every array element (aggregate and throw all issues with the offending slug/index in the message), then cross-referential checks per Pattern 2 — every route's from/to slug exists in programs; every bonus's (from,to) pair matches an existing route; every redemption's partnerProgramSlug exists in programs; no duplicate program slugs, route (from,to) pairs, or redemption slugs. Throw Error on any failure; error messages name the offending entity but never include env/connection values.
    - Document Assumption A4 as a code comment near the bonus schema: promotional transfer bonuses multiply the base-converted amount and do NOT stack with structural block bonuses (Marriott 5K/60K); rule pending Nick's confirmation at the DATA-04 checkpoint.
  </action>
  <verify>
    <automated>npm run typecheck && npx tsx -e "import('./src/data/types.ts').then(m => { if (typeof m.validateDataset !== 'function') throw new Error('validateDataset missing'); m.validateDataset({ programs: [], routes: [], bonuses: [], redemptions: [] }); console.log('types.ts OK'); })"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/types.ts exports programSeedSchema, transferRouteSeedSchema, transferBonusSeedSchema, redemptionSeedSchema, validateDataset, and the four z.infer types
    - The tsx one-liner above prints "types.ts OK" and exits 0 (empty dataset passes; function exists)
    - `grep -vE "^\s*//" src/data/types.ts | grep -c "drizzle-zod"` reports 0 (hand-written schemas only)
    - The file contains the string "must be set together" (bonus-pair refinement) and a comment mentioning the promo/structural non-stacking rule
    - File is under 500 lines
  </acceptance_criteria>
  <done>Seed-boundary validation exists: Zod schemas mirror the Drizzle shapes, validateDataset enforces cross-references and uniqueness, and the module loads clean under tsx.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push the new schema to Neon (destructive: drops health_check)</name>
  <files>(none — remote schema operation against Neon)</files>
  <read_first>drizzle.config.ts, scripts/db-check.ts (as repointed in Task 1), .planning/phases/02-redemption-database/02-RESEARCH.md (Runtime State Inventory + Pitfall 6)</read_first>
  <action>
    Run `npx drizzle-kit push --force` from the repo root. The --force flag is REQUIRED and intentional: dropping health_check (with its ~2 Phase 1 proof rows) is a destructive change that otherwise triggers an interactive data-loss prompt that hangs non-TTY execution (Pitfall 6). The data loss is intended and harmless. DATABASE_URL comes from .env.development.local (drizzle.config.ts already does the guarded loadEnvFile). Never echo or log the connection string; if the command fails, report only the error message class. If .env.development.local is missing, run `vercel env pull .env.development.local` first.
    After the push succeeds, confirm the live schema by running the repointed `npx tsx scripts/db-check.ts` — it must connect and print a programs count of 0 (tables exist, empty until plan 02-04 seeds them).
  </action>
  <verify>
    <automated>npx drizzle-kit push --force && npx tsx scripts/db-check.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push --force` exits 0
    - `npx tsx scripts/db-check.ts` exits 0 and prints "programs rows: 0" (proves programs table exists in Neon and health_check coupling is fully retired)
    - No command output contains "postgresql://" or any DATABASE_URL fragment
  </acceptance_criteria>
  <done>Neon's live schema matches src/db/schema.ts: 4 curated tables exist, health_check is dropped, and the db-check round-trip proves connectivity against the new schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo seed files → database | The only data ingress this phase; all values from repo-controlled TypeScript, no user input |
| local scripts → Neon | Scripts carry DATABASE_URL; output and errors must never leak it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information disclosure | scripts/db-check.ts, drizzle-kit push output | mitigate | T-01-08 pattern preserved: counts-only output, catch prints err.message only, never the connection string; acceptance criteria grep push/check output for absence of DATABASE_URL fragments |
| T-02-02 | Information disclosure | src/app/page.tsx error path | mitigate | T-01-07 pattern preserved: empty catch, neutral fallback string, caught error never rendered |
| T-02-03 | Tampering (injection) | New table queries in page.tsx/db-check.ts | mitigate | Drizzle parameterized query builder only (count/isNotNull helpers); no raw SQL string interpolation |
| T-02-04 | Tampering (bad data) | Seed boundary (V5 input validation) | mitigate | validateDataset Zod-parses every element + cross-refs before any DB write path exists (consumed by plans 02-04/02-05) |
| T-02-SC | Tampering | npm install zod | mitigate | Audited in 02-RESEARCH.md Package Legitimacy Audit: registry ~6yrs, tens of M weekly downloads, no postinstall, slopcheck [OK] — no human gate required; install nothing else |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run build && npm test` all green after the schema swap
- `grep -rc "healthCheck" src/ scripts/` finds 0 occurrences
- `npx tsx scripts/db-check.ts` exits 0 against the pushed schema
</verification>

<success_criteria>
- The 4 curated tables + 3 enums exist in src/db/schema.ts AND in live Neon (pushed), modeling everything phase success criterion 1 lists: ratio numerator/denominator + increment + structural bonus block on routes, dated manual bonus overrides, and redemptions with points range, cash fare, taxes/fees, verified date, and booking notes
- src/data/types.ts provides the Zod seed boundary with cross-referential validateDataset
- Both healthCheck consumers migrated in the same plan — no intermediate broken-build state
- zod is the only new dependency
</success_criteria>

<output>
Create `.planning/phases/02-redemption-database/02-01-SUMMARY.md` when done
</output>
