---
phase: 02-redemption-database
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - src/data/redemptions.ts
  - src/data/redemptions-flights.ts
  - src/data/redemptions-hotels.ts
  - src/data/transfers.ts
  - src/data/programs.ts
  - src/data/types.ts
  - tests/seed-data.test.ts
  - tests/transfers.test.ts
autonomous: false
requirements: [DATA-04, DATA-01]
must_haves:
  truths:
    - "At least 30 entries carry a non-null verifiedAt set by Nick against live 2026 sources — no entry is marked verified by Claude"
    - "Every one of the 8 enterable programs is reachable by at least one verified redemption (direct or via an active route)"
    - "The coverage gate is now an executable test that fails the build if verification coverage regresses"
    - "The live Neon database and homepage count reflect the verified dataset"
  artifacts:
    - path: "tests/seed-data.test.ts"
      provides: "Activated coverage gate: ≥30 verified + all-8-programs reachability"
      contains: "toBeGreaterThanOrEqual(30)"
    - path: "src/data/redemptions.ts"
      provides: "≥30 entries with verifiedAt dates + Nick-corrected numbers"
  key_links:
    - from: "Nick (human checkpoint)"
      to: "src/data/*.ts"
      via: "corrections + verifiedAt dates applied as a diffable git event"
    - from: "src/app/page.tsx"
      to: "Neon redemptions table"
      via: "verified-count status line ≥ 30 after reseed"
---

<objective>
Run the DATA-04 verification gate: Nick verifies at least 30 drafted entries (and the transfer-route assumptions A1-A4) against live 2026 sources, corrections land in the seed files, the ≥30-verified/all-8-programs coverage test activates, and the verified dataset reseeds to Neon.

Purpose: This is the phase's truth control and schedule long pole. Claude-drafted numbers are stale by construction (2026 devaluations); "no unverified entry ships" becomes enforceable by a WHERE clause plus a CI test — not by discipline.
Output: ≥30 verified entries in seed files, activated coverage gate, reseeded Neon, homepage showing ≥30 verified redemptions live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-redemption-database/02-RESEARCH.md
@.planning/phases/02-redemption-database/02-02-SUMMARY.md
@.planning/phases/02-redemption-database/02-03-SUMMARY.md
@.planning/phases/02-redemption-database/02-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Nick verifies 30+ entries and the transfer-rule assumptions against live 2026 sources</name>
  <files>(none — human review of src/data/*.ts; edits land in Task 2)</files>
  <read_first>src/data/redemptions-flights.ts, src/data/redemptions-hotels.ts, src/data/transfers.ts, src/data/programs.ts (present their contents/summaries to Nick as the review surface)</read_first>
  <action>Pause execution and present the verification request below to Nick. Do not modify any file, mark any entry verified, or proceed to Task 2 until Nick responds with his corrections and rulings. This gate cannot be automated: data truth (2026 award prices, ratios, partner lists) is exactly what Claude's training lag gets wrong (Pitfall 1).</action>
  <what-built>The full draft dataset is authored, seeded to Neon, and structurally validated: 35-40 draft redemptions (all verifiedAt: null, each sourceNote naming what to check), the transfer-route graph including the Marriott/Amex-Hilton/Bilt edge cases, 1-2 draft transfer bonuses, and a green test suite freezing the assumed transfer math. Browse the data with `npx drizzle-kit studio` or read the files directly in src/data/.</what-built>
  <how-to-verify>
    1. Open src/data/redemptions-flights.ts and src/data/redemptions-hotels.ts (and src/data/transfers.ts). Each entry's sourceNote says what to check.
    2. For at least 30 entries spanning reach for ALL 8 programs (Chase UR, Amex MR, Capital One, Citi TY, Bilt, Hyatt, Hilton, Marriott): confirm against live sources (current award charts/searches, Google Flights for cash fares) that pointsMin/pointsMax, cashFareCents, taxesFeesCents, partner program, and bookingHint are accurate as of 2026. Watch for pre-devaluation numbers (Marriott +5-10%, Hyatt, Aeroplan +20-67%, Bilt 2.0 partner list) — drafts WILL contain stale values.
    3. For each confirmed entry, provide the corrected numbers (if changed) and the verification date (YYYY-MM-DD) to set as verifiedAt.
    4. Rule confirmations needed (currently [ASSUMED] in code/tests):
       - A1: Marriott transfer increment (assumed 3,000 pts) and the exact 5K-per-60K bonus semantics — the test currently expects 59,000 Bonvoy → 19,000 miles under that rule; state the correct rule if different.
       - A2: bank-program transfer increments (assumed 1,000).
       - A3: cash-out baselines per program (Chase 1.0¢, Amex 0.6¢, Capital One 0.5¢, Citi 1.0¢, Bilt ~0) — provisional until your Phase 3 methodology sign-off, but veto anything clearly wrong now.
       - A4: promotional bonuses multiply the base-converted amount and do NOT stack with Marriott's block bonus — confirm or correct.
       - Cash-fare convention: confirm the fare convention recorded in the entries' methodologyNote (e.g. discounted realistic fare vs full-flex) so Phase 3's methodology page matches the data.
       - Transfer bonuses: confirm/replace the draft bonus rows with any actually-live 2026 promos (with dates + source).
  </how-to-verify>
  <verify>
    <human-check>Nick has supplied corrections + verifiedAt dates covering ≥30 entries that span all 8 programs, plus rulings on A1-A4 and the cash-fare convention.</human-check>
  </verify>
  <acceptance_criteria>
    - A corrections list from Nick exists covering ≥30 entry slugs with verification dates
    - Explicit rulings (confirm or correct) recorded for A1, A2, A3, A4, and the cash-fare convention
    - Any live 2026 transfer bonuses identified (or draft bonus rows explicitly rejected)
  </acceptance_criteria>
  <resume-signal>Provide the corrections list + verifiedAt dates per entry slug (any format — inline notes, edited files, or dictated changes) and the A1-A4/fare-convention rulings. Type "verified" when the list is complete, or describe blockers.</resume-signal>
  <done>Nick's verification rulings are captured in the checkpoint response — the raw input Task 2 materializes into the seed files.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Nick's corrections, set verifiedAt dates, activate the coverage gate</name>
  <files>src/data/redemptions.ts, src/data/redemptions-flights.ts, src/data/redemptions-hotels.ts, src/data/transfers.ts, src/data/programs.ts, src/data/types.ts, tests/seed-data.test.ts, tests/transfers.test.ts</files>
  <read_first>All files listed above in their current state, plus the checkpoint output from Task 1 (Nick's corrections and rulings), .planning/phases/02-redemption-database/02-RESEARCH.md (coverage test lines 425-438, Pattern 3)</read_first>
  <action>
    Apply every correction Nick provided to the seed files: update pointsMin/pointsMax, cashFareCents, taxesFeesCents, partner slugs, bookingHint text, bonus rows, and route fields exactly as ruled. Set `verifiedAt: "YYYY-MM-DD"` (Nick's dates) ONLY on entries Nick explicitly confirmed — entries he did not review stay null. Update each verified entry's sourceNote to name the 2026 source used. If Nick's A1 ruling differs from the assumed Marriott rule (increment 3000 / 59K→19,000), update the route row in src/data/transfers.ts AND the literal expected values in tests/transfers.test.ts to encode his rule as the new frozen spec; same for A2 increments. If A4 is corrected, update the rule comment in src/data/types.ts and src/engine/transfers.ts and adjust the composition test. Record the confirmed cash-fare convention in the types.ts comment block so Phase 3's methodology page inherits it.
    Then activate the coverage gate in tests/seed-data.test.ts (replacing the deferred-gate comment from 02-04): assert `redemptions.filter(r => r.verifiedAt !== null).length` is ≥ 30 via toBeGreaterThanOrEqual(30), and for each of the 8 enterable programs assert at least one VERIFIED redemption is reachable — either partnerProgramSlug equals the program's slug (direct hotel use) or an active route exists from the program to the redemption's partner (per the RESEARCH.md coverage test shape, with a per-program failure message naming the uncovered slug).
  </action>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 INCLUDING the newly-activated coverage assertions (≥30 verified, all 8 programs reachable) — the gate is now regression-proof
    - `npx tsx -e "import('./src/data/index.ts').then(d=>{const v=d.redemptions.filter(r=>r.verifiedAt!==null);console.log(v.length+' verified');if(v.length<30)throw new Error('below gate')})"` prints ≥30
    - Every verified entry has a sourceNote referencing a 2026-current source; zero entries were marked verified without appearing in Nick's confirmation list
    - tests/transfers.test.ts expected values match Nick's A1/A2 rulings (unchanged if he confirmed the assumptions)
  </acceptance_criteria>
  <done>Nick's verification is materialized as a diffable git event, ≥30 entries are verified with all 8 programs covered, and the coverage floor is CI-enforced from now on.</done>
</task>

<task type="auto">
  <name>Task 3: Reseed Neon with the verified dataset and confirm the full phase gate</name>
  <files>(none — runs existing scripts against Neon)</files>
  <read_first>scripts/seed.ts, scripts/db-check.ts, src/app/page.tsx</read_first>
  <action>
    Run `npm run db:seed` twice — both runs must exit 0 with identical counts, now showing ≥30 in the "(N verified)" suffix (idempotency re-proven over the final dataset). Confirm the verified count in the live DB with a read-only tsx one-liner selecting the count of redemptions where verified_at is not null (import db + redemptions from ../src/db with count/isNotNull from drizzle-orm — same query shape page.tsx uses), asserting ≥30. Run the full local gate: `npm test && npm run typecheck && npm run lint && npm run build`. Commit and push to main so Vercel auto-deploys; the production homepage's status line should render "N verified redemptions live" with N ≥ 30 (spot-check the deployed URL if reachable; the DB-level count assertion is the binding proof).
  </action>
  <verify>
    <automated>npm run db:seed && npm run db:seed && npx tsx -e "process.loadEnvFile('.env.development.local');Promise.all([import('./src/db/index.ts'),import('drizzle-orm')]).then(async([m,o])=>{const[{n}]=await m.db.select({n:o.count()}).from(m.redemptions).where(o.isNotNull(m.redemptions.verifiedAt));console.log(n+' verified in DB');if(Number(n)<30)process.exit(1)})" && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Both seed runs exit 0 with identical counts; verified suffix ≥ 30
    - The DB-level query prints a verified count ≥ 30 and exits 0
    - `npm test`, `npm run typecheck`, `npm run lint`, `npm run build` all exit 0
    - No command output contains a connection-string fragment
  </acceptance_criteria>
  <done>The verified curated dataset is live in Neon, idempotency holds on the final data, the homepage proof-of-life reflects ≥30 verified redemptions, and every phase success criterion is demonstrably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verification → seed files | Nick's rulings are the trust anchor for data truth; applied as reviewable git diffs |
| local seed/query scripts → Neon | DATABASE_URL in scope; outputs must stay secret-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Spoofing (false verification claims) | verifiedAt field | mitigate | verifiedAt set only from Nick's explicit confirmation list (acceptance criterion); coverage test makes silent regression fail CI; git diff of the verification commit is the audit record |
| T-02-15 | Repudiation (unverifiable numbers) | Verified entries | mitigate | Each verified entry's sourceNote updated to the 2026 source Nick used — defensibility is part of the product pitch |
| T-02-16 | Information disclosure | Reseed + DB count one-liner | mitigate | Established patterns: counts-only output, message-only errors, no connection-string echo (asserted in acceptance criteria) |
| T-02-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- Coverage gate green in CI: ≥30 verified, all 8 enterable programs reachable by a verified entry
- `npm run db:seed` idempotent over the final dataset; DB-level verified count ≥ 30
- Full local gate green: test, typecheck, lint, build
- No entry marked verified without Nick's confirmation
</verification>

<success_criteria>
- Phase success criterion 3 proven and CI-enforced: ≥30 Nick-verified entries covering all 8 programs, no unverified entry marked shippable (verifiedAt null = draft, filtered by all downstream readers)
- DATA-04 satisfied: content drafted collaboratively, verified by Nick, gate executable
- A1-A4 assumptions resolved into frozen specs (tests updated to Nick's rulings)
- Neon + production homepage reflect the verified dataset
</success_criteria>

<output>
Create `.planning/phases/02-redemption-database/02-05-SUMMARY.md` when done
</output>
