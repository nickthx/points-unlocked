---
phase: 02-redemption-database
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/engine/transfers.ts
  - tests/transfers.test.ts
autonomous: true
requirements: [DATA-02]
must_haves:
  truths:
    - "The Marriott edge case computes correctly from the real seed row: 120,000 Bonvoy → 50,000 miles (base 40,000 + two 5K blocks)"
    - "Amex MR→Hilton 1:2 computes correctly: 60,000 MR → 120,000 Hilton"
    - "Bilt 1:1 computes correctly: 25,000 Bilt → 25,000 miles"
    - "Promotional bonuses compose on the base-converted amount: 30% on 10,000 MR→Hilton → 26,000"
    - "The engine module imports nothing from db, app, next, or react"
  artifacts:
    - path: "src/engine/transfers.ts"
      provides: "Pure integer transfer math shared with Phase 3"
      exports: ["computePartnerPoints", "applyPromoBonus"]
      min_lines: 20
    - path: "tests/transfers.test.ts"
      provides: "Hand-computed edge-case table run against REAL seed rows"
      min_lines: 40
  key_links:
    - from: "tests/transfers.test.ts"
      to: "src/data/transfers.ts"
      via: "imports real routes (not fixtures) so a data typo in the Marriott row fails CI"
      pattern: "from \"(\\.\\./src|@)/data"
    - from: "src/engine/transfers.ts"
      to: "src/data/types.ts"
      via: "TransferRouteSeed type import only (purity boundary)"
---

<objective>
Implement the pure transfer-math engine functions (computePartnerPoints, applyPromoBonus) test-first, with the hand-computed edge-case table exercised against the REAL seed routes from src/data/transfers.ts — proving phase success criterion 2 (DATA-02) and giving Phase 3 its foundation function instead of a future re-implementation.

Purpose: Simple-multiplier math is right for ~80% of routes and wrong for exactly the famous ones an expert would check (Pitfall 2). Freezing the block-bonus and increment semantics in tests against live seed data makes both a math bug AND a data-entry typo fail CI.
Output: src/engine/transfers.ts (pure TS), tests/transfers.test.ts (green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-redemption-database/02-RESEARCH.md
@.planning/phases/02-redemption-database/02-PATTERNS.md
@.planning/phases/02-redemption-database/02-02-SUMMARY.md
</context>

<interfaces>
From src/data/types.ts (02-01): `TransferRouteSeed` — { fromProgramSlug, toProgramSlug, ratioNumerator, ratioDenominator, incrementPoints, bonusMilesPerBlock, bonusBlockPoints, transferTimeDays, active, notes }.
From src/data/transfers.ts (02-02): `routes: TransferRouteSeed[]` containing the marriott-bonvoy block-bonus route (1/3, increment 3000, 5000/60000), amex-mr→hilton-honors (2/1, increment 1000), and a bilt 1:1 route.

Contract this plan CREATES (Phase 3 builds path-resolution on top of these signatures):
- `computePartnerPoints(route, sourcePoints): number` — floors sourcePoints to the route increment, applies integer ratio with Math.floor, adds structural block bonus per full bonusBlockPoints block transferred.
- `applyPromoBonus(basePartnerPoints, bonusPercent): number` — floor(base × (100 + pct) / 100); applies to the base-converted amount, never to source points; does not stack with structural block bonuses (A4).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computePartnerPoints — failing edge-case tests first, then the pure implementation</name>
  <files>tests/transfers.test.ts, src/engine/transfers.ts</files>
  <read_first>src/data/transfers.ts, src/data/types.ts, tests/smoke.test.ts (vitest conventions: named imports, no globals), vitest.config.ts, .planning/phases/02-redemption-database/02-RESEARCH.md (Code Examples lines 375-408, Open Question 3)</read_first>
  <behavior>
    All expectations computed against REAL rows looked up from src/data/transfers.ts routes (find by fromProgramSlug/toProgramSlug; test fails loudly if a route is missing):
    - Marriott→airline (1/3, increment 3000, bonus 5000/60000): 120,000 → 50,000 (base 40,000 + 2×5,000); 60,000 → 25,000 (base 20,000 + 1×5,000); 59,000 → 19,000 (transferable floors to 57,000 → base 19,000, no full 60K block)
    - amex-mr→hilton-honors (2/1, increment 1000): 60,000 → 120,000
    - bilt→airline (1/1, increment 1000): 25,000 → 25,000
    - Sub-increment balance: 900 points on a 1000-increment route → 0
  </behavior>
  <action>
    RED: write tests/transfers.test.ts using named vitest imports (describe/it/expect — no globals) importing `routes` from `../src/data/transfers` (fall back to relative import if the @/ alias fails under vitest — no vite-tsconfig-paths plugin is installed) and `computePartnerPoints` from `../src/engine/transfers`. Include a comment noting the Marriott increment=3000 and 59K→19,000 expectation encode Assumption A1 — the frozen spec unless Nick's checkpoint (plan 02-05) states a different rule. Run `npx vitest run tests/transfers.test.ts` and confirm it FAILS (module missing). Commit the failing test.
    GREEN: implement src/engine/transfers.ts per the RESEARCH.md Code Example semantics — `transferable = Math.floor(sourcePoints / incrementPoints) * incrementPoints`; `base = Math.floor(transferable * ratioNumerator / ratioDenominator)`; block bonus = `Math.floor(transferable / bonusBlockPoints) * bonusMilesPerBlock` when both bonus fields are set, else 0; return base + bonus. Integer math with Math.floor throughout, no floats. The ONLY import allowed is the TransferRouteSeed type (type-only) from ../data/types. Short intent-comment header noting the file is Phase 3's engine foundation and must stay framework/DB-free.
  </action>
  <verify>
    <automated>npx vitest run tests/transfers.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RED commit exists where tests/transfers.test.ts fails; GREEN state: `npx vitest run tests/transfers.test.ts` exits 0
    - All six behavior expectations above pass against routes imported from src/data/transfers.ts (not inline fixtures) — `grep -v "^\s*//" tests/transfers.test.ts | grep -c "src/data/transfers\|@/data"` ≥ 1
    - 120000→50000, 60000→25000, 59000→19000, 60000 MR→120000, 25000→25000, 900→0 all asserted with literal expected numbers
  </acceptance_criteria>
  <done>computePartnerPoints exists as a pure function whose block-bonus and increment semantics are frozen by tests over real seed rows — a Marriott data typo or a math regression both fail CI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applyPromoBonus + composition test + engine purity gate</name>
  <files>tests/transfers.test.ts, src/engine/transfers.ts</files>
  <read_first>src/engine/transfers.ts (as created in Task 1), tests/transfers.test.ts (as created in Task 1), .planning/phases/02-redemption-database/02-RESEARCH.md (promo composition rule lines 388-393, Assumption A4)</read_first>
  <behavior>
    - applyPromoBonus(10_000, 30) → 13,000 (plain 30% on 1:1-equivalent base)
    - Composition (DATA-03 rule): applyPromoBonus(computePartnerPoints(mrToHilton, 10_000), 30) → 26,000 — the bonus multiplies the base-CONVERTED amount (20,000 Hilton), never the source MR points
    - Rounding: applyPromoBonus(1_001, 30) → 1,301 (Math.floor of 1301.3)
  </behavior>
  <action>
    RED: add a describe block for promo-bonus composition to tests/transfers.test.ts with the three expectations above (mrToHilton looked up from the real routes array). Confirm failure (applyPromoBonus not exported). GREEN: add `applyPromoBonus(basePartnerPoints, bonusPercent)` to src/engine/transfers.ts returning `Math.floor(basePartnerPoints * (100 + bonusPercent) / 100)`, with a comment documenting the A4 non-stacking rule (promotional bonus applies to base conversion only; structural block bonuses are separate and unstacked — pending Nick's confirmation).
    Then enforce the engine purity boundary (the architecture's hard constraint: this module becomes the v2 advisor's tool) with the grep gate in verify — src/engine/transfers.ts must import nothing from next, react, src/db, or src/app; the only permitted import is a type-only import from ../data/types.
  </action>
  <verify>
    <automated>npx vitest run tests/transfers.test.ts && npm test && test "$(grep -vE '^\s*(//|\*)' src/engine/transfers.ts | grep -cE 'from ["'"'"'](next|react|@/db|@/app|\.\./db|\.\./app)')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/transfers.test.ts` exits 0 with the composition assertions passing (26,000 literal asserted)
    - The purity grep gate reports 0 forbidden imports (comment lines excluded from the count)
    - `grep -c "export function" src/engine/transfers.ts` reports 2 (computePartnerPoints, applyPromoBonus)
    - Full `npm test` suite green
  </acceptance_criteria>
  <done>Promo-bonus math and its composition rule are implemented, tested against real seed rows, documented for Nick's veto, and the engine module is provably framework/DB-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed data → engine math | Engine consumes only repo-controlled, Zod-validated route rows; no user input this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Tampering (wrong-math integrity) | computePartnerPoints / applyPromoBonus | mitigate | Hand-computed expected values frozen in tests over real seed rows; integer-only Math.floor arithmetic — no float drift in finance-credibility numbers |
| T-02-09 | Elevation of privilege (boundary erosion) | src/engine/transfers.ts imports | mitigate | Purity grep gate in Task 2 verify fails the task if db/app/next/react imports appear |
| T-02-SC | Tampering | package installs | accept | This plan installs nothing; tests are DB-free (no DATABASE_URL in scope) |
</threat_model>

<verification>
- `npx vitest run tests/transfers.test.ts` green: Marriott 120K/60K/59K, MR→Hilton, Bilt, sub-increment, promo composition all pass against real seed rows
- Purity gate: zero forbidden imports in src/engine/transfers.ts
- `npm test && npm run typecheck && npm run build` green
</verification>

<success_criteria>
- Phase success criterion 2 proven: Marriott 3:1 + 5K/60K, Amex→Hilton 1:2, and Bilt 1:1 compute correctly in automated validation over the actual seed rows
- Phase 3 inherits computePartnerPoints/applyPromoBonus rather than re-implementing (one implementation, zero drift)
- A1/A4 assumptions are explicitly encoded and labeled in code/tests for Nick's checkpoint review
</success_criteria>

<output>
Create `.planning/phases/02-redemption-database/02-03-SUMMARY.md` when done
</output>
