---
phase: 03-valuation-ranking-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engine/types.ts
  - tests/engine-purity.test.ts
  - src/data/programs.ts
autonomous: false
requirements: [VAL-02, VAL-05]
must_haves:
  truths:
    - "A1 (cheapest path = min raw source points), A2 (conservative pointsMax gating), and the Bilt cash-out baseline are ratified by Nick and recorded in code comments"
    - "The engine's public result shape exists as plain hand-written interfaces with zero runtime imports"
    - "An executable purity test fails CI if any src/engine file imports next, react, zod (value), drizzle, the db layer, or node builtins"
  artifacts:
    - path: "src/engine/types.ts"
      provides: "Balances, EngineDataset, TransferPath, RankedResult, EngineOptions, RankedResults contracts"
      contains: "export interface RankedResult"
    - path: "tests/engine-purity.test.ts"
      provides: "Executable purity gate (phase success criterion 5)"
      contains: "node:fs"
  key_links:
    - from: "src/engine/types.ts"
      to: "src/data/types.ts"
      via: "type-only import"
      pattern: "import type \\{.*\\} from \"\\.\\./data/types\""
    - from: "tests/engine-purity.test.ts"
      to: "src/engine/"
      via: "fs.readdirSync over engine source files"
      pattern: "src/engine"
---

<objective>
Ratify the three open design assumptions with Nick (A1 cheapest-path rule, A2 conservative range gating, Bilt 0.1¢ cash-out baseline — the sign-off Phase 2 explicitly deferred to Phase 3), then lay the engine's foundation: the public type contracts every later plan implements against, and the automated purity gate that makes phase success criterion 5 ("imports nothing from Next.js, React, or the database layer") CI-executable before any new engine code exists.

Purpose: Interface-first — waves 2 and 3 implement against these contracts instead of inventing shapes, and the purity gate guards them from their first commit.
Output: Ratified assumptions recorded in code comments, `src/engine/types.ts`, `tests/engine-purity.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-valuation-ranking-engine/03-RESEARCH.md
@.planning/phases/03-valuation-ranking-engine/03-PATTERNS.md
@src/engine/transfers.ts
@src/data/types.ts
</context>

<interfaces>
<!-- Frozen Phase 2 primitives — consume, never modify (src/engine/transfers.ts): -->

```typescript
export function computePartnerPoints(route: TransferRouteSeed, sourcePoints: number): number
export function applyPromoBonus(basePartnerPoints: number, bonusPercent: number): number
```

<!-- Seed types available via type-only import from ../data/types (z.infer exports): -->

```typescript
export type ProgramSeed       // slug, name, kind, isUserEnterable, cashOutBaselineCppX100: number | null
export type TransferRouteSeed // fromProgramSlug, toProgramSlug, ratioNumerator, ratioDenominator, incrementPoints, bonusMilesPerBlock, bonusBlockPoints, transferTimeDays, active, notes
export type TransferBonusSeed // fromProgramSlug, toProgramSlug, bonusPercent, startDate, endDate, sourceNote
export type RedemptionSeed    // slug, partnerProgramSlug, title, category, pointsMin, pointsMax, taxesFeesCents, cashFareCents, availabilityRating, bookingHint, verifiedAt, ...
```
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Ratify A1, A2, and the Bilt cash-out baseline</name>
  <read_first>
    - .planning/phases/03-valuation-ranking-engine/03-RESEARCH.md (Assumptions Log + Pattern 4 + Pattern 7 + Open Question 1)
    - src/data/programs.ts (the bilt entry's cashOutBaselineCppX100 and its stand-in comment)
  </read_first>
  <what-built>Nothing yet — this checkpoint ratifies three design rules before any engine code encodes them. Phase 2's DATA-04 close-out explicitly deferred the Bilt baseline sign-off to "the Phase 3 methodology sign-off"; A1/A2 are [ASSUMED] recommendations from 03-RESEARCH.md.</what-built>
  <how-to-verify>
    Confirm or amend each of the following three rulings:

    1. **A1 — "Cheapest transfer path" = minimum raw requiredSourcePoints.** When multiple held programs reach the same partner, the engine picks the path needing the fewest raw source points; ties break to direct-use first, then lowest fromProgramSlug alphabetically. (Rejected alternative: opportunity-cost-cheapest — it makes null-baseline programs like Marriott/Hyatt/Hilton look "free" and lets path choice inflate the wow delta, a credibility hazard.)

    2. **A2 — Bookability and ranking gate on the conservative end: pointsMax ?? pointsMin.** Dynamic-priced awards (Hilton 160K–200K spreads) are only "bookable now" if the balance covers pointsMax; valuations are returned at BOTH ends so the UI can render honest ranges. (Alternative: optimistic pointsMin gating — moves more results into bookable-now but overpromises on exactly the programs where overpromising hurts most.)

    3. **Bilt cash-out baseline — keep the 0.1¢ stand-in (cashOutBaselineCppX100 = 10)?** Encodes "effectively no cash-out path" for Bilt. If you want a different figure, name the integer (100 = 1.0¢/pt scale). This value feeds every Bilt wow delta.

    FYI defaults that ship unless you object (no response needed): A3 overlapping promos on one route → apply highest bonusPercent; A5 engine itself filters verifiedAt:null drafts and active:false routes (fail-closed); A7 single-hop transfers only (no Chase→Marriott→Alaska chains); almost-there threshold defaults to 0.75 coverage, parameterized.
  </how-to-verify>
  <acceptance_criteria>
    - Nick has responded with either "approved" or explicit per-item amendments
    - Each amendment (if any) is restated as a concrete value or rule before Task 2 begins (e.g., "Bilt baseline = 25", "A2: gate on pointsMin")
  </acceptance_criteria>
  <action>Present the three rulings above to Nick exactly as written in <how-to-verify> (do not implement anything yet). Wait for the resume signal; restate any amendment as a concrete value/rule and carry it into Task 2. No files change in this task.</action>
  <verify>
    <human-check>Nick replies "approved" or lists per-item amendments</human-check>
  </verify>
  <done>All three rulings have an explicit outcome (default ratified or amended value recorded) before Task 2 starts.</done>
  <resume-signal>Type "approved" to ratify all three as recommended, or list amendments per item (e.g., "A1 ok, A2 ok, Bilt → 25").</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Engine type contracts + ratified-assumption recording</name>
  <files>src/engine/types.ts, src/data/programs.ts</files>
  <read_first>
    - src/engine/transfers.ts (module-header purity comment pattern, lines 1-10; assumption-provenance JSDoc style)
    - src/data/types.ts (Dataset interface shape lines 98-103; unit-comment convention, e.g. "// 100 = 1.0 cents/pt")
    - src/data/programs.ts (bilt entry + its stand-in comment; the 8 enterable program slugs)
    - .planning/phases/03-valuation-ranking-engine/03-RESEARCH.md (§"Engine result shape")
  </read_first>
  <action>
    Create `src/engine/types.ts` — plain hand-written interfaces, ZERO runtime imports (a value import of zod would fail Task 3's gate). Open with the transfers.ts-style module-header comment restating the purity + integer-math contract. The ONLY import: `import type { TransferBonusSeed, RedemptionSeed, ProgramSeed, TransferRouteSeed } from "../data/types";`

    Export exactly these (refine field docs, not shapes — waves 2/3 implement against them):
    - `type EnterableProgramSlug` — union of the 8 canonical slugs frozen by tests/seed-data.test.ts: "chase-ur" | "amex-mr" | "capital-one" | "citi-ty" | "bilt" | "world-of-hyatt" | "hilton-honors" | "marriott-bonvoy"
    - `type Balances = Partial<Record<EnterableProgramSlug, number>>`
    - `interface EngineDataset { programs: ProgramSeed[]; routes: TransferRouteSeed[]; bonuses: TransferBonusSeed[]; redemptions: RedemptionSeed[] }` (mirrors the non-exported Dataset in src/data/types.ts)
    - `interface TransferPath { kind: "direct" | "transfer"; fromProgramSlug: string; routeKey?: string; requiredSourcePoints: number; activeBonus: TransferBonusSeed | null }` — routeKey uses the existing `${from}→${to}` Unicode-arrow format; requiredSourcePoints is increment-aligned and promo-adjusted
    - `interface ValueRange { atMin: number; atMax: number | null }` — atMax null when redemption.pointsMax is null (fixed chart)
    - `interface RankedResult { redemption: RedemptionSeed; chosenPath: TransferPath; alternatePaths: TransferPath[]; pointsNeeded: { min: number; max: number | null }; cppX100: ValueRange; effectiveCppX100: ValueRange; wowDeltaCents: ValueRange; coverage: number; pointsAway: number | null }` — coverage is the single sanctioned float (display-only); pointsAway is non-null only for almost-there results, denominated in the chosen path's source currency
    - `interface EngineOptions { almostThereThreshold?: number }` — document default 0.75
    - `interface RankedResults { bookableNow: RankedResult[]; almostThere: RankedResult[] }`
    - `interface RankInput { balances: Balances; dataset: EngineDataset; asOf: string; options?: EngineOptions }` — asOf is an ISO YYYY-MM-DD string compared lexically; the engine never reads the clock

    Every money/scale field gets a unit comment (cents, cppX100 where 100 = 1.0¢/pt) per the src/data/types.ts convention. Fields encoding Task 1 rulings cite them: `// A1 (CONFIRMED by Nick <date>): ...`, `// A2 (CONFIRMED ...)`.

    In `src/data/programs.ts`: apply the Task 1 Bilt ruling. If the 0.1¢ stand-in was kept, replace the "final convention lands with the Phase 3 methodology sign-off" language with a CONFIRMED note citing the ruling date. If a new integer was chosen, change bilt's cashOutBaselineCppX100 to it and update the comment. Then run `npm test` — if any existing seed test pins the old value, update that expectation to match the ruling.
  </action>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/engine/types.ts` contains `export interface RankedResult`, `export interface TransferPath`, `export interface EngineDataset`, `export interface RankedResults`, `export type Balances`, and `export interface EngineOptions`
    - The only import statement in src/engine/types.ts is a type-only import from "../data/types" (grep: `import type` present; no other `from "` specifier outside `./`)
    - src/data/programs.ts no longer contains the phrase "Phase 3 methodology sign-off" as an open item; the bilt cashOutBaselineCppX100 matches Task 1's ruling
    - `npm run typecheck` exits 0; `npm test` exits 0 (existing 28 tests stay green)
  </acceptance_criteria>
  <done>Engine contracts exist as pure types; Bilt ruling is recorded in data + comments; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Executable purity gate</name>
  <files>tests/engine-purity.test.ts</files>
  <read_first>
    - tests/seed-data.test.ts (structural-gate style: header comment naming the invariant, descriptive failure messages)
    - src/engine/transfers.ts (lines 3-7 — the contract comment this test executes)
    - vitest.config.ts (confirm node environment + tests/**/*.test.ts include)
  </read_first>
  <behavior>
    - Test 1: every import/export-from specifier in every src/engine/*.ts file is either intra-engine relative (starts with "./") or exactly "../data/types" used in a statement matching /import\s+type\s/ — failure message names the offending file and specifier
    - Test 2: no import specifier in src/engine matches any forbidden pattern: "next", "react", "zod", "drizzle", "@neondatabase", "server-only", "node:", a "/db" path segment, "../app", or "@/"
    - Test 3: no src/engine source file contains "Date.now(" or "new Date(" (determinism guard — asOf is an input)
    - The suite passes against the current engine (transfers.ts + types.ts) — RED first only if a deliberate temp violation is used to prove the gate fires, then removed
  </behavior>
  <action>
    Create `tests/engine-purity.test.ts`. Use `node:fs` (readdirSync, readFileSync) and `node:path` — permitted in tests, never in the engine. Enumerate every `.ts` file under `src/engine/`, extract specifiers with a regex over statements like `import ... from "x"` / `export ... from "x"` (e.g. /(?:import|export)\s[^;]*?from\s+["']([^"']+)["']/g), and assert the allowlist/denylist from <behavior>. Note in the header comment that `import type ... from "../data/types"` is safe despite that file importing zod at runtime, because type-only imports erase at compile time; and that this test replaces the manual grep gate from plan 02-03 so phase success criterion 5 can never silently regress in Phase 4+.
  </action>
  <verify>
    <automated>npx vitest run tests/engine-purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/engine-purity.test.ts` exits 0
    - Proof the gate fires: temporarily adding `import { z } from "zod";` to src/engine/types.ts makes the test fail with a message naming types.ts and "zod", then the temp line is removed and the test passes again (record the failing output in the summary)
    - tests/engine-purity.test.ts contains the string "node:fs" and enumerates src/engine dynamically (no hardcoded file list that would miss future engine files)
  </acceptance_criteria>
  <done>Success criterion 5 is an executable CI gate that already guards transfers.ts and types.ts, and will automatically cover paths/valuation/ranking/index as waves 2-3 add them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed data → engine | Repo-controlled, CI-validated data; not user input in this phase |
| engine → future callers (Phase 4 UI, v2 advisor) | Purity boundary — no framework/DB/env access may leak in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (integrity) | Bilt baseline in src/data/programs.ts feeding every Bilt wow delta | mitigate | Blocking human checkpoint (Task 1) ratifies the value before any delta ships; ruling recorded in code comment |
| T-03-02 | Tampering (boundary erosion) | src/engine/* import surface | mitigate | tests/engine-purity.test.ts fails CI on any framework/DB/node/clock import (Task 3) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: nothing to audit); `npm ci` state unchanged |
</threat_model>

<verification>
- `npm test` green (existing transfers/seed-data suites untouched and passing)
- `npm run typecheck` and `npm run lint` exit 0
- `npx vitest run tests/engine-purity.test.ts` passes and demonstrably fails on a deliberate violation
</verification>

<success_criteria>
- A1, A2, and the Bilt baseline are ratified (or amended) by Nick and cited in code comments with dates
- src/engine/types.ts exports the full engine contract surface with zero runtime imports
- Purity gate is executable and dynamic over src/engine/
</success_criteria>

<output>
Create `.planning/phases/03-valuation-ranking-engine/03-01-SUMMARY.md` when done. Record the exact rulings (A1/A2/Bilt) — wave 2 plans encode them.
</output>
