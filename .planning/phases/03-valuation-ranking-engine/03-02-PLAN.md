---
phase: 03-valuation-ranking-engine
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/engine/paths.ts
  - tests/engine-paths.test.ts
autonomous: true
requirements: [VAL-05]
must_haves:
  truths:
    - "requiredSourcePoints inverts the frozen transfer math exactly: 60,000 Alaska miles via Marriott costs 150,000 Bonvoy (block bonus honored), never the naive 180,000"
    - "An active promo lowers requiredSourcePoints (200,000 Hilton via Amex costs 77,000 MR during the +30% window, 100,000 MR after it) and never stacks with block bonuses (A4)"
    - "When multiple held programs reach the same partner, the chosen path needs the fewest raw source points, with deterministic tie-breaks (A1)"
  artifacts:
    - path: "src/engine/paths.ts"
      provides: "activeBonusFor, effectivePartnerPoints, requiredSourcePoints, resolvePaths"
      contains: "export function requiredSourcePoints"
    - path: "tests/engine-paths.test.ts"
      provides: "Inverse-math + cheapest-path unit tests against real seed rows (SC-2, SC-4)"
      min_lines: 80
  key_links:
    - from: "src/engine/paths.ts"
      to: "src/engine/transfers.ts"
      via: "composition of frozen primitives"
      pattern: "import \\{.*computePartnerPoints.*\\} from \"\\./transfers\""
    - from: "tests/engine-paths.test.ts"
      to: "src/data/transfers.ts"
      via: "real seed-row fixtures"
      pattern: "from \"\\.\\./src/data"
---

<objective>
Build `src/engine/paths.ts` — the phase's real algorithm — via TDD: active-bonus resolution, the A4-safe effective conversion, binary-search inverse transfer math, and A1 cheapest-path selection. Every expectation is hand-computed against REAL seed rows so a data typo fails CI exactly like a math regression.

Purpose: bookability, points-away, and effective cost all hang off "how many source points do I need?" — which naive ratio division gets wrong by up to 30,000 points on the route experts check first.
Output: Four pure functions in paths.ts + tests/engine-paths.test.ts, all increment-aligned integer math.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-valuation-ranking-engine/03-RESEARCH.md
@.planning/phases/03-valuation-ranking-engine/03-PATTERNS.md
@.planning/phases/03-valuation-ranking-engine/03-01-SUMMARY.md
@src/engine/transfers.ts
@src/engine/types.ts
@src/data/transfers.ts
@tests/transfers.test.ts
</context>

<interfaces>
<!-- Frozen primitives to compose (src/engine/transfers.ts — DO NOT MODIFY): -->

```typescript
export function computePartnerPoints(route: TransferRouteSeed, sourcePoints: number): number
// floor to incrementPoints → floor(× ratioNumerator / ratioDenominator) → + block bonus per full bonusBlockPoints

export function applyPromoBonus(basePartnerPoints: number, bonusPercent: number): number
// floor(base × (100 + pct) / 100); A4: caller passes BASE-ONLY conversion when a promo applies
```

<!-- Contracts from 03-01 (src/engine/types.ts — read the actual file; shape as planned): -->

```typescript
export type Balances = Partial<Record<EnterableProgramSlug, number>>
export interface EngineDataset { programs; routes; bonuses; redemptions }
export interface TransferPath {
  kind: "direct" | "transfer";
  fromProgramSlug: string;
  routeKey?: string;                     // `${from}→${to}` Unicode arrow
  requiredSourcePoints: number;          // increment-aligned, promo-adjusted
  activeBonus: TransferBonusSeed | null;
}
```

<!-- Real fixture rows (verified in src/data/transfers.ts by 03-PATTERNS.md): -->
<!-- marriott-bonvoy→alaska-mileage-plan: 1:3 (ratioNumerator 1, ratioDenominator 3), incrementPoints 3000, bonus 5000 per 60000 -->
<!-- bilt→alaska-mileage-plan: 1:1, incrementPoints 1000 -->
<!-- amex-mr→hilton-honors: ratioNumerator 2, ratioDenominator 1 -->
<!-- Live bonus row: amex-mr→hilton-honors, bonusPercent 30, startDate "2026-09-01", endDate "2026-10-14" -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: activeBonusFor + effectivePartnerPoints (A4 branch)</name>
  <files>src/engine/paths.ts, tests/engine-paths.test.ts</files>
  <read_first>
    - src/engine/transfers.ts (whole file — primitives being composed, header comment pattern to copy, A4 JSDoc)
    - src/engine/types.ts (contracts created by 03-01; confirm actual field names)
    - tests/transfers.test.ts (test structure to copy: findRoute helper, hand-computed it-titles, underscore literals)
    - src/data/transfers.ts (the live Amex→Hilton bonus row's exact fields; route() helper style for the synthetic A4 fixture)
    - .planning/phases/03-valuation-ranking-engine/03-01-SUMMARY.md (ratified A1/A2/A3 rulings and any amendments)
  </read_first>
  <behavior>
    - activeBonusFor(route, bonuses, "2026-09-01") and ("2026-10-14") return the live Amex→Hilton bonus row (boundary-inclusive); ("2026-08-31") and ("2026-10-15") return null — lexical string comparison, never Date parsing
    - With two synthetic overlapping bonuses on one route (20% and 30%), activeBonusFor returns the 30% one (A3)
    - effectivePartnerPoints(marriottAlaskaRoute, null, 150_000) === 60_000 (base 50,000 + 2 × 5,000 block bonus — delegates to computePartnerPoints)
    - effectivePartnerPoints(marriottAlaskaRoute, syntheticBonus20pct, 150_000) === 60_000 — base-ONLY 50,000 × 1.20, NOT (50,000 + 10,000) × 1.20 = 72,000; this pins A4 non-stacking with a synthetic route+promo pair no real data has yet
    - effectivePartnerPoints(amexHiltonRoute, liveBonus30pct, 10_000) === 26_000 (base 20,000 × 1.30 — matches the frozen Phase 2 promo composition test)
  </behavior>
  <action>
    RED: create `tests/engine-paths.test.ts` copying the tests/transfers.test.ts structure — vitest named imports, real rows from ../src/data/transfers via a throwing findRoute/findBonus helper, rationale header comment, hand-computed arithmetic in it-titles. Build the synthetic A4 fixture with the spread-override style ({ ...marriottRoute } or the route()-helper pattern) typed `satisfies TransferRouteSeed`/`satisfies TransferBonusSeed`. Commit failing tests: `test(03-02): add failing tests for active-bonus + effective conversion`.

    GREEN: create `src/engine/paths.ts` with the transfers.ts-style purity header comment. Imports: `import { applyPromoBonus, computePartnerPoints } from "./transfers";` plus type-only from "../data/types" and engine types from "./types". Implement:
    - `activeBonusFor(route: TransferRouteSeed, bonuses: TransferBonusSeed[], asOf: string): TransferBonusSeed | null` — filter matching fromProgramSlug/toProgramSlug with `b.startDate <= asOf && asOf <= b.endDate`; on multiple matches return highest bonusPercent (A3 comment with ruling date); else null
    - `effectivePartnerPoints(route: TransferRouteSeed, bonus: TransferBonusSeed | null, sourcePoints: number): number` — bonus null → computePartnerPoints(route, sourcePoints); else compute base-only via `computePartnerPoints({ ...route, bonusMilesPerBlock: null, bonusBlockPoints: null }, sourcePoints)` then applyPromoBonus(baseOnly, bonus.bonusPercent). JSDoc cites A4 (CONFIRMED by Nick 2026-09-01).
    Commit: `feat(03-02): active-bonus window + A4-safe effective conversion`.
  </action>
  <verify>
    <automated>npx vitest run tests/engine-paths.test.ts && npx vitest run tests/engine-purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/paths.ts contains `export function activeBonusFor(` and `export function effectivePartnerPoints(`
    - tests assert the A4 non-stacking case: synthetic route with BOTH block bonus and promo yields floor(baseOnly × 1.20), with the compounded value (72_000) explicitly ruled out in a comment or assertion
    - Boundary dates "2026-09-01" / "2026-10-14" active and "2026-10-15" inactive are each pinned by a test
    - `npx vitest run tests/engine-paths.test.ts` exits 0; purity test still exits 0
    - Two commits exist: a failing-test commit (test(03-02)) then an implementation commit (feat(03-02))
  </acceptance_criteria>
  <done>Bonus-window and effective-conversion semantics are frozen by tests against the live bonus row and a synthetic A4 fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: requiredSourcePoints — binary-search inverse transfer math</name>
  <files>src/engine/paths.ts, tests/engine-paths.test.ts</files>
  <read_first>
    - src/engine/paths.ts (Task 1's effectivePartnerPoints — the function being inverted)
    - .planning/phases/03-valuation-ranking-engine/03-RESEARCH.md (Pattern 3 — bound derivation and search sketch; Pitfall 2)
  </read_first>
  <behavior>
    - requiredSourcePoints(marriottAlaskaRoute, null, 60_000) === 150_000 — NOT naive 180_000 (base 50,000 + two 5K block bonuses); companion assertion: effectivePartnerPoints(marriottAlaskaRoute, null, 147_000) === 59_000, proving 150,000 is minimal
    - requiredSourcePoints(amexHiltonRoute, liveBonus30pct, 200_000) === 77_000 (154,000 base × 1.30 = 200,200 ≥ 200,000; 76,000 → 197,600 fails)
    - requiredSourcePoints(amexHiltonRoute, null, 200_000) === 100_000 (no promo)
    - requiredSourcePoints(biltAlaskaRoute, null, 60_000) === 60_000 (1:1, increment 1000)
    - requiredSourcePoints(route, bonus, 0) === 0; result % route.incrementPoints === 0 for a property-style spread of needed values on the Marriott route (increment alignment by construction)
  </behavior>
  <action>
    RED first (test(03-02) commit), then GREEN (feat(03-02) commit).

    Implement in src/engine/paths.ts: `requiredSourcePoints(route: TransferRouteSeed, bonus: TransferBonusSeed | null, partnerPointsNeeded: number): number | null` — the minimum increment-multiple source amount whose effectivePartnerPoints covers the need. Algorithm (RESEARCH Pattern 3): upper bound = ceil(needed × ratioDenominator / ratioNumerator) rounded UP to incrementPoints (valid because block bonuses and promos only ADD partner points, so the base-ratio ceiling always suffices); binary search k ∈ [0, upper/incrementPoints] for the smallest k with effectivePartnerPoints(route, bonus, k × incrementPoints) ≥ needed (monotonic non-decreasing, so exact); return k × incrementPoints, or null if even the upper bound fails (defensive — unreachable for valid routes, keep the branch). Integer-only: Math.ceil/Math.floor on integers; JSDoc explains WHY naive division is wrong (the Marriott 180K-vs-150K example) — this doc feeds the Phase 5 methodology page.
  </action>
  <verify>
    <automated>npx vitest run tests/engine-paths.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/paths.ts contains `export function requiredSourcePoints(`
    - Test titles pin: Marriott 60_000 → 150_000; Amex+promo 200_000 → 77_000; Amex no-promo → 100_000; Bilt 60_000 → 60_000
    - An increment-alignment assertion exists (result modulo incrementPoints === 0 across multiple needed values)
    - `npx vitest run tests/engine-paths.test.ts` exits 0
  </acceptance_criteria>
  <done>Inverse math is exact, increment-aligned, promo-aware, and frozen against real Marriott/Bilt/Amex rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: resolvePaths — A1 cheapest-path selection</name>
  <files>src/engine/paths.ts, tests/engine-paths.test.ts</files>
  <read_first>
    - src/engine/paths.ts (Tasks 1-2 functions being composed)
    - src/engine/types.ts (TransferPath shape — kind, routeKey, activeBonus fields)
    - .planning/phases/03-valuation-ranking-engine/03-01-SUMMARY.md (exact ratified A1 rule and tie-breaks)
  </read_first>
  <behavior>
    - balances { bilt: 70_000, "marriott-bonvoy": 200_000 }, partner alaska-mileage-plan, needed 60_000: chosenPath.fromProgramSlug === "bilt" with requiredSourcePoints 60_000; alternatePaths contains the Marriott path with requiredSourcePoints 150_000 (min raw source points wins)
    - balances { "hilton-honors": 250_000, "amex-mr": 300_000 }, partner hilton-honors, needed 200_000, asOf "2026-09-15": chosenPath is the amex-mr transfer path (77_000 < direct 200_000) with activeBonus non-null (bonusPercent 30) and routeKey "amex-mr→hilton-honors"; same balances at asOf "2026-10-15": chosen is still amex (100_000 < 200_000) but activeBonus is null
    - Tie-break: when direct and a transfer path need equal points, direct wins (kind "direct"); among equal transfer paths, lowest fromProgramSlug alphabetically
    - No affordable path: balances { "marriott-bonvoy": 100_000 }, needed 60_000 Alaska → chosenPath is the Marriott path (max coverage among unaffordable candidates), requiredSourcePoints 150_000
    - Inactive routes (active: false) and programs absent from balances (or with balance ≤ 0) never appear as candidates; returns null when no held program reaches the partner at all
  </behavior>
  <action>
    RED then GREEN, two commits as before.

    Implement: `resolvePaths(partnerProgramSlug: string, partnerPointsNeeded: number, balances: Balances, dataset: EngineDataset, asOf: string): { chosenPath: TransferPath; alternatePaths: TransferPath[] } | null`.

    Candidates: (a) direct-use if balances[partnerProgramSlug] is a positive number — TransferPath { kind: "direct", fromProgramSlug: partnerProgramSlug, requiredSourcePoints: partnerPointsNeeded, activeBonus: null }, no routeKey; (b) every route in dataset.routes with toProgramSlug === partnerProgramSlug, active === true, and a positive balance for fromProgramSlug — kind "transfer", routeKey `${fromProgramSlug}→${toProgramSlug}` (Unicode arrow, matches validateDataset's format), requiredSourcePoints via Task 2 with activeBonusFor's bonus (skip candidates where requiredSourcePoints is null). Single hop only — A7 comment.

    Selection (A1, cite ruling + date): partition candidates by affordable = balances[fromProgramSlug] >= requiredSourcePoints. If any affordable → chosen = minimum requiredSourcePoints; ties → prefer kind "direct", then lowest fromProgramSlug (localeCompare-free: plain < on ASCII slugs). Else → chosen = maximum coverage (balances[from] / requiredSourcePoints — comparison only, no float stored here), same tie-breaks. alternatePaths = remaining candidates sorted by requiredSourcePoints ascending then fromProgramSlug. Return null when candidates is empty.
  </action>
  <verify>
    <automated>npx vitest run tests/engine-paths.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/paths.ts contains `export function resolvePaths(`
    - The Bilt-vs-Marriott (chosen bilt 60_000) and direct-Hilton-vs-Amex-promo (chosen amex-mr 77_000, activeBonus.bonusPercent === 30) fixtures both pass
    - A test asserts resolvePaths returns null when balances hold no program reaching the partner
    - `npm test` exits 0 (full suite including purity gate and untouched Phase 2 suites)
  </acceptance_criteria>
  <done>Cheapest-path resolution (SC-4) is implemented per ratified A1 and frozen by two real multi-path fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| balances argument → paths functions | Junk values (negative, NaN) are possible from future callers; resolvePaths treats non-positive balances as absent (full defensive coercion is ranking's job in 03-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering (integrity) | requiredSourcePoints — silent overshoot/undershoot = product-destroying wrong numbers | mitigate | Hand-computed frozen tests vs real seed rows (150K-not-180K, 77K-vs-100K); minimality proven by the 147K counter-case |
| T-03-04 | Tampering (integrity) | A4 stacking regression compounding bonuses | mitigate | Synthetic route+promo test pins base-only branch — exists precisely because no real row has both yet |
| T-03-05 | DoS | Unbounded search loop on hostile partnerPointsNeeded | mitigate | Binary search over a finite ceiling bound; needed ≤ 0 short-circuits to 0 |
| T-03-SC | Tampering | Package installs | accept | None this phase |
</threat_model>

<verification>
- `npx vitest run tests/engine-paths.test.ts` green; `npm test` green; `npx vitest run tests/engine-purity.test.ts` green (paths.ts is auto-covered by the wave-1 gate)
- `npm run typecheck && npm run lint` exit 0
</verification>

<success_criteria>
- Phase SC-2: inverse math exact and increment-aligned (Marriott 150K fixture)
- Phase SC-4: cheapest path per ratified A1 with deterministic tie-breaks
- VAL-05 (path half): active bonus lowers requiredSourcePoints and is surfaced on TransferPath.activeBonus
</success_criteria>

<output>
Create `.planning/phases/03-valuation-ranking-engine/03-02-SUMMARY.md` when done.
</output>
