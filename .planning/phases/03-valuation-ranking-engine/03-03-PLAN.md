---
phase: 03-valuation-ranking-engine
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/engine/valuation.ts
  - tests/engine-valuation.test.ts
autonomous: true
requirements: [VAL-02]
must_haves:
  truths:
    - "cppX100 matches hand-computed TPG examples: ANA business 933 (9.33 cpp), ANA First via Virgin 1876 — taxes subtracted, cents-unit mapping correct (no 100× inflation)"
    - "Wow delta uses per-program cash-out baselines from the dataset, never a flat 1¢; null baselines (hotel currencies) contribute a cash-out value of 0"
    - "All money math is integer (cents, cppX100); no floats in finance numbers"
  artifacts:
    - path: "src/engine/valuation.ts"
      provides: "cppX100, effectiveCppX100, cashOutValueCents, wowDeltaCents"
      contains: "export function cppX100"
    - path: "tests/engine-valuation.test.ts"
      provides: "VAL-02 hand-computed cpp anchors + wow-delta + null-baseline tests"
      min_lines: 60
  key_links:
    - from: "tests/engine-valuation.test.ts"
      to: "src/data/programs.ts"
      via: "real baseline fixtures (chase-ur 100, bilt per 03-01 ruling, hotels null)"
      pattern: "cashOutBaselineCppX100"
    - from: "src/engine/valuation.ts"
      to: "src/engine/types.ts"
      via: "type-only engine imports"
      pattern: "from \"\\./types\"|from \"\\.\\./data/types\""
---

<objective>
Build `src/engine/valuation.ts` via TDD: the TPG cents-per-point formula in exact integer units (VAL-02), the per-source-point effective cpp that transfer bonuses improve, and the wow-delta computation with per-program cash-out baselines. Anchored to hand-computed examples from real verified seed rows so the formula can never drift 100× (the cents-vs-dollars pitfall).

Purpose: these four functions ARE the product's finance-credibility claim — every number Phase 4 displays and Phase 5's methodology page defends comes from here.
Output: src/engine/valuation.ts + tests/engine-valuation.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-valuation-ranking-engine/03-RESEARCH.md
@.planning/phases/03-valuation-ranking-engine/03-PATTERNS.md
@.planning/phases/03-valuation-ranking-engine/03-01-SUMMARY.md
@src/engine/transfers.ts
@src/engine/types.ts
@src/data/programs.ts
@tests/transfers.test.ts
</context>

<interfaces>
<!-- Unit conventions (frozen Phase 2): money in integer cents; cpp as integer cppX100 where 100 = 1.0 cents/pt -->
<!-- (same scale as ProgramSeed.cashOutBaselineCppX100 in src/data/programs.ts). -->

<!-- Real baselines (src/data/programs.ts): chase-ur 100, citi-ty 100, amex-mr 60, capital-one 50, -->
<!-- bilt 10 (or the 03-01 ratified value — read the file), world-of-hyatt/hilton-honors/marriott-bonvoy null. -->

<!-- VAL-02 formula in cents (RESEARCH Pattern 5): the TPG dollars-form "×100" vanishes in cents; -->
<!-- to stay integer at cppX100 scale: cppX100 = round(((cashFareCents − taxesFeesCents) × 100) / points). -->

<!-- Hand-computed anchors from real verified rows: -->
<!-- ana-business-tokyo-roundtrip: cashFareCents 900_000, taxesFeesCents 60_000, pointsMax 90_000 → cppX100 = 933 -->
<!-- ANA First via Virgin row: cashFareCents 1_400_000, taxesFeesCents 40_000, points 72_500 → cppX100 = 1876 -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cppX100 + effectiveCppX100 (VAL-02 formula, integer units)</name>
  <files>src/engine/valuation.ts, tests/engine-valuation.test.ts</files>
  <read_first>
    - src/engine/types.ts (03-01 contracts; confirm ValueRange field names for later consumers)
    - src/engine/transfers.ts (purity header pattern + integer-math comment to copy)
    - tests/transfers.test.ts (test-file structure: real-row rationale header, throwing lookup helpers, hand-computed it-titles)
    - src/data/redemptions-flights.ts (find the exact ANA business + ANA First row slugs and their cashFareCents/taxesFeesCents/points fields — verify the anchor arithmetic against the real rows before writing expectations)
  </read_first>
  <behavior>
    - cppX100(900_000, 60_000, 90_000) === 933 — it-title shows the TPG dollars-form check: ($9,000 − $600) ÷ 90,000 × 100 = 9.33 cpp
    - cppX100(1_400_000, 40_000, 72_500) === 1876 (ANA First via Virgin)
    - The same two cases asserted against the REAL redemption rows (look up by slug via a throwing findRedemption helper, feed the row's own cashFareCents/taxesFeesCents/points) — a seed typo fails CI like a math bug
    - effectiveCppX100 is the identical formula over requiredSourcePoints: effectiveCppX100 with 77_000 source points > effectiveCppX100 with 100_000 for the same fare/taxes (an active promo raises per-source-point value — the VAL-05 mechanism)
    - Guard: points ≤ 0 or non-finite inputs → both functions return 0 (never NaN/Infinity out)
  </behavior>
  <action>
    RED: create `tests/engine-valuation.test.ts` (transfers.test.ts structure; import real rows from ../src/data — redemptions arrays — with a throwing findRedemption(slug) helper). Commit `test(03-03): add failing TPG cpp anchor tests`.

    GREEN: create `src/engine/valuation.ts` with the standard purity/integer header. Implement:
    - `cppX100(cashFareCents: number, taxesFeesCents: number, partnerPoints: number): number` — `Math.round(((cashFareCents - taxesFeesCents) * 100) / partnerPoints)`; JSDoc states the TPG convention verbatim ((cash fare − taxes/fees) ÷ points × 100), the cents-unit mapping (the ×100 in dollars-form cancels; the surviving ×100 is the cppX100 scale), and cites VAL-02. Return 0 on partnerPoints ≤ 0 or any non-finite input.
    - `effectiveCppX100(cashFareCents: number, taxesFeesCents: number, requiredSourcePoints: number): number` — same formula over source points; JSDoc: this is the number an active transfer bonus improves (VAL-05); both figures are returned to callers deliberately (partner-point cpp is redemption-intrinsic, source-point cpp is path-dependent).
    Commit `feat(03-03): TPG cppX100 + effectiveCppX100 in integer units`.
  </action>
  <verify>
    <automated>npx vitest run tests/engine-valuation.test.ts && npx vitest run tests/engine-purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/valuation.ts contains `export function cppX100(` and `export function effectiveCppX100(`
    - Tests pin 933 and 1876 both from literal inputs AND from the real rows' own fields
    - A test rules out the 100× unit bug: cppX100(900_000, 60_000, 90_000) is 933, not 93_333 (comment naming Pitfall 1)
    - `npx vitest run tests/engine-valuation.test.ts` exits 0; purity gate exits 0
  </acceptance_criteria>
  <done>VAL-02's formula is implemented once, unit-safe, and frozen against hand-computed real-row anchors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cashOutValueCents + wowDeltaCents (per-program baselines)</name>
  <files>src/engine/valuation.ts, tests/engine-valuation.test.ts</files>
  <read_first>
    - src/engine/valuation.ts (Task 1 state)
    - src/data/programs.ts (exact cashOutBaselineCppX100 values including the 03-01 Bilt ruling; ProgramSeed shape)
    - .planning/phases/03-valuation-ranking-engine/03-RESEARCH.md (Pattern 6 — spentSourcePoints semantics, taxes on the value side)
  </read_first>
  <behavior>
    - cashOutValueCents(90_000, chaseUrProgram) === 90_000 (baseline 100 → floor(90,000 × 100 / 100) = 90,000 cents = $900)
    - cashOutValueCents(60_000, biltProgram) === floor(60_000 × biltBaseline / 100) — with the stand-in 10 that's 6_000 cents ($60); use the ratified value from src/data/programs.ts
    - cashOutValueCents(anyPoints, worldOfHyattProgram) === 0 (null baseline → no cash-out path; cite the "null ⇒ 0" rule)
    - wowDeltaCents(900_000, 60_000, 90_000, chaseUrProgram) === 750_000 — (840,000 net cash value) − 90,000 cash-out = $7,500 delta
    - wowDeltaCents with a Bilt spend of 60_000 on the same fare === 840_000 − cashOutValueCents(60_000, bilt) (with baseline 10: 834_000)
    - wowDeltaCents with a null-baseline program === the full net cash value (840_000)
  </behavior>
  <action>
    RED then GREEN, two commits (test(03-03) / feat(03-03)).

    Implement in src/engine/valuation.ts:
    - `cashOutValueCents(spentSourcePoints: number, sourceProgram: ProgramSeed): number` — `sourceProgram.cashOutBaselineCppX100 === null ? 0 : Math.floor((spentSourcePoints * sourceProgram.cashOutBaselineCppX100) / 100)`. JSDoc: per-program baselines, never a flat 1¢ (Pitfall: flat baselines are the attackable-methodology failure); null = partner-only currency with no cash-out path.
    - `wowDeltaCents(cashFareCents: number, taxesFeesCents: number, spentSourcePoints: number, sourceProgram: ProgramSeed): number` — `(cashFareCents - taxesFeesCents) - cashOutValueCents(spentSourcePoints, sourceProgram)`. JSDoc: RANK-01's definition (transfer-partner value minus cash-out value); taxes subtracted on the value side to stay consistent with the cpp numerator (you pay taxes in cash either way); spentSourcePoints is the chosen path's increment-aligned requiredSourcePoints, NOT the full balance.
    Tests use real ProgramSeed rows from ../src/data/programs via a throwing findProgram(slug) helper.
  </action>
  <verify>
    <automated>npx vitest run tests/engine-valuation.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/valuation.ts contains `export function cashOutValueCents(` and `export function wowDeltaCents(`
    - A null-baseline test uses a real hotel program row (world-of-hyatt, hilton-honors, or marriott-bonvoy) and asserts 0 cash-out / full-net-value delta
    - The chase-ur 750_000-cent delta anchor passes; the Bilt case reads the ratified baseline from the real row (no hardcoded 10 if 03-01 changed it)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Wow delta is program-honest, integer-only, and frozen against real baseline rows — the hero number is defensible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| numeric arguments → valuation functions | Callers (ranking, future advisor) may pass junk; functions return 0 rather than NaN on non-finite/non-positive divisors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering (integrity) | cppX100 unit mapping — a 100× error destroys the credibility claim | mitigate | Hand-computed anchors (933, 1876) checked in TPG dollars-form in test titles; explicit not-93_333 assertion |
| T-03-07 | Tampering (integrity) | Flat-baseline or wrong-baseline wow deltas (attackable methodology) | mitigate | Baselines read from ProgramSeed rows only; null-baseline and per-program tests against real data |
| T-03-08 | DoS/Integrity | Division by zero / NaN propagation | mitigate | points ≤ 0 and non-finite guards returning 0, pinned by tests |
| T-03-SC | Tampering | Package installs | accept | None this phase |
</threat_model>

<verification>
- `npx vitest run tests/engine-valuation.test.ts` green; `npm test` green; purity gate green (valuation.ts auto-covered)
- `npm run typecheck && npm run lint` exit 0
</verification>

<success_criteria>
- VAL-02: cpp follows the TPG convention, verified by unit tests against hand-computed examples from real rows
- Wow delta uses per-program baselines with null → 0; all finance math integer-only
- effectiveCppX100 exists as the bonus-sensitive figure 03-04 wires into results (VAL-05's valuation half)
</success_criteria>

<output>
Create `.planning/phases/03-valuation-ranking-engine/03-03-SUMMARY.md` when done.
</output>
