---
phase: 03-valuation-ranking-engine
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/engine/ranking.ts
  - src/engine/index.ts
  - tests/engine-ranking.test.ts
autonomous: true
requirements: [VAL-02, VAL-05]
must_haves:
  truths:
    - "Given balances, rankRedemptions returns bookable-now redemptions ranked by wow delta desc plus an almost-there set with points-away amounts (phase SC-1)"
    - "An active transfer bonus end-to-end lowers requiredSourcePoints, raises effectiveCppX100, and is surfaced on the result; the same call at an asOf past endDate shows no bonus (VAL-05)"
    - "Draft redemptions (verifiedAt null) never appear in any partition for any balance; hostile balances (negative, NaN, Infinity, huge) never produce NaN outputs"
    - "The engine has a single public barrel that Phase 4 and the v2 advisor import"
  artifacts:
    - path: "src/engine/ranking.ts"
      provides: "rankRedemptions orchestrator (filter → resolve → value → partition → sort)"
      contains: "export function rankRedemptions"
    - path: "src/engine/index.ts"
      provides: "Public engine barrel"
      contains: "export * from \"./ranking\""
    - path: "tests/engine-ranking.test.ts"
      provides: "VAL-05 end-to-end, partition/sort/threshold, edge-balance, draft-exclusion tests"
      min_lines: 100
  key_links:
    - from: "src/engine/ranking.ts"
      to: "src/engine/paths.ts"
      via: "resolvePaths composition"
      pattern: "from \"\\./paths\""
    - from: "src/engine/ranking.ts"
      to: "src/engine/valuation.ts"
      via: "cpp/wow-delta composition"
      pattern: "from \"\\./valuation\""
---

<objective>
Build `src/engine/ranking.ts` — the orchestrator that turns { balances, dataset, asOf, options } into { bookableNow, almostThere } — plus the public barrel `src/engine/index.ts`. This closes the phase: VAL-05 becomes verifiable end-to-end (a live bonus visibly changes cost, cpp, and surfaces on the result), and the sealed module ships behind one import surface.

Purpose: this is the exact function Phase 4's results page renders and the v2 advisor later calls as a tool — plain data in, plain data out.
Output: rankRedemptions + barrel + the phase's heaviest test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-valuation-ranking-engine/03-RESEARCH.md
@.planning/phases/03-valuation-ranking-engine/03-PATTERNS.md
@.planning/phases/03-valuation-ranking-engine/03-01-SUMMARY.md
@src/engine/types.ts
@src/engine/paths.ts
@src/engine/valuation.ts
@src/data/index.ts
@tests/seed-data.test.ts
</context>

<interfaces>
<!-- Wave-2 functions being orchestrated (read actual files for exact signatures): -->

```typescript
// paths.ts (03-02)
export function activeBonusFor(route, bonuses, asOf): TransferBonusSeed | null
export function effectivePartnerPoints(route, bonus, sourcePoints): number
export function requiredSourcePoints(route, bonus, partnerPointsNeeded): number | null
export function resolvePaths(partnerProgramSlug, partnerPointsNeeded, balances, dataset, asOf):
  { chosenPath: TransferPath; alternatePaths: TransferPath[] } | null

// valuation.ts (03-03)
export function cppX100(cashFareCents, taxesFeesCents, partnerPoints): number
export function effectiveCppX100(cashFareCents, taxesFeesCents, requiredSourcePoints): number
export function cashOutValueCents(spentSourcePoints, sourceProgram): number
export function wowDeltaCents(cashFareCents, taxesFeesCents, spentSourcePoints, sourceProgram): number

// types.ts (03-01)
export interface RankInput { balances; dataset; asOf; options? }
export interface RankedResults { bookableNow: RankedResult[]; almostThere: RankedResult[] }
```

<!-- Known draft rows that must NEVER surface: st-regis-maldives, gritti-palace-venice (verifiedAt: null). -->
<!-- Barrel analog: src/data/index.ts — header comment + export * lines; note the filename collision -->
<!-- hazard: src/engine/transfers.ts vs src/data/transfers.ts (consumers importing both must alias). -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rankRedemptions core — filter, resolve, value, partition, sort</name>
  <files>src/engine/ranking.ts, tests/engine-ranking.test.ts</files>
  <read_first>
    - src/engine/paths.ts and src/engine/valuation.ts (exact wave-2 signatures being composed)
    - src/engine/types.ts (RankInput/RankedResult/RankedResults field names — the contract this must satisfy)
    - src/data/types.ts (validateDataset lines 147-213 — the repo's dataset-iteration orchestration shape)
    - tests/seed-data.test.ts (collection-iteration test style with descriptive expect messages)
    - .planning/phases/03-valuation-ranking-engine/03-01-SUMMARY.md (ratified A2 gating rule + threshold default)
  </read_first>
  <behavior>
    - With balances covering a redemption's conservative need (pointsMax ?? pointsMin, per A2), the result lands in bookableNow with coverage ≥ 1 and pointsAway null; bookableNow is sorted by conservative wowDeltaCents (atMax ?? atMin) descending, ties by redemption.slug ascending
    - With coverage in [0.75, 1), the result lands in almostThere with pointsAway === chosenPath.requiredSourcePoints − balance (positive integer, in the chosen path's source currency); almostThere sorts by coverage descending, ties by slug
    - Coverage below the threshold → excluded entirely; options { almostThereThreshold: 0.5 } widens the set (a redemption at coverage 0.6 appears only with the override)
    - Redemptions whose partner no held program reaches are absent from both partitions
    - Every result carries: cppX100 and wowDeltaCents at both ends (atMax null when pointsMax null), effectiveCppX100 over the chosen path, chosenPath + alternatePaths, redemption passthrough (availabilityRating, verifiedAt, bookingHint reachable via result.redemption)
  </behavior>
  <action>
    RED: create `tests/engine-ranking.test.ts` importing the REAL dataset from ../src/data (programs, routes, bonuses, redemptions arrays — assemble the EngineDataset argument in the test) and pinning asOf values. Craft balance fixtures that hit each partition using real rows (e.g., a chase-ur balance covering a Hyatt redemption's need; a balance at ~80% of a conservative requirement for almost-there). Commit `test(03-04): failing ranking partition/sort/threshold tests`.

    GREEN: create `src/engine/ranking.ts` (standard purity header; imports only from ./paths, ./valuation, ./types, plus type-only ../data/types). Implement `rankRedemptions(input: RankInput): RankedResults`:
    1. Sanitize balances: keep entries where Number.isSafeInteger(value) && value > 0 (defensive; cite Pitfall 6). Sanitized map is used everywhere below.
    2. Filter dataset.redemptions to verifiedAt !== null (A5, fail-closed; inactive routes are already excluded inside resolvePaths).
    3. Per redemption: conservativeNeed = pointsMax ?? pointsMin (A2, cite ruling). resolvePaths(partnerProgramSlug, conservativeNeed, sanitized, dataset, asOf); skip on null.
    4. coverage = balance[chosenPath.fromProgramSlug] / chosenPath.requiredSourcePoints (the single sanctioned float; requiredSourcePoints 0 → treat as coverage 1).
    5. Valuations: cppX100 atMin over pointsMin, atMax over pointsMax (null when pointsMax null). effectiveCppX100: atMax uses chosenPath.requiredSourcePoints (the conservative figure); atMin re-runs requiredSourcePoints for pointsMin on the chosen route+bonus (direct paths: pointsMin itself). wowDeltaCents likewise at both ends via the source program's ProgramSeed (look up chosenPath.fromProgramSlug in dataset.programs). pointsNeeded = { min: pointsMin, max: pointsMax }.
    6. Partition: coverage ≥ 1 → bookableNow (pointsAway null); threshold ≤ coverage < 1 → almostThere with pointsAway = requiredSourcePoints − balance; else drop. threshold = options?.almostThereThreshold ?? 0.75 (A4 default, parameterized so Phase 4 tunes without engine edits — cite Pitfall 8).
    7. Sort as specified in behavior; all comparators deterministic (numeric then slug).
    Commit `feat(03-04): rankRedemptions orchestrator`.
  </action>
  <verify>
    <automated>npx vitest run tests/engine-ranking.test.ts && npx vitest run tests/engine-purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/ranking.ts contains `export function rankRedemptions(`
    - Tests assert partition membership, both sort orders, the 0.75 default boundary (coverage exactly at threshold is included in almostThere), and the options override
    - pointsAway asserted as exactly requiredSourcePoints − balance for a hand-picked fixture
    - `npx vitest run tests/engine-ranking.test.ts` exits 0; purity gate exits 0
  </acceptance_criteria>
  <done>Phase SC-1: balances in → { bookableNow ranked by wow delta, almostThere with points-away } out, per ratified A2 gating.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VAL-05 end-to-end + hardening (bonus auto-adjust, drafts, hostile balances)</name>
  <files>src/engine/ranking.ts, tests/engine-ranking.test.ts</files>
  <read_first>
    - src/engine/ranking.ts (Task 1 state)
    - src/data/transfers.ts (live Amex→Hilton bonus row dates) and src/data/redemptions-hotels.ts (a verified Hilton redemption with pointsMax ~200_000 — e.g. the Conrad Maldives row; confirm slug and fields)
    - .planning/phases/03-valuation-ranking-engine/03-RESEARCH.md (Pitfall 5, Pitfall 6, "Looks Done But Isn't" edge balances)
  </read_first>
  <behavior>
    - VAL-05 end-to-end: balances { "amex-mr": 120_000 }, same Hilton redemption, asOf "2026-09-15" vs "2026-10-15": at 09-15 the result's chosenPath.activeBonus is non-null (bonusPercent 30), requiredSourcePoints is LOWER, and effectiveCppX100 (conservative end) is HIGHER than at 10-15 where activeBonus is null — asserted by direct comparison of the two calls
    - Draft exclusion: with 5_000_000 in every one of the 8 programs, no result in either partition has redemption.slug "st-regis-maldives" or "gritti-palace-venice"
    - Hostile balances: {} → both partitions empty; { "chase-ur": -5 }, { "chase-ur": NaN }, { "chase-ur": Infinity }, { "chase-ur": 0 } → all treated as absent (both partitions empty); { "chase-ur": 5_000_000 } → results exist and every numeric field on every result satisfies Number.isFinite (walk cppX100/effectiveCppX100/wowDeltaCents at both non-null ends, coverage, requiredSourcePoints)
    - availabilityRating passthrough: every returned result's redemption.availabilityRating is one of the three known enum values (loop with descriptive expect messages per seed-data.test.ts style)
    - Determinism: two identical rankRedemptions calls produce deeply equal output (no clock/randomness — purity in behavior, not just imports)
  </behavior>
  <action>
    RED: extend tests/engine-ranking.test.ts with the cases above (commit `test(03-04): failing VAL-05 end-to-end + hardening tests`). GREEN: adjust ranking.ts only as needed — Task 1's sanitize step should already handle most; fix any NaN leaks or draft leaks the tests expose (commit `feat(03-04): harden ranking against hostile balances`; if nothing needs fixing, note tests-only in the commit).
  </action>
  <verify>
    <automated>npx vitest run tests/engine-ranking.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - A single test directly compares asOf "2026-09-15" vs "2026-10-15" outputs for the same input and asserts: lower requiredSourcePoints, higher effectiveCppX100, activeBonus non-null↔null (VAL-05 verified end-to-end)
    - Draft-exclusion test enumerates both known draft slugs against max-balance input
    - All five hostile-balance cases pass with no NaN/Infinity anywhere in output
    - `npm test` exits 0
  </acceptance_criteria>
  <done>VAL-05 is CI-verified end-to-end; the engine fails closed on drafts and junk input.</done>
</task>

<task type="auto">
  <name>Task 3: Public barrel + phase gates</name>
  <files>src/engine/index.ts</files>
  <read_first>
    - src/data/index.ts (barrel analog — header comment style, export * lines, collision-hazard note)
    - src/engine/types.ts, src/engine/paths.ts, src/engine/valuation.ts, src/engine/ranking.ts (surfaces being exported)
  </read_first>
  <action>
    Create `src/engine/index.ts`: header comment naming the consumers (Phase 4 results UI; the v2 advisor calls rankRedemptions as a tool) and warning that src/engine/transfers.ts and src/data/transfers.ts share a filename — consumers importing both must alias. Then exactly five lines: `export * from "./types";`, `export * from "./transfers";`, `export * from "./paths";`, `export * from "./valuation";`, `export * from "./ranking";`.

    Then run the full phase gate suite: `npm test`, `npm run typecheck`, `npm run lint`, `npm run build` — all must exit 0. If export-name collisions between engine files surface at typecheck, resolve by renaming the newer symbol (never touching frozen transfers.ts exports).
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/index.ts contains `export * from "./ranking";` and exports all five engine modules
    - `npm test`, `npm run typecheck`, `npm run lint`, and `npm run build` each exit 0
    - Purity test passes with index.ts included (its specifiers are all intra-engine "./" — auto-covered)
  </acceptance_criteria>
  <done>The sealed engine ships behind one import surface with every phase gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller balances → rankRedemptions | Phase 4 will pass URL-derived user input here; zod clamping is Phase 4's boundary job, but the engine must degrade gracefully on junk TODAY (defense in depth) |
| dataset → rankRedemptions | Repo-controlled + CI-validated; engine trusts the seed boundary and does not re-validate (a runtime zod call would break purity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | DoS/Integrity | NaN/Infinity/negative/huge balances rendering as NaN valuations publicly | mitigate | Sanitize step (Number.isSafeInteger && > 0) + five hostile-balance tests asserting finite outputs (Task 2) |
| T-03-10 | Information integrity | Unverified drafts leaking into public results (violates DATA-04 "no unverified entry ships") | mitigate | Engine-side verifiedAt filter (A5 fail-closed) + max-balance draft-exclusion test naming both known draft slugs |
| T-03-11 | Repudiation/Integrity | Non-deterministic results (clock reads) breaking shareable-URL reproducibility | mitigate | asOf is an input; purity test forbids Date.now/new Date; determinism test asserts identical calls → deep-equal output |
| T-03-SC | Tampering | Package installs | accept | None this phase |
</threat_model>

<verification>
- `npm test` green (all 7+ test files including Phase 2 regression suites)
- `npm run typecheck`, `npm run lint`, `npm run build` exit 0
- Purity gate green over the complete 6-file engine
</verification>

<success_criteria>
- Phase SC-1: bookable-now ranked by wow delta + almost-there with points-away, from real balances against the real dataset
- VAL-05: active bonus auto-adjusts effective points cost and cpp, surfaced on the result — verified by a direct before/after-endDate comparison
- Phase SC-5 holds over the finished module (purity gate green on all 6 engine files)
- Engine importable as a single barrel by Phase 4 and the v2 advisor
</success_criteria>

<output>
Create `.planning/phases/03-valuation-ranking-engine/03-04-SUMMARY.md` when done.
</output>
