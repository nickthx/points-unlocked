---
phase: 05-credibility-layer
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/methodology/page.tsx
  - tests/methodology-page.test.ts
  - src/components/site-footer.tsx
  - src/components/core-experience.tsx
autonomous: true
requirements: [VAL-03]

must_haves:
  truths:
    - "A visitor can open /methodology and read how cash fares are sourced, how taxes and fees are treated, and why award prices are ranges (the dynamic-award-pricing disclaimer)"
    - "Every number on the methodology page is computed from the same @/data and @/engine the results use — re-ratifying a baseline in programs.ts changes the page with zero edits"
    - "The results view and the site footer both link to /methodology"
    - "/methodology is a static, zero-client-JS, DB-free route"
  artifacts:
    - path: "src/app/methodology/page.tsx"
      provides: "VAL-03 static RSC with nine sections, live baseline table, worked ANA example"
      min_lines: 150
      contains: "cppX100"
    - path: "tests/methodology-page.test.ts"
      provides: "renderToStaticMarkup assertions on sections, live values, A2 wording"
      min_lines: 40
    - path: "src/components/site-footer.tsx"
      provides: "SiteFooter server component with the methodology link"
      exports: ["SiteFooter"]
      contains: "href=\"/methodology\""
    - path: "src/components/core-experience.tsx"
      provides: "'How we calculate these numbers →' link under the Bookable now heading"
      contains: "href=\"/methodology\""
  key_links:
    - from: "src/app/methodology/page.tsx"
      to: "@/data programs"
      via: "programs.filter(isUserEnterable) rendered as the cash-out baseline table"
      pattern: "isUserEnterable"
    - from: "src/app/methodology/page.tsx"
      to: "@/engine cppX100"
      via: "worked ANA example formatted through formatCpp"
      pattern: "formatCpp\\(cppX100\\("
    - from: "src/components/core-experience.tsx"
      to: "/methodology"
      via: "next/link"
      pattern: "href=\"/methodology\""
---

<objective>
Ship the VAL-03 methodology page as a static server component whose numbers are rendered from `@/data` + `@/engine` (drift guard, not a second source of truth), and link it from the results view and a new site footer.

Purpose: "The numbers are defensible" — finance credibility is part of the pitch. Prose that states exactly what the engine does, with live figures, is what makes the valuations attackable-proof.
Output: `src/app/methodology/page.tsx`, `tests/methodology-page.test.ts`, `src/components/site-footer.tsx`, one link in `src/components/core-experience.tsx`. (Mounting `<SiteFooter/>` in the layout happens in plan 05-05 to avoid a `layout.tsx` file conflict with 05-03.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-credibility-layer/05-RESEARCH.md
@.planning/phases/05-credibility-layer/05-PATTERNS.md
@.planning/phases/04-core-experience/04-UI-SPEC.md

<interfaces>
From src/data (barrel): `programs: ProgramSeed[]` — fields `slug`, `name`, `isUserEnterable: boolean`, `cashOutBaselineCppX100: number | null`. Enterable rows today: Chase UR 100, Amex MR 60, Capital One 50, Citi TY 100, Bilt 10, Hyatt null, Hilton null, Marriott null. `redemptions: RedemptionSeed[]` — `slug`, `title`, `pointsMin`, `pointsMax: number | null`, `cashFareCents`, `taxesFeesCents`, `availabilityRating: "wide_open" | "plan_ahead" | "hard_to_find"`, `verifiedAt: string | null`. Anchor row: `slug: "ana-business-tokyo-roundtrip"`, title "ANA business class to Tokyo — round trip", cashFareCents 900000, taxesFeesCents 60000, pointsMin 75000, pointsMax 90000 → `cppX100 === 933` (pinned by tests/engine-valuation.test.ts).

From src/engine (barrel): `cppX100(cashFareCents: number, taxesFeesCents: number, partnerPoints: number): number` = `Math.round(((cashFareCents - taxesFeesCents) * 100) / partnerPoints)`.

From src/lib/format.ts: `formatDollars(cents)`, `formatCpp(cppX100)` (933 → "9.3¢"), `formatPoints(points)`.

UI-SPEC class vocabulary (copy verbatim, from 05-PATTERNS "UI-SPEC class vocabulary"):
- Page container: `mx-auto flex w-full max-w-3xl flex-col gap-8 px-4 py-12 sm:px-6 md:py-16`
- Page h1: `font-display text-ink text-display font-semibold` (NO `md:text-display-xl` — that is reserved for `/`)
- Section heading (28px): `font-heading text-ink text-[1.75rem] leading-tight font-semibold`
- Body: `text-ink text-base leading-6`; muted: `text-ink/70 text-base leading-6`
- Label (14px): `text-ink/70 text-sm font-semibold`; stamp/link: `text-ink/70 text-sm leading-5`
- Section spacing: `gap-6` within, `mt-12` between
- Terracotta: NOT permitted anywhere in this plan (reserved for wow delta, "Copy my link", bonus badge)

Verified in plan-time check: `renderToStaticMarkup(createElement(Page))` renders `next/link` as a plain `<a href>` in the vitest node environment — no router mock needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /methodology static page rendering engine-sourced numbers + SSR test</name>
  <files>src/app/methodology/page.tsx, tests/methodology-page.test.ts</files>
  <read_first>
    - src/app/page.tsx (page shell, module-header comment habit, `<main className="bg-cream flex flex-1 flex-col">`)
    - src/app/layout.tsx lines 21-27 (`metadata` export shape)
    - src/components/core-experience.tsx lines 74-82 and 245-285 (SECTION_HEADING_CLASS, EmptyState `<dl>` figure layout, body classes)
    - src/components/result-card.tsx lines 1-18 and 35-60 (import ordering; null-baseline branch)
    - src/engine/valuation.ts (JSDoc for cppX100, cashOutValueCents, wowDeltaCents — the prose must describe these exactly)
    - src/engine/paths.ts JSDoc header (increments, Marriott 5K/60K, A1 tie rule, A7 single hop, "60,000 Alaska via Marriott costs 150,000 Bonvoy" example)
    - src/engine/ranking.ts lines 85-110 (A2 conservative gate `pointsMax ?? pointsMin`, A5 fail-closed on `verifiedAt === null`)
    - src/data/types.ts lines 40-60 (A4 no-stacking rule, cash-fare benchmark convention — inherited verbatim)
    - .planning/phases/05-credibility-layer/05-RESEARCH.md §"Methodology Content Architecture (VAL-03)" (the nine sections)
    - tests/balance-params.test.ts lines 142-162 (source-scan describe shape) and tests/seed-data.test.ts lines 15-30 (iterating real enterable programs)
  </read_first>
  <behavior>
    - `html = renderToStaticMarkup(createElement(MethodologyPage))` contains `<h1` with text "How we value your points"
    - `html` contains each of the nine `<h2>` headings verbatim: "What you're looking at", "Where cash fares come from", "Taxes and fees", "Cents per point", "What cashing out means", "Transfer math", "Dynamic award pricing", "Verification and freshness", "Independence"
    - For every `p` in `programs.filter(p => p.isUserEnterable)`: `html` contains `p.name` and contains `p.cashOutBaselineCppX100 === null ? "Pure travel value" : formatCpp(p.cashOutBaselineCppX100)` (note: JSX escapes `'` as `&#x27;` — assert on strings without apostrophes, or compare after `.replace(/&#x27;/g, "'")`)
    - `html` contains `formatCpp(cppX100(anchor.cashFareCents, anchor.taxesFeesCents, anchor.pointsMax ?? anchor.pointsMin))` (i.e. "9.3¢") AND `formatDollars(anchor.cashFareCents)` ("$9,000") AND `formatPoints(anchor.pointsMax ?? anchor.pointsMin)` ("90,000") where `anchor = redemptions.find(r => r.slug === "ana-business-tokyo-roundtrip")`
    - `html` contains "high end" and "conservative" (A2 wording), "not financial advice", "no affiliate links", "150,000" (Marriott worked example), "Bilt", and `href="/"`
    - `html` does NOT contain "1¢ each" and does NOT contain "terracotta"
    - Source scan of `src/app/methodology/page.tsx`: does not contain `"use client"`, `searchParams`, `new Date`, `from "@/db`, or `export const dynamic`
  </behavior>
  <action>
    Create `src/app/methodology/page.tsx` as a SYNC server component (`export default function MethodologyPage()` — no `async`, no `searchParams`, so the route prerenders as `○ /methodology` and renders in vitest). Imports in house order: `import type { Metadata } from "next"; import Link from "next/link";` blank; `import { programs, redemptions } from "@/data"; import { cppX100 } from "@/engine"; import { formatCpp, formatDollars, formatPoints } from "@/lib/format";` — barrels only. Module-header comment: VAL-03, "the page is the drift guard — every number is rendered from data, never typed", accent discipline (no terracotta), T-05-04.

    `export const metadata: Metadata = { title: "Methodology — Points Unlocked", description: "How we source cash fares, treat taxes and fees, value points, and why award prices are ranges." }` (the layout's `metadataBase`/openGraph defaults are inherited).

    Module scope: `const anchor = redemptions.find((r) => r.slug === "ana-business-tokyo-roundtrip");` and `const enterablePrograms = programs.filter((p) => p.isUserEnterable);`. Inside the component compute `anchorCpp` only when `anchor` is defined (conditional, never a non-null assertion).

    Markup: `<main className="bg-cream flex flex-1 flex-col">` → `<article className="mx-auto flex w-full max-w-3xl flex-col gap-8 px-4 py-12 sm:px-6 md:py-16">` → `<h1 className="font-display text-ink text-display font-semibold">How we value your points</h1>` + one muted lede paragraph, then nine `<section className="flex flex-col gap-6">` blocks (add `mt-12` is NOT needed inside a `gap-8` flex column; keep `gap-8` on the article), each with `<h2 className="font-heading text-ink text-[1.75rem] leading-tight font-semibold">` using the exact nine headings in `<behavior>` order, and body `<p className="text-ink text-base leading-6">` prose. Content per section (state what the engine ACTUALLY does — verify against the read_first JSDoc):
    1. What you're looking at — dual valuation (cash fare + cents per point); the hero dollar figure is transfer-partner value minus what the same points fetch cashed out (`wowDeltaCents`); always the conservative end of any range.
    2. Where cash fares come from — representative hand-verified benchmarks, not live prices; discounted realistic retail for economy/business, undiscounted retail for First; each entry carries a source note and methodology note; refreshed manually and stamped with a verified date.
    3. Taxes and fees — subtracted from the cash fare before dividing by points because you pay them in cash either way; the wow figure subtracts them on the value side for the same reason. Show the formula as text: "cents per point = (cash fare − taxes and fees) ÷ points".
    4. Cents per point — TPG convention; render the worked ANA example as a `<p>` in this exact shape: `{anchor.title}: ({formatDollars(anchor.cashFareCents)} − {formatDollars(anchor.taxesFeesCents)}) ÷ {formatPoints(anchor.pointsMax ?? anchor.pointsMin)} points = {formatCpp(anchorCpp)} per point.` Mention the per-partner-point figure and that an active transfer bonus improves the effective per-source-point figure.
    5. What cashing out means — a `<table>` of `enterablePrograms` with `<th>` cells "Program" / "Cash-out value per point" (`text-ink/70 text-sm font-semibold` label cells, `text-ink text-base leading-6` body cells), one `<tr key={p.slug}>` per program; value cell `p.cashOutBaselineCppX100 === null ? "Pure travel value" : formatCpp(p.cashOutBaselineCppX100)`. Prose: each program's OWN baseline, never a flat 1¢; Bilt's figure is a stand-in for "effectively no cash-out path"; hotel currencies are shown as pure travel value.
    6. Transfer math — ratios and increments (transfers move in blocks, rounded down); Marriott's 5,000-mile bonus per 60,000 points; promotional bonuses multiply the base conversion and never stack with structural bonuses; single-hop transfers only; "cheapest path" = fewest source points, direct use winning ties. Include the sentence: "60,000 Alaska miles via Marriott cost 150,000 Bonvoy points, not the naive 180,000."
    7. Dynamic award pricing — many programs price awards dynamically; entries carry a range (low–high); we rank and label "Bookable now" on the high end of the range, the conservative reading, so the site never overpromises; award space is not guaranteed — every entry carries an availability rating (wide open, plan ahead, hard to find); prices and partners change without notice.
    8. Verification and freshness — every entry shown has a "Verified" date; unverified drafts never appear (fail closed); when a transfer-bonus window ends the figures revert automatically.
    9. Independence — no affiliate links, no card recommendations in this version, educational only and not financial advice. (Discretion: no `mailto:` — no ratified contact address exists yet; Phase 6 legal adds a contact channel. Do not invent one.)
    Close with `<Link href="/" className="text-ink/70 text-sm leading-5 underline-offset-4 hover:underline">Back to your results</Link>`.
    Voice per UI-SPEC: confident, concrete, second person, no exclamation marks. Keep the file under 400 lines; if prose pushes past that, move sections 6–9 into a sibling `src/app/methodology/sections.tsx` exporting plain components (still sync, still no client directive) — and add it to files_modified in the SUMMARY.

    Create `tests/methodology-page.test.ts` per `<behavior>`: `import { createElement } from "react"; import { renderToStaticMarkup } from "react-dom/server";` relative imports `../src/app/methodology/page` (default export), `../src/data`, `../src/engine/valuation` (cppX100), `../src/lib/format`, plus `readFileSync` source scan of the page file. `describe("/methodology (VAL-03)")` with one `it` per bullet; render once in a module-scope const.
  </action>
  <verify>
    <automated>npx vitest run tests/methodology-page.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/methodology-page.test.ts` exits 0 with ≥ 8 passing `it` cases
    - `grep -c '<h2' src/app/methodology/page.tsx` (plus `sections.tsx` if created) totals exactly `9`
    - `grep -c 'export default function MethodologyPage' src/app/methodology/page.tsx` outputs `1`
    - `grep -cE '"use client"|searchParams|export const dynamic|new Date|from "@/db|drizzle|terracotta' src/app/methodology/page.tsx` outputs `0`
    - `grep -c 'formatCpp(cppX100(' src/app/methodology/page.tsx` ≥ 1
    - `grep -c 'isUserEnterable' src/app/methodology/page.tsx` outputs `1`
    - `grep -c 'ana-business-tokyo-roundtrip' src/app/methodology/page.tsx` outputs `1`
    - `grep -oE ">[^<]*![^<]*<" src/app/methodology/page.tsx | wc -l` outputs `0` (no exclamation marks inside JSX text nodes)
    - `wc -l src/app/methodology/page.tsx` < 500
    - `npm run typecheck` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>`/methodology` renders nine accurate sections with live baseline and worked-example figures, tested via SSR string assertions.</done>
</task>

<task type="auto">
  <name>Task 2: SiteFooter component + methodology link under "Bookable now"</name>
  <files>src/components/site-footer.tsx, src/components/core-experience.tsx</files>
  <read_first>
    - src/components/almost-there.tsx lines 1-30 (server-compatible section, accent-discipline comment)
    - src/components/core-experience.tsx lines 1-30 (import block) and 200-225 (the "Bookable now" section where the link goes)
    - src/components/result-card.tsx line 120-130 (the `text-ink/70 text-sm leading-5` stamp class)
    - .planning/phases/04-core-experience/04-04-PLAN.md line 185-192 (the Phase 4 acceptance greps that must keep passing: "Bookable now" count, no `@/db`)
  </read_first>
  <action>
    1. Create `src/components/site-footer.tsx` — server component (no client directive, no hooks). `import Link from "next/link";`. Header comment: VAL-03 link placement + accent discipline (ink only). `export function SiteFooter()` returns `<footer className="border-ink/10 mt-auto border-t">` wrapping `<div className="mx-auto flex w-full max-w-3xl flex-col gap-2 px-4 py-8 sm:flex-row sm:items-center sm:justify-between sm:px-6">` with (a) `<p className="text-ink text-sm font-semibold">Points Unlocked</p>`, (b) `<nav aria-label="Footer">` containing `<Link href="/methodology" className="text-ink/70 text-sm leading-5 underline-offset-4 hover:underline">Methodology</Link>` and, separated by a `text-ink/70 text-sm leading-5` span reading "Educational only — not financial advice". No terracotta. This file is NOT yet mounted (plan 05-05 mounts it in `layout.tsx`).
    2. Modify `src/components/core-experience.tsx`: add `import Link from "next/link";` to the external import block (before the `nuqs` import). In the "Bookable now" section replace the bare `<h2 className={SECTION_HEADING_CLASS}>Bookable now</h2>` with a `<div className="flex flex-col gap-2">` containing that identical `<h2>` (text "Bookable now" stays verbatim and appears exactly once in the file) followed by `<Link href="/methodology" className="text-ink/70 text-sm leading-5 underline-offset-4 hover:underline">How we calculate these numbers →</Link>`. Do not touch any other branch, hook, or the existing header comment; do not write the literal `from "@/db` or the word drizzle anywhere (grep gate, T-04-11).
  </action>
  <verify>
    <automated>test "$(grep -v '^\s*//' src/components/core-experience.tsx | grep -c 'href="/methodology"')" = "1" && test "$(grep -v '^\s*//' src/components/site-footer.tsx | grep -c 'href="/methodology"')" = "1" && test "$(grep -c 'Bookable now' src/components/core-experience.tsx)" = "1" && ! grep -rlE 'from "@/db|drizzle' src/components && npm run typecheck && npm run lint && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/components/core-experience.tsx | grep -c 'href="/methodology"'` outputs `1`
    - `grep -c 'How we calculate these numbers' src/components/core-experience.tsx` outputs `1`
    - `grep -c 'Bookable now' src/components/core-experience.tsx` outputs `1` (heading text unchanged, still once)
    - `head -1 src/components/core-experience.tsx` is `"use client";`
    - `grep -c 'export function SiteFooter' src/components/site-footer.tsx` outputs `1`
    - `grep -v '^\s*//' src/components/site-footer.tsx | grep -c 'href="/methodology"'` outputs `1`
    - `grep -cE '"use client"|terracotta' src/components/site-footer.tsx` outputs `0`
    - `grep -rlE 'from "@/db|drizzle' src/components` produces no output (exit 1)
    - `npm run typecheck`, `npm run lint`, `npx vitest run` exit 0
  </acceptance_criteria>
  <done>Footer component exists with the methodology link; the results view links to /methodology under the Bookable now heading; guest flow remains DB-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seed data → rendered prose | Curated TypeScript seed fields rendered into a static page (trusted authoring boundary, JSX-escaped) |
| Public internet → /methodology | Static prerendered route; no request-time input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Repudiation | src/app/methodology/page.tsx | mitigate | Numbers are rendered from `programs`/`redemptions` through `cppX100` + formatters, never typed; `tests/methodology-page.test.ts` asserts each enterable baseline and the 933 anchor appear, and that A2 "high end"/"conservative" wording is present — prose cannot silently drift from the engine |
| T-05-05 | Information disclosure | /methodology route | mitigate | Sync server component, no `searchParams`, no `@/db`, no client directive → static `○` route with zero client JS; source-scan test + grep gate enforce it |
| T-05-06 | Information disclosure | src/components/core-experience.tsx | mitigate | Only a `next/link` is added to the client island; DB-free grep gate over `src/components` re-run in Task 2 (T-04-11 carried forward) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run` green (includes tests/methodology-page.test.ts)
- `npm run typecheck && npm run lint` exit 0
- `grep -rlE 'from "@/db|drizzle' src/components` → no matches
- Nine `<h2>` headings; live baseline table; worked ANA example equals `formatCpp(cppX100(...))`
</verification>

<success_criteria>
- VAL-03: methodology page explains fare sourcing (section 2), taxes/fees treatment (section 3), and the dynamic-award-pricing disclaimer (section 7), with live numbers
- Links to `/methodology` exist in both the results view and the footer component
- No terracotta, no DB import, no client JS on the page
</success_criteria>

<output>
Create `.planning/phases/05-credibility-layer/05-02-SUMMARY.md` when done
</output>
