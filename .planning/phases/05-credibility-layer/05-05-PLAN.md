---
phase: 05-credibility-layer
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: false
requirements: [VAL-03, PLAT-03, PLAT-04]

must_haves:
  truths:
    - "Every page shows the site footer with the Methodology link; / shows the advisor tease below the results"
    - "The production site at https://points-unlocked.vercel.app serves the Phase 5 build: OG tags in the head, a PNG at /og, /methodology live"
    - "A share link pasted into LinkedIn Post Inspector unfurls with the branded image and the wow number (ROADMAP success criterion 2)"
    - "A real email submitted on production lands as a row in interest_signups"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "<SiteFooter /> mounted after {children}"
      contains: "SiteFooter"
    - path: "src/app/page.tsx"
      provides: "<AdvisorTease /> mounted after <CoreExperience />"
      contains: "AdvisorTease"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/site-footer.tsx"
      via: "import + render inside <body>"
      pattern: "<SiteFooter />"
    - from: "src/app/page.tsx"
      to: "src/components/advisor-tease.tsx"
      via: "import + render inside <main>"
      pattern: "<AdvisorTease />"
    - from: "production HTML head"
      to: "https://points-unlocked.vercel.app/og?ur=90000&mr=50000"
      via: "og:image absolute URL"
      pattern: "og:image"
---

<objective>
Wire the last two mounts (footer in the layout, tease on `/`), run every phase gate locally, deploy to production, probe the live OG/tag/cache behavior with curl, then pause for the one thing only a human can do: confirm the unfurl in LinkedIn Post Inspector and the Vercel Open Graph tab, and submit a waitlist email that the executor then counts in Neon.

Purpose: ROADMAP success criteria 1–3 are all "TRUE on the product" statements; criterion 2 explicitly requires a link-preview inspector against the production URL.
Output: two mounts, green gates, production deploy, captured unfurl + cache evidence, one confirmed `interest_signups` row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-credibility-layer/05-RESEARCH.md
@.planning/phases/04-core-experience/04-UI-SPEC.md
@.planning/phases/05-credibility-layer/05-02-SUMMARY.md
@.planning/phases/05-credibility-layer/05-03-SUMMARY.md
@.planning/phases/05-credibility-layer/05-04-SUMMARY.md

<interfaces>
From src/components/site-footer.tsx (05-02): `export function SiteFooter(): JSX.Element` — server component, no props.
From src/components/advisor-tease.tsx (05-04): `export function AdvisorTease(): JSX.Element` — `"use client"`, no props (do NOT pass `asOf` or share text).
From scripts/db-check.ts (05-04): prints `programs rows: N` and `interest_signups rows: N`.

Deploy mechanism (Phase 1 decision): Git auto-deploy is unconditional — pushing `main` to `origin` deploys production at https://points-unlocked.vercel.app. Local `main` is currently ~72 commits ahead of `origin/main`; the push publishes Phases 2–5 together. Preview deployments are Deployment-Protected (crawlers get 401) — verify on production only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount SiteFooter + AdvisorTease and run every phase gate locally</name>
  <files>src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - src/app/layout.tsx (post-05-03 state: metadataBase + NuqsAdapter wrap)
    - src/app/page.tsx (post-05-03 state: generateMetadata + Home)
    - src/components/site-footer.tsx and src/components/advisor-tease.tsx (export names)
    - .planning/phases/04-core-experience/04-04-PLAN.md lines 185-192 (Phase 4 gates that must still pass)
  </read_first>
  <action>
    1. `src/app/layout.tsx`: add `import { SiteFooter } from "@/components/site-footer";` (in the `@/components` import position) and render `<SiteFooter />` immediately after `{children}` inside `<NuqsAdapter>` (the body is already `flex min-h-full flex-col`; pages use `flex-1` on `<main>`, so the footer's `mt-auto` lands it at the bottom). Nothing else changes.
    2. `src/app/page.tsx`: add `import { AdvisorTease } from "@/components/advisor-tease";` after the `CoreExperience` import and render `<AdvisorTease />` inside `<main>` directly after `<CoreExperience asOf={asOf} />`, wrapped in `<div className="mx-auto w-full max-w-3xl px-4 pb-16 sm:px-6">` so it aligns with the island's container. No props.
    3. Run the full gate set and record each result in the SUMMARY: `npx vitest run` (expect 152 + the four Phase 5 files green), `npm run typecheck`, `npm run lint`, `npm run build` (route table must list `ƒ /`, `ƒ /og`, `○ /methodology`), DB-free gate `grep -rlE 'from "@/db|drizzle' src/components src/app` → exactly `src/app/actions/interest.ts`, Phase 4 gates: `grep -c 'NuqsAdapter' src/app/layout.tsx` = 2, `grep -c 'index: false' src/app/layout.tsx` = 1, `grep -c 'force-dynamic' src/app/page.tsx` = 0, `head -1 src/components/core-experience.tsx` = `"use client";`.
    4. Secrets gate before any push (T-05-17): `git ls-files | grep -E '(^|/)\.env'` must print nothing and `git ls-files src/assets/fonts` must list exactly the two `.woff` files.
  </action>
  <verify>
    <automated>test "$(grep -c '<SiteFooter />' src/app/layout.tsx)" = "1" && test "$(grep -c '<AdvisorTease />' src/app/page.tsx)" = "1" && test "$(grep -rlE 'from "@/db|drizzle' src/components src/app | tr -d '\r')" = "src/app/actions/interest.ts" && ! git ls-files | grep -qE '(^|/)\.env' && npx vitest run && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '<SiteFooter />' src/app/layout.tsx` outputs `1`; `grep -c 'from "@/components/site-footer"' src/app/layout.tsx` outputs `1`
    - `grep -c '<AdvisorTease />' src/app/page.tsx` outputs `1`; `grep -c 'from "@/components/advisor-tease"' src/app/page.tsx` outputs `1`
    - `grep -c 'NuqsAdapter' src/app/layout.tsx` outputs `2`; `grep -c 'index: false' src/app/layout.tsx` outputs `1`
    - `grep -rlE 'from "@/db|drizzle' src/components src/app` outputs exactly `src/app/actions/interest.ts`
    - `npm run build` exit 0 and its output contains `ƒ /og` and `○ /methodology`
    - `npx vitest run`, `npm run typecheck`, `npm run lint` exit 0
    - `git ls-files | grep -E '(^|/)\.env'` prints nothing
  </acceptance_criteria>
  <done>Footer and tease are mounted; every automated Phase 4 + Phase 5 gate is green; the tree is safe to publish.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to production and probe OG tags, PNG, and CDN cache with curl</name>
  <files></files>
  <read_first>
    - .planning/phases/05-credibility-layer/05-RESEARCH.md Pitfall 6 and Assumption A2 (cache-key check) and §"Validation Architecture" PLAT-03 rows
    - .planning/STATE.md "Decisions" (Git auto-deploy unconditional; DATABASE_URL via Marketplace injection)
  </read_first>
  <action>
    1. Commit any remaining work from Task 1, then deploy: `git push origin main`. This publishes all unpushed commits (Phases 2–5) to production via Vercel Git auto-deploy. If the push is rejected for auth/branch-protection reasons, raise an auth-gate checkpoint (do not force-push, do not change remotes).
    2. Wait for the deploy: poll `curl -s "https://points-unlocked.vercel.app/?ur=90000&mr=50000" | grep -c 'property="og:image"'` every 30 seconds until it prints ≥ 1 (give up after 10 minutes and report). Optionally cross-check with `vercel ls --prod` / `vercel inspect` if the CLI is logged in; never print env values.
    3. Capture production evidence into the SUMMARY (paste the exact lines):
       - Tags: `curl -s "https://points-unlocked.vercel.app/?ur=90000&mr=50000" | grep -oE '<meta (property="og:[a-z:]+"|name="twitter:[a-z]+")[^>]*>'` — must include `og:title`, `og:description`, `og:url` containing `ur=90000&amp;mr=50000` (or `ur=90000&mr=50000`), `og:image` equal to `https://points-unlocked.vercel.app/og?ur=90000&mr=50000` (absolute, canonical order), and `twitter:card` `summary_large_image`.
       - Image: `curl -sI "https://points-unlocked.vercel.app/og?ur=90000&mr=50000"` twice; first must show `content-type: image/png` and `cache-control` containing `s-maxage=86400`; second must show `x-vercel-cache: HIT`.
       - Cache-key isolation (A2 / T-05-11): `curl -s -o /dev/null -w '%{size_download}\n' "https://points-unlocked.vercel.app/og?ur=90000"` and the same for `?hyatt=75000` — the two byte sizes must differ.
       - Methodology: `curl -s -o /dev/null -w '%{http_code}\n' https://points-unlocked.vercel.app/methodology` → `200`; `curl -s https://points-unlocked.vercel.app/ | grep -c 'href="/methodology"'` ≥ 1.
       - Baseline card: `curl -sI https://points-unlocked.vercel.app/og` → `content-type: image/png`.
  </action>
  <verify>
    <automated>test "$(git rev-list --count origin/main..main)" = "0" && curl -s "https://points-unlocked.vercel.app/?ur=90000&mr=50000" | grep -q 'content="https://points-unlocked.vercel.app/og?ur=90000&amp;mr=50000"\|content="https://points-unlocked.vercel.app/og?ur=90000&mr=50000"' && curl -sI "https://points-unlocked.vercel.app/og?ur=90000&mr=50000" | grep -qi '^content-type: image/png' && curl -s -o /dev/null -w '%{http_code}' https://points-unlocked.vercel.app/methodology | grep -q '^200$'</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --count origin/main..main` outputs `0` (everything pushed)
    - Production `/?ur=90000&mr=50000` HTML contains an `og:image` meta whose content is `https://points-unlocked.vercel.app/og?ur=90000&mr=50000` (ampersand may be HTML-escaped), plus `og:title`, `og:description`, `og:url` with the params, and `twitter:card` = `summary_large_image`
    - `curl -sI …/og?ur=90000&mr=50000` → `content-type: image/png` and `cache-control` contains `s-maxage=86400`; a second request shows `x-vercel-cache: HIT`
    - `size_download` for `/og?ur=90000` differs from `/og?hyatt=75000`
    - `/methodology` returns HTTP 200 and `/` HTML contains `href="/methodology"` ≥ 1
    - `/og` (no params) → `content-type: image/png`
    - All curl output lines pasted into the SUMMARY as evidence
  </acceptance_criteria>
  <done>Production serves the Phase 5 build; tags, image, cache, and methodology route are verified by curl with evidence recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the unfurl in a link-preview inspector and submit one waitlist email</name>
  <what-built>Phase 5 is deployed to https://points-unlocked.vercel.app: per-share-link OG tags + a branded /og PNG (PLAT-03), the /methodology page linked from results and footer (VAL-03), and the advisor "coming soon" tease with an email hook writing to Neon (PLAT-04). Curl evidence for tags/PNG/cache is already in the SUMMARY draft; third-party crawler rendering and a real form submission cannot be exercised by the executor.</what-built>
  <files></files>
  <read_first>
    - .planning/phases/05-credibility-layer/05-05-SUMMARY.md draft (curl evidence from Task 2)
    - .planning/phases/05-credibility-layer/05-RESEARCH.md Pitfall 6 (LinkedIn caches ~7 days; Post Inspector forces a re-scrape; previews are Deployment-Protected)
    - scripts/db-check.ts (post-approval row-count command)
  </read_first>
  <action>Pause for the human steps in how-to-verify. Before pausing, print the two inspector URLs and the production share link verbatim. After the human resumes with "approved", run `npx tsx scripts/db-check.ts` and record the `interest_signups rows: N` line (N must be ≥ 1) plus any screenshot paths or inspector output the human supplied in the SUMMARY. If the human reports a mismatch, do not mark the phase complete — record the discrepancy and stop.</action>
  <how-to-verify>
    1. Open https://www.linkedin.com/post-inspector/ and inspect `https://points-unlocked.vercel.app/?ur=90000&mr=50000`. Expected: a preview card with the branded image showing a terracotta dollar figure (the wow delta), the title "90,000 Chase Ultimate Rewards points → …", and the description ending "See every redemption these balances unlock." If LinkedIn shows a stale/empty preview, click "Inspect" again to force a re-scrape (it caches ~7 days).
    2. Inspect the bare URL `https://points-unlocked.vercel.app/` in the same tool. Expected: the baseline card (ink headline "What are your points actually worth?", no dollar figure).
    3. In the Vercel dashboard open the current production deployment → "Open Graph" tab for the path `/?ur=90000&mr=50000`. Expected: same image and tags as step 1. (Optional: paste the link into a Slack DM or iMessage and confirm a card renders.)
    4. On https://points-unlocked.vercel.app/?ur=90000 scroll below the results: confirm the "Coming soon / The AI card-roadmap advisor" section, then submit a real email you control in the form. Expected: the form is replaced by "You're on the list." with no page reload. Submit the same email again from a fresh visit — expected: the same "You're on the list." (idempotent, no "already registered" message). Submit `not-an-email` — expected: "Enter a valid email address." under the form.
    5. Click "Methodology" in the footer and "How we calculate these numbers →" under "Bookable now". Expected: /methodology with nine sections, the cash-out table showing each program's own baseline (Amex 0.6¢, Capital One 0.5¢, Hyatt/Hilton/Marriott "Pure travel value"), and the ANA worked example ending "= 9.3¢ per point."
    6. Note the screenshot path(s) or paste the inspector's tag list so the executor can record them in the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>npx tsx scripts/db-check.ts | grep -E "^interest_signups rows: [1-9][0-9]*$"</automated>
    <human-check>LinkedIn Post Inspector and Vercel Open Graph tab both render the branded image for the share link; steps 1–5 confirmed</human-check>
  </verify>
  <acceptance_criteria>
    - Human resume signal is "approved" (steps 1–5 all matched expectations)
    - `npx tsx scripts/db-check.ts` prints a line matching `^interest_signups rows: [1-9][0-9]*$` after the human submission
    - Inspector evidence (screenshot path or pasted tag list) and the row-count line are recorded in 05-05-SUMMARY.md
  </acceptance_criteria>
  <done>Unfurl confirmed in a link-preview inspector against production; one waitlist row confirmed in Neon; evidence recorded.</done>
  <resume-signal>Type "approved" (optionally followed by screenshot paths or the inspector output), or describe what did not match.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub → Vercel production | Publishing ~72 commits to the public production host |
| Social-platform crawlers → production | LinkedIn/Slack/iMessage fetch HTML + PNG anonymously |
| Human → production form → Neon | The first real PII row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-17 | Information disclosure | git push of the accumulated history | mitigate | `git ls-files | grep -E '\.env'` must be empty before push; `.env*` gitignored since Phase 1 (T-01-08); no secrets in fonts/assets (binary `.woff` only) |
| T-05-11 | Tampering | CDN cache cross-contamination between share links | mitigate | Task 2 proves different param sets return different PNG sizes and repeat requests HIT the cache per URL (A2 verified in production, not assumed) |
| T-05-18 | Spoofing | Deployment-Protected preview host in OG URLs | mitigate | `metadataBase` is the fixed production host (05-03); Task 2 asserts the absolute `og:image` host is `points-unlocked.vercel.app` |
| T-05-16 | Repudiation | First real PII row | accept | Human submits their own email with visible consent copy; Phase 6 adds the privacy policy and deletion path; row count (not contents) is what the executor reads back |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase |
</threat_model>

<verification>
- After the checkpoint is approved: `npx tsx scripts/db-check.ts` prints `interest_signups rows: N` with N ≥ 1 — record the line (not the email) in the SUMMARY
- Curl evidence from Task 2 and the inspector evidence from Task 3 are both in the SUMMARY
- `npx vitest run && npm run typecheck && npm run lint && npm run build` green on the pushed commit
</verification>

<success_criteria>
- ROADMAP Phase 5 criterion 1: /methodology explains fare sourcing, taxes/fees, and dynamic award pricing, linked from results (footer + Bookable now link) — human-confirmed
- Criterion 2: share links unfurl with proper OG tags and the branded OG image — verified in LinkedIn Post Inspector + Vercel OG tab against production
- Criterion 3: the v2 advisor "coming soon" tease is present with a working interest hook — one row confirmed in `interest_signups`
- VAL-03, PLAT-03, PLAT-04 all satisfied on the deployed product
</success_criteria>

<output>
Create `.planning/phases/05-credibility-layer/05-05-SUMMARY.md` when done
</output>
