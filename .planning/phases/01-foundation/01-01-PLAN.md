---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - tsconfig.json
  - eslint.config.mjs
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - vitest.config.ts
  - tests/smoke.test.ts
  - .prettierrc
  - src/db/schema.ts
  - src/data/.gitkeep
  - src/engine/.gitkeep
  - src/components/.gitkeep
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "A Next.js 16 App Router app exists at repo root and `npm run build` exits 0 (D-01 groundwork)"
    - "`npm run lint`, `npm run typecheck`, and `npm test` all exit 0 locally (D-05 prerequisites)"
    - "One Vitest smoke test passes with no jsdom/testing-library setup (D-09, D-10)"
    - "No E2E automation tooling exists anywhere in the scaffold — critical path is verified manually per phase (D-12)"
    - ".gitignore covers .env files before any env file ever exists in the repo"
    - "The full directory skeleton src/app, src/db, src/data, src/engine, src/components exists (D-14)"
  artifacts:
    - path: "package.json"
      provides: "next >=16.3.3, scripts: dev/build/lint/typecheck/test/format"
      contains: "\"typecheck\""
    - path: "vitest.config.ts"
      provides: "Vitest config, node environment, no coverage config (D-11)"
    - path: "tests/smoke.test.ts"
      provides: "Placeholder smoke test (D-10)"
    - path: "src/db/schema.ts"
      provides: "Schema stub slot for Phase 2 (D-14); replaced with health_check in plan 01-05"
    - path: ".gitignore"
      provides: "Secrets hygiene"
      contains: ".env"
  key_links:
    - from: "package.json scripts"
      to: ".github/workflows/ci.yml (plan 01-03)"
      via: "npm run lint / typecheck / test invoked by CI"
      pattern: "\"test\": \"vitest run\""
---

<objective>
Scaffold the Next.js 16 (App Router) + Tailwind v4 + TypeScript app in the existing repo, wire the full quality toolchain (ESLint, Prettier, typecheck, Vitest smoke test), and lay down the D-14 directory skeleton. This is the Wave 1 foundation every other plan builds on.

Purpose: PLAT-01 — a production-grade skeleton; the repo becomes a real buildable app without losing the planning docs.
Output: Buildable Next.js app at repo root, green lint/typecheck/test scripts, directory skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 app into the existing repo</name>
  <files>package.json, package-lock.json, .gitignore, tsconfig.json, eslint.config.mjs, next.config.ts, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css</files>
  <read_first>
    - CLAUDE.md (repo root — locked stack versions and What NOT to Use table; plans MUST comply)
    - .planning/research/STACK.md (canonical versions: next 16.3.4 / min ^16.3.3, tailwindcss 4.3.x, react 19.2.x)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01..D-16)
  </read_first>
  <action>
    The repo root is non-empty (.planning/, CLAUDE.md, PROJECT-BRIEF.md, .git), so create-next-app cannot run in place. Scaffold into a temp directory under the session scratchpad (e.g. {scratchpad}/scaffold), then move the scaffold's contents into C:\Users\geoca\points-unlocked.

    1. Run create-next-app non-interactively: `npx create-next-app@latest {scratchpad}/scaffold --typescript --app --tailwind --eslint --src-dir --import-alias "@/*" --use-npm --yes`. This yields Next 16.3.x + React 19.2.x + Tailwind v4 with CSS-first config (no tailwind.config.js) per STACK.md.
    2. Delete the scaffold's own .git directory if create-next-app initialized one, then move ALL scaffold contents including dotfiles (.gitignore, eslint config, etc.) into the repo root. Do not overwrite or delete .planning/, CLAUDE.md, PROJECT-BRIEF.md, or the existing .git.
    3. Verify the resulting .gitignore covers env files (`.env*` line, as create-next-app generates) — this MUST be in place before any later plan pulls env vars (security: secrets never committed). If the line is missing, add `.env*` plus an un-ignore for none (no .env.example needed this phase).
    4. Assert package.json "next" resolves to >=16.3.3 (August 2026 security floor). If lower, set `"next": "^16.3.4"` and run `npm install`.
    5. Confirm the v4/v16 shape: NO tailwind.config.js at root, NO middleware.ts (Next 16 uses proxy.ts — neither file is needed this phase), globals.css uses `@import "tailwindcss"`.
    6. Run `npm run build` to prove a clean build. Do NOT create any .env file in this plan.
  </action>
  <verify>
    <automated>npm run build exits 0; node -e "const p=require('./package.json'); if(!/16\.(3\.([3-9]|\d{2,})|[4-9]|\d{2,})/.test(p.dependencies.next)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json exists at repo root with dependencies.next satisfying >=16.3.3
    - .gitignore contains a line matching `.env`
    - src/app/page.tsx, src/app/layout.tsx, src/app/globals.css exist
    - No tailwind.config.js and no middleware.ts exist at repo root
    - .planning/ directory, CLAUDE.md, and PROJECT-BRIEF.md still present and unmodified
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Repo root is a buildable Next.js 16 App Router app with Tailwind v4, planning docs intact, env files ignored before any exist.</done>
</task>

<task type="auto">
  <name>Task 2: Quality toolchain (Vitest, typecheck, Prettier) + D-14 directory skeleton</name>
  <files>package.json, vitest.config.ts, tests/smoke.test.ts, .prettierrc, src/db/schema.ts, src/data/.gitkeep, src/engine/.gitkeep, src/components/.gitkeep</files>
  <read_first>
    - package.json (as produced by Task 1 — current scripts and deps)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08 no git hooks, D-09 Vitest, D-10 smoke test only, D-11 no coverage, D-14 skeleton)
  </read_first>
  <action>
    1. Install dev deps: `npm install -D vitest prettier prettier-plugin-tailwindcss` (per D-09 Vitest is the runner; per STACK.md prettier-plugin-tailwindcss for class sorting).
    2. Add package.json scripts: `"typecheck": "tsc --noEmit"`, `"test": "vitest run"`, `"format": "prettier --write ."`. Keep the scaffold's existing dev/build/lint scripts.
    3. Create vitest.config.ts: minimal defineConfig from `vitest/config`, test.environment "node", test.include covering `tests/**/*.test.ts`. Per D-10 do NOT add jsdom, @testing-library, or any DOM setup. Per D-11 do NOT add coverage config or thresholds. Per D-12 do NOT add any E2E tooling.
    4. Create tests/smoke.test.ts: one trivial passing test (describe "smoke" / it asserts a truthy expression) so `npm test` runs green in CI from day one (D-10).
    5. Create .prettierrc with `{"plugins": ["prettier-plugin-tailwindcss"]}`. Per D-08: NO husky, NO lint-staged, NO git hooks of any kind — npm scripts + CI are the enforcement points.
    6. D-14 directory skeleton: create src/db/schema.ts containing only a one-line comment stating the Drizzle schema lands via plan 01-05 (D-16) and Phase 2 replaces it with the real schema; create empty-dir keepers src/data/.gitkeep, src/engine/.gitkeep, src/components/.gitkeep. src/app already exists from Task 1.
    7. Run lint, typecheck, and test to confirm all three exit 0.
  </action>
  <verify>
    <automated>npm run lint && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0; `npm run typecheck` exits 0; `npm test` exits 0 reporting exactly 1 passed test
    - package.json devDependencies contain vitest, prettier, prettier-plugin-tailwindcss
    - package.json contains no husky or lint-staged entries; no .husky/ directory exists (D-08)
    - vitest.config.ts contains no coverage configuration (D-11)
    - Directories src/app, src/db, src/data, src/engine, src/components all exist (D-14)
    - tests/smoke.test.ts exists; src/db/schema.ts exists
  </acceptance_criteria>
  <done>lint + typecheck + test all green via npm scripts; skeleton directories in place for Phases 2-4 to drop into their slots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → public GitHub repo | Anything committed becomes public (portfolio repo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .gitignore / env files | mitigate | `.env*` ignored in Task 1, before any env file exists anywhere in the phase; no .env file created in this plan |
| T-01-SC | Tampering | npm installs | mitigate | All packages (next, react, tailwindcss, vitest, prettier, prettier-plugin-tailwindcss) are named with exact versions verified against the npm registry in .planning/research/STACK.md (queried 2026-08-31, HIGH confidence); no [ASSUMED]/[SUS] packages in this plan |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint && npm run typecheck && npm test` all exit 0
- `git check-ignore .env` exits 0 (path would be ignored)
- src/{app,db,data,engine,components} all exist
</verification>

<success_criteria>
Buildable Next.js 16 + Tailwind v4 app at repo root with green lint/typecheck/test scripts, the D-14 skeleton, no git hooks, no coverage config, and env hygiene in place — ready for Wave 2 (UI foundation ∥ CI) to build on.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
