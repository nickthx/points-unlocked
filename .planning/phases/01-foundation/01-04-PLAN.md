---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified:
  - .vercel/project.json
  - .env.development.local
autonomous: false
requirements: [PLAT-01]
user_setup:
  - service: vercel
    why: "Hosting + auto-deploy from GitHub (PLAT-01)"
    env_vars: []
    dashboard_config:
      - task: "Approve Vercel CLI login if an auth gate is raised (vercel login)"
        location: "Browser — vercel.com device confirmation"
  - service: neon
    why: "Postgres database, provisioned through the Vercel Marketplace (env vars auto-injected)"
    env_vars:
      - name: DATABASE_URL
        source: "Auto-injected into the Vercel project by the Neon Marketplace integration — not manually copied"
    dashboard_config:
      - task: "Install the Neon integration into the points-unlocked project (creates database, injects DATABASE_URL)"
        location: "Vercel Dashboard → project points-unlocked → Storage → Marketplace → Neon (or vercel.com/marketplace/neon)"

must_haves:
  truths:
    - "A Vercel project exists, linked to the GitHub repo, so pushing to main auto-deploys production (D-01, D-06)"
    - "The app is served on a free *.vercel.app subdomain — no custom domain (D-02)"
    - "The Neon Marketplace integration is installed and DATABASE_URL exists in the Vercel project's environments"
    - "DATABASE_URL is pulled locally into a gitignored env file — env vars flow locally AND in production"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Local link to the Vercel project (gitignored)"
    - path: ".env.development.local"
      provides: "Local DATABASE_URL pulled via vercel env pull (gitignored, never committed)"
      contains: "DATABASE_URL"
  key_links:
    - from: "GitHub repo main branch"
      to: "Vercel production deployment"
      via: "Vercel Git integration (vercel link + git connect)"
    - from: "Neon Marketplace integration"
      to: "Vercel project env vars"
      via: "auto-injected DATABASE_URL"
---

<objective>
Create the Vercel project, wire push-to-main auto-deploy from the GitHub repo, install the Neon Postgres integration from the Vercel Marketplace (the one step Claude cannot complete alone — browser confirmation), and pull the injected env vars locally.

Purpose: PLAT-01 / phase success criteria 1-2 — public Vercel URL with auto-deploy, and DATABASE_URL flowing locally and in production for plan 01-05's Drizzle wiring.
Output: Linked Vercel project on *.vercel.app, Neon integration installed, .env.development.local with DATABASE_URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create + link Vercel project and connect Git auto-deploy</name>
  <files>.vercel/project.json</files>
  <read_first>
    - .gitignore (confirm `.vercel` and `.env*` are ignored BEFORE this task creates either artifact)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 real app from day one, D-02 free *.vercel.app subdomain, project name is Claude's discretion)
  </read_first>
  <action>
    1. Verify `.vercel` and `.env*` appear in .gitignore (created by plan 01-01's scaffold). If `.vercel` is missing, add it before running any vercel command.
    2. Check auth: `vercel whoami`. If unauthenticated, raise a dynamic auth-gate checkpoint for `vercel login`, then retry.
    3. Link/create the project non-interactively: `vercel link --yes --project points-unlocked` (project name "points-unlocked" — Claude's discretion per CONTEXT; creates the project if absent). Do NOT configure a custom domain — D-02 locks the free points-unlocked.vercel.app-style subdomain until Phase 7.
    4. Connect the GitHub repo for auto-deploy: `vercel git connect` (uses the origin remote wired in plan 01-03). This makes every push to main a production deploy, unconditionally — D-06: no required checks, no deploy gating.
    5. Confirm the link: `vercel project ls` shows points-unlocked; .vercel/project.json exists locally and `git check-ignore .vercel/project.json` exits 0.
  </action>
  <verify>
    <automated>vercel whoami && git check-ignore .vercel/project.json</automated>
  </verify>
  <acceptance_criteria>
    - .vercel/project.json exists and `git check-ignore .vercel/project.json` exits 0
    - `vercel project ls` output includes points-unlocked
    - `vercel git connect` reported the repo connected (or `vercel project inspect points-unlocked` shows the linked Git repository)
    - No custom domain configured (project serves only *.vercel.app per D-02)
  </acceptance_criteria>
  <done>Vercel project exists, is linked locally, and is bound to the GitHub repo so main auto-deploys to the free subdomain.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Install Neon Postgres from the Vercel Marketplace</name>
  <files>(none — Vercel/Neon dashboard state)</files>
  <read_first>
    - .planning/research/STACK.md (Neon-via-Marketplace rationale: one click, DATABASE_URL auto-injected, billing through Vercel)
  </read_first>
  <action>
    Automation-first: before pausing, attempt the CLI path — `vercel integration add neon`. This typically opens a browser confirmation the human must complete (Marketplace consent + provisioning cannot be finished headlessly). Whether the CLI opens the flow or the user starts from the dashboard, the human completes the install per how-to-verify below, then Claude resumes and confirms DATABASE_URL via `vercel env ls`.
  </action>
  <verify>
    <automated>vercel env ls | findstr DATABASE_URL</automated>
    <!-- run after the human resumes the checkpoint -->
  </verify>
  <acceptance_criteria>
    - Neon integration shows as installed for the points-unlocked project
    - `vercel env ls` lists DATABASE_URL (Development, Preview, and Production)
  </acceptance_criteria>
  <done>Neon database provisioned through the Marketplace with DATABASE_URL injected into the Vercel project's environments.</done>
  <what-built>Vercel project "points-unlocked" is created and Git-connected (Task 1). Claude first attempts the CLI path: `vercel integration add neon` — if that completes the browser handoff for you, most of this checkpoint is just confirming. What remains is inherently a browser/consent flow: approving the Marketplace integration install and database provisioning (billing consent through Vercel).</what-built>
  <how-to-verify>
    1. Open the Vercel Dashboard → project "points-unlocked" → Storage tab (or vercel.com/marketplace/neon) and install the Neon integration into this project.
    2. Accept the defaults: free plan, one database, connect to the points-unlocked project. Allow it to inject environment variables.
    3. Confirm in project Settings → Environment Variables that DATABASE_URL now exists for Development, Preview, and Production (Neon may inject additional POSTGRES_/DATABASE_ vars — fine, leave them).
    4. Tell Claude it's done; Claude will then run `vercel env ls` to confirm DATABASE_URL is present before proceeding.
  </how-to-verify>
  <resume-signal>Type "done" (or "approved") once the Neon integration is installed and DATABASE_URL shows in the Vercel project env vars</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Pull env vars locally and verify secret hygiene</name>
  <files>.env.development.local</files>
  <read_first>
    - .gitignore (assert `.env*` coverage one more time before an env file is written to disk)
  </read_first>
  <action>
    1. Run `vercel env ls` and assert DATABASE_URL is listed (proof the Marketplace injection landed).
    2. Run `vercel env pull .env.development.local` to write the development env vars locally — this is the file Next.js auto-loads and plan 01-05's drizzle.config.ts will load.
    3. Hygiene assertions: `git check-ignore .env.development.local` exits 0; `git status --porcelain` shows no env file staged/untracked-and-addable... (it will show nothing for ignored files); the file contains a line starting with `DATABASE_URL=`. NEVER print the value of DATABASE_URL to output or logs — assert presence by key name only (e.g. findstr/grep for `^DATABASE_URL=` and report match count).
  </action>
  <verify>
    <automated>git check-ignore .env.development.local && node -e "const f=require('fs').readFileSync('.env.development.local','utf8'); process.exit(/^DATABASE_URL=.+/m.test(f)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `vercel env ls` output includes DATABASE_URL
    - .env.development.local exists locally and contains a DATABASE_URL= line (value never echoed)
    - `git check-ignore .env.development.local` exits 0
    - `git ls-files` contains no file matching .env*
  </acceptance_criteria>
  <done>DATABASE_URL flows in production (Vercel env) and locally (pulled file), with the secret provably gitignored — plan 01-05 can push schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local machine → git history → public GitHub | An env file committed once is permanently leaked on a public repo |
| Vercel/Neon → app | DATABASE_URL grants full DB access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | .env.development.local | mitigate | .gitignore `.env*` verified (read_first) BEFORE `vercel env pull` writes the file; acceptance criterion asserts `git ls-files` has no .env* match |
| T-01-05 | Information Disclosure | executor output/logs | mitigate | Task 3 explicitly forbids echoing the DATABASE_URL value; presence asserted by key name and regex only |
| T-01-06 | Elevation of Privilege | Marketplace integration consent | transfer | Billing/provisioning consent is a human browser action (checkpoint Task 2) — Claude never handles Vercel/Neon credentials directly |
</threat_model>

<verification>
- Vercel project linked, Git-connected, on free *.vercel.app subdomain only
- DATABASE_URL present in `vercel env ls` and in gitignored .env.development.local
- No .env* file tracked by git
</verification>

<success_criteria>
Push-to-main auto-deploy is wired and Neon Postgres env vars flow both locally and in production — phase success criterion 2's infrastructure half complete; only the Drizzle proof (01-05) remains.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
