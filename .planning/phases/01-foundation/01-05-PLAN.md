---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-04]
files_modified:
  - package.json
  - package-lock.json
  - drizzle.config.ts
  - src/db/schema.ts
  - src/db/index.ts
  - scripts/db-check.ts
  - src/app/page.tsx
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "The full data path is proven: schema.ts → drizzle-kit push → Neon → queried from a server component (D-16)"
    - "One placeholder table health_check exists in Neon — no real product tables stubbed early (D-16)"
    - "The production *.vercel.app URL serves the real app shell over HTTP 200 with 'Points Unlocked' in the HTML (D-01, D-02, PLAT-01)"
    - "The production deployment was produced by a push to main (auto-deploy verified, D-06/D-07)"
    - "lint, typecheck, and test still run green with the DB code in place"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Drizzle placeholder table"
      contains: "export const healthCheck"
    - path: "src/db/index.ts"
      provides: "Drizzle client over drizzle-orm/neon-http"
      contains: "neon-http"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config pointing at src/db/schema.ts with DATABASE_URL"
    - path: "scripts/db-check.ts"
      provides: "Round-trip proof script (insert + select against health_check)"
    - path: "src/app/page.tsx"
      provides: "Server component querying health_check with graceful fallback (D-16)"
      contains: "healthCheck"
  key_links:
    - from: "drizzle.config.ts"
      to: ".env.development.local"
      via: "process.loadEnvFile guarded load of DATABASE_URL"
      pattern: "loadEnvFile"
    - from: "src/app/page.tsx"
      to: "src/db/index.ts"
      via: "db.select().from(healthCheck) inside try/catch"
      pattern: "from\\(healthCheck\\)"
    - from: "git push to main"
      to: "https://points-unlocked*.vercel.app"
      via: "Vercel Git integration production deploy"
---

<objective>
Wire Drizzle to Neon, push the D-16 placeholder `health_check` table, prove the full path by querying it from the homepage server component, and verify the production deployment end-to-end (200 + content + noindex on the public URL, deployed by a push to main).

Purpose: PLAT-01 — closes all three phase success criteria; every later phase now ships onto verified live infrastructure.
Output: health_check table live in Neon, DB-backed status line on the homepage, verified public production URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle deps, health_check schema, client, and config (D-16)</name>
  <files>package.json, package-lock.json, src/db/schema.ts, src/db/index.ts, drizzle.config.ts, scripts/db-check.ts</files>
  <read_first>
    - src/db/schema.ts (current stub from plan 01-01 — replace its contents)
    - .env.development.local (existence only — do NOT read or print its values; it holds DATABASE_URL)
    - CLAUDE.md (repo root — locked versions: drizzle-orm 0.45.x, drizzle-kit 0.31.x, @neondatabase/serverless 1.1.x; What NOT to Use: no @vercel/postgres)
    - .planning/research/STACK.md (neon-http driver pattern: drizzle(neon(DATABASE_URL)) via drizzle-orm/neon-http)
  </read_first>
  <action>
    1. Install: `npm install drizzle-orm @neondatabase/serverless` and `npm install -D drizzle-kit tsx` (tsx runs the check script; STACK.md already designates it for seed scripts). Per CLAUDE.md do NOT install @vercel/postgres or any Prisma package.
    2. Replace src/db/schema.ts contents with the D-16 placeholder table only — `export const healthCheck = pgTable("health_check", ...)` with columns: `id` (serial or identity, primary key), `status` (text, notNull, default "ok"), `checkedAt` (timestamp with defaultNow). Add a comment: Phase 2 replaces this with the real schema (programs, transfer_routes, transfer_bonuses, redemptions per ARCHITECTURE.md); per D-16 do NOT stub real table names now.
    3. Create src/db/index.ts: `import { neon } from "@neondatabase/serverless"`, `import { drizzle } from "drizzle-orm/neon-http"`, export `db = drizzle(neon(process.env.DATABASE_URL!))` plus re-export of the schema. Keep it minimal — no pooling config needed with the HTTP driver.
    4. Create drizzle.config.ts at repo root: `dialect: "postgresql"`, `schema: "./src/db/schema.ts"`, `out: "./drizzle"`, `dbCredentials: { url: process.env.DATABASE_URL! }`. At the top, load local env for CLI runs with a guarded `try { process.loadEnvFile(".env.development.local") } catch {}` (Node 22 built-in — no dotenv dependency; the try/catch keeps CI/Vercel working where the file doesn't exist and env vars are already set).
    5. Create scripts/db-check.ts: a tsx script importing db + healthCheck that inserts one row ({ status: "ok" }) and selects rows back, printing only the row count and status value (never the connection string), exiting 0 on success and 1 on failure. Load env the same guarded way.
    6. `npm run typecheck` and `npm run lint` green.
  </action>
  <verify>
    <automated>npm run typecheck && node -e "const s=require('fs').readFileSync('src/db/schema.ts','utf8'); if(!s.includes('export const healthCheck')||!s.includes('health_check')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include drizzle-orm and @neondatabase/serverless; devDependencies include drizzle-kit and tsx; no @vercel/postgres, no prisma
    - src/db/schema.ts contains `export const healthCheck` mapping table name "health_check" and no other table
    - src/db/index.ts imports from "drizzle-orm/neon-http"
    - drizzle.config.ts exists with schema "./src/db/schema.ts" and a guarded loadEnvFile call
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Drizzle layer compiles: placeholder schema, neon-http client, CLI config, and a round-trip check script ready for the push.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema to Neon and prove the round trip</name>
  <files>(no repo files — remote database state)</files>
  <read_first>
    - drizzle.config.ts (the config the push will use — created in Task 1)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-16: push via drizzle-kit to prove schema.ts → Neon)
  </read_first>
  <action>
    BLOCKING TASK — the phase cannot pass verification without this; build/typecheck succeed without it, which would be a false positive.

    1. Run `npx drizzle-kit push`. Creating a brand-new table on an empty database requires no destructive confirmation; if drizzle-kit nevertheless prompts interactively (it can on ambiguous changes), re-run as `npx drizzle-kit push --force` — acceptable here because the database is empty and the only change is creating health_check.
    2. Prove the full round trip: `npx tsx scripts/db-check.ts` — must exit 0, reporting at least 1 row with status "ok" in health_check. This proves DATABASE_URL works locally against the production Neon database (the Marketplace default; per STACK.md dev/prod parity is exact).
    3. If DATABASE_URL is missing or the connection fails, stop and surface the error (do not fake success) — the fix path is re-running plan 01-04 Task 3 (`vercel env pull`).
  </action>
  <verify>
    <automated>npx tsx scripts/db-check.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` completes without error
    - `npx tsx scripts/db-check.ts` exits 0 and reports >=1 row from health_check with status "ok"
    - No connection string value appears in any command output
  </acceptance_criteria>
  <done>health_check exists in Neon with a live row — schema.ts → Neon proven, D-16 push requirement satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Query from a server component + verify production deploy end-to-end</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current D-04 homepage from plan 01-02 — extend, do not rebuild; keep wordmark, pitch, in-progress note intact)
    - src/db/index.ts and src/db/schema.ts (exports to consume: db, healthCheck)
    - .planning/research/PITFALLS.md (Neon cold-start note; never render raw errors publicly)
  </read_first>
  <action>
    1. Extend src/app/page.tsx (already an async-capable server component): add `export const dynamic = "force-dynamic"` with a comment that this is a Phase 1 placeholder proving the live DB path (D-16) and Phase 2+ moves to cached reads per ARCHITECTURE.md. In the component, wrap `await db.select().from(healthCheck).limit(1)` in try/catch. On success render a subtle one-line status inside the existing in-progress note (e.g. "infrastructure: live"); on failure render a neutral fallback (e.g. "infrastructure: warming up") — NEVER render the caught error object or message (public page, T-01-07). Keep D-04 content (wordmark, pitch, note) untouched.
    2. Run `npm run build`, `npm run lint`, `npm run typecheck`, `npm test` — all green (build may execute no DB call since the route is dynamic; that's expected).
    3. After the orchestrator's commit is pushed to main, verify auto-deploy: poll `vercel ls points-unlocked` (or `vercel inspect` on the latest deployment) until the newest production deployment is READY, and confirm it was created by the Git integration from the main-branch commit (deployment meta shows the commit SHA), not a manual `vercel deploy`. If no deployment appears within ~5 minutes, run `vercel deploy --prod` once to unblock, but record in the SUMMARY that Git auto-deploy needs attention — the phase success criterion is auto-deploy.
    4. Verify the public URL: fetch `https://<production-domain>` (from `vercel inspect`/project settings; a *.vercel.app domain per D-02). Assert: HTTP status 200; body contains "Points Unlocked"; body contains a robots meta tag with noindex (D-03); body contains the exact success-branch text "infrastructure: live" — the failure fallback "infrastructure: warming up" does NOT pass (it would mean the production DATABASE_URL is broken, silently faking success criterion 2). If the fallback renders, diagnose the production env var before proceeding.
  </action>
  <verify>
    <automated>npm run build && npm test && node -e "const p=require('fs').readFileSync('src/app/page.tsx','utf8'); if(!/from\(healthCheck\)/.test(p)||!/catch/.test(p)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains a try/catch-wrapped `db.select().from(healthCheck)` query and still contains "Points Unlocked" (D-04 preserved)
    - src/app/page.tsx renders no raw error/exception content in the failure branch
    - `npm run build && npm run lint && npm run typecheck && npm test` all exit 0
    - Latest Vercel production deployment is READY and sourced from a main-branch git commit
    - `curl -s -o - -w "%{http_code}" https://<production-domain>/` returns 200 and the body contains "Points Unlocked", a noindex robots meta tag, and the exact DB success text "infrastructure: live" (the fallback "infrastructure: warming up" fails this criterion)
  </acceptance_criteria>
  <done>The public *.vercel.app URL serves the real app shell, rendered by a server component that read from Neon through Drizzle, deployed automatically by a push to main — all three phase success criteria observably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app server → Neon | DATABASE_URL-authenticated; secret lives only in env |
| public internet → rendered page | Anything the server component renders is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | src/app/page.tsx error path | mitigate | try/catch renders a fixed neutral string; the caught error (which can embed host/connection details) is never rendered or logged with values |
| T-01-08 | Information Disclosure | scripts/db-check.ts output | mitigate | Script prints row count + status only; connection string never echoed |
| T-01-SC | Tampering | npm installs (drizzle-orm, @neondatabase/serverless, drizzle-kit, tsx) | mitigate | All four named with exact versions verified against the npm registry in .planning/research/STACK.md (2026-08-31); no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
Phase-closing checks (goal-backward against all three success criteria):
1. `curl` of the production *.vercel.app URL → 200, "Points Unlocked", noindex meta, and the exact DB success text "infrastructure: live" — not the "warming up" fallback (criteria 1 + 2 + D-01/D-02/D-03/D-04)
2. `npx tsx scripts/db-check.ts` exits 0 locally; the same DATABASE_URL is present in `vercel env ls` (criterion 2, D-16)
3. Latest production deployment sourced from a main-branch push; latest CI run green on lint/typecheck/test jobs (criterion 3, D-05/D-06/D-07)
</verification>

<success_criteria>
All three Phase 1 success criteria are observably true on live infrastructure: public URL serving the built app, Neon connected through Drizzle with env vars flowing both sides, and push-to-main auto-deploy with green advisory CI.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
