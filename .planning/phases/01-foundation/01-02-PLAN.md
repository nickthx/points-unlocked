---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - components.json
  - package.json
  - package-lock.json
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/lib/utils.ts
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "shadcn/ui is initialized with the form-adjacent set (button, input, label, card, dialog) vendored so Phase 4 needs no CLI runs mid-build (D-15)"
    - "Fraunces + Inter load via next/font/google and are wired into Tailwind v4 @theme tokens with a warm palette (D-13)"
    - "The homepage shows the Points Unlocked wordmark, a one-sentence pitch, and an in-progress note — the real app shell, no holding page (D-01, D-04)"
    - "Every page carries a robots noindex meta tag until the Phase 7 launch gate (D-03)"
    - "npm run build still exits 0"
  artifacts:
    - path: "components.json"
      provides: "shadcn config (Tailwind v4 + React 19 preset)"
    - path: "src/components/ui/dialog.tsx"
      provides: "Vendored shadcn dialog (D-15 set complete)"
    - path: "src/app/globals.css"
      provides: "@theme tokens: --font-display (Fraunces), --font-sans (Inter), warm palette variables (D-13)"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "next/font setup + robots noindex metadata"
      contains: "noindex"
    - path: "src/app/page.tsx"
      provides: "D-04 homepage: wordmark + pitch + in-progress note"
      contains: "Points Unlocked"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "font CSS variables consumed by @theme tokens"
      pattern: "--font-fraunces"
---

<objective>
Initialize shadcn/ui with the D-15 component set, land the D-13 editorial typography (Fraunces + Inter) and warm-palette @theme tokens, and replace the scaffold homepage with the D-04 minimal-but-intentional shell carrying the D-03 noindex tag.

Purpose: PLAT-01 — the deployed URL always shows the real app (D-01); Phases 4-5 build on real design tokens instead of retrofitting (D-13).
Output: Vendored shadcn primitives, typography/palette token system, homepage shell with noindex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/STACK.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn init + vendored component set (D-15)</name>
  <files>components.json, package.json, package-lock.json, src/components/ui/button.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/components/ui/card.tsx, src/components/ui/dialog.tsx, src/lib/utils.ts, src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current Tailwind v4 content from scaffold — shadcn init rewrites/extends it)
    - components.json IF it exists (it should not yet)
    - CLAUDE.md (repo root — What NOT to Use: tailwindcss-animate is deprecated; tw-animate-css is the v4 replacement installed by shadcn init)
  </read_first>
  <action>
    1. Run `npx shadcn@latest init --yes` with a neutral base color (the warm editorial palette comes from our own @theme tokens in Task 2, not from shadcn's base). Current CLI defaults to the Tailwind v4 + React 19 preset per STACK.md.
    2. Run `npx shadcn@latest add button input label card dialog --yes` — exactly the D-15 form-adjacent set so Phase 4's balance-entry form needs zero CLI runs mid-build. Do not add other components.
    3. Verify the init used v4-era animation: package.json includes `tw-animate-css`, NOT `tailwindcss-animate` (deprecated per CLAUDE.md). Confirm lucide-react landed as the icon dependency (shadcn default).
    4. Run `npm run build` and `npm run lint` to confirm the vendored components compile clean.
  </action>
  <verify>
    <automated>npm run build && node -e "['button','input','label','card','dialog'].forEach(c=>{require('fs').accessSync('src/components/ui/'+c+'.tsx')})"</automated>
  </verify>
  <acceptance_criteria>
    - components.json exists at repo root
    - src/components/ui/{button,input,label,card,dialog}.tsx all exist (D-15)
    - src/lib/utils.ts exists exporting the `cn` helper
    - package.json contains tw-animate-css; package.json does NOT contain tailwindcss-animate
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All five D-15 shadcn components vendored and compiling; no deprecated animation package.</done>
</task>

<task type="auto">
  <name>Task 2: Editorial typography + warm @theme tokens (D-13)</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (post-shadcn-init state from Task 1 — preserve shadcn's CSS variables, extend with our tokens)
    - src/app/layout.tsx (scaffold font setup to replace)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-13 exact requirement + specifics: Fraunces chosen for its optical-size axis to carry big dollar numbers)
  </read_first>
  <action>
    1. In src/app/layout.tsx, replace the scaffold's Geist font setup: import `Fraunces` and `Inter` from `next/font/google`. Configure Fraunces as a variable font with the `opsz` axis enabled (axes: ["opsz"]) — the optical-size axis is why Fraunces was chosen (it will carry the big dollar numbers in the Phase 4 wow reveal). Expose CSS variables `--font-fraunces` and `--font-inter` (variable option on each font), both with `subsets: ["latin"]` and `display: "swap"`. Apply both variable classNames to the html or body element.
    2. In src/app/globals.css, inside a Tailwind v4 `@theme` block (CSS-first config — NO tailwind.config.js per CLAUDE.md), define: `--font-display: var(--font-fraunces), ui-serif, Georgia, serif;` and `--font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;` so `font-display` and `font-sans` utilities exist. Add a display type-scale token or two for headline sizes (executor's discretion on exact values).
    3. Add warm palette starting values as @theme color tokens (Claude's discretion per CONTEXT, refined in Phase 7): a warm off-white/cream page background (e.g. oklch or hex around #FAF7F2), warm near-black ink for text, and one warm accent (terracotta/amber family). Name them semantically (e.g. --color-cream, --color-ink, --color-accent). Preserve all shadcn-generated CSS variables — extend, don't replace.
    4. Set the body background/text to the warm tokens and default body font to the sans token.
    5. `npm run build` green.
  </action>
  <verify>
    <automated>npm run build && node -e "const c=require('fs').readFileSync('src/app/globals.css','utf8'); if(!c.includes('@theme')||!c.includes('--font-fraunces')) process.exit(1); const l=require('fs').readFileSync('src/app/layout.tsx','utf8'); if(!l.includes('Fraunces')||!l.includes('Inter')) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/layout.tsx imports Fraunces and Inter from next/font/google with variable CSS custom properties --font-fraunces / --font-inter
    - src/app/globals.css contains an @theme block referencing var(--font-fraunces) and var(--font-inter)
    - src/app/globals.css defines warm palette color tokens inside @theme
    - No tailwind.config.js exists at repo root (v4 CSS-first preserved)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Fraunces (with opsz) + Inter self-hosted via next/font; Tailwind v4 @theme exposes font-display/font-sans utilities and warm palette tokens for Phases 4-7.</done>
</task>

<task type="auto">
  <name>Task 3: D-04 homepage shell + D-03 noindex</name>
  <files>src/app/page.tsx, src/app/layout.tsx</files>
  <read_first>
    - src/app/page.tsx (scaffold content to fully replace)
    - src/app/layout.tsx (post-Task-2 state — add metadata export here)
    - .planning/PROJECT.md (the one-sentence pitch source: "shows people who have credit card points — but no idea what to do with them — how powerful those points actually are")
  </read_first>
  <action>
    1. Replace src/app/page.tsx entirely with the D-04 homepage: the "Points Unlocked" wordmark set in the display font (font-display / Fraunces, large), a one-sentence pitch derived from PROJECT.md ("See what your credit card points are actually worth" or close paraphrase — beginner-readable, no jargon), and a short in-progress note (e.g. "In progress — launching soon"). Intentional-looking but minimal: centered layout, warm background token, generous whitespace. This page IS the production app shell (D-01, no holding page) and is replaced by the balance-entry flow in Phase 4. Keep it a server component; no client JS.
    2. In src/app/layout.tsx export `metadata` with: title "Points Unlocked", a description matching the pitch, and `robots: { index: false, follow: false }` — the D-03 noindex gate. Add a code comment marking removal as a Phase 7 launch-gate task.
    3. `npm run build` green; confirm the built HTML for / contains the robots meta by building and grepping .next output or by running the dev server and curling localhost.
  </action>
  <verify>
    <automated>npm run build && node -e "const p=require('fs').readFileSync('src/app/page.tsx','utf8'); if(!p.includes('Points Unlocked')) process.exit(1); const l=require('fs').readFileSync('src/app/layout.tsx','utf8'); if(!/index:\s*false/.test(l)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains the string "Points Unlocked" and renders wordmark + one-sentence pitch + in-progress note (D-04)
    - src/app/layout.tsx metadata export sets robots index: false (D-03) with a Phase 7 removal comment
    - `npm run build` exits 0
    - Built output for the root route contains a meta robots tag with noindex
  </acceptance_criteria>
  <done>Production-bound homepage shell exists — wordmark, pitch, in-progress note, noindexed — the real app from day one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel → public internet | The deployed page is world-readable pre-launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | pre-launch site in search indexes | mitigate | robots noindex metadata in src/app/layout.tsx (D-03); removal is an explicit Phase 7 launch-gate task |
| T-01-SC | Tampering | npm installs (shadcn-added deps) | mitigate | shadcn CLI adds only its documented deps (radix primitives, lucide-react, tw-animate-css, clsx/tailwind-merge) — all named in .planning/research/STACK.md npm-registry verification (2026-08-31); no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `npm run build && npm run lint && npm run typecheck && npm test` all exit 0
- All five D-15 components exist under src/components/ui/
- globals.css @theme defines font + warm palette tokens; layout loads Fraunces (opsz) + Inter
- Homepage contains "Points Unlocked" + pitch + in-progress note; robots noindex present
</verification>

<success_criteria>
The app shell that will sit on the production URL is real, intentional, token-driven, and noindexed; Phase 4 can build the balance-entry flow with preinstalled primitives and real typography tokens.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
