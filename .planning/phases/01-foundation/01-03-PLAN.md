---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [PLAT-01]

must_haves:
  truths:
    - "The repo has a GitHub remote named origin and pushes to main reach it (D-07 workflow)"
    - "Every push runs lint + typecheck + test as visible GitHub checks (D-05)"
    - "CI is advisory only — nothing blocks a Vercel deploy on red (D-06)"
    - "No git hooks exist anywhere in the setup (D-08)"
    - "The CI workflow references no secrets, so nothing can leak into CI logs"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions workflow: lint, typecheck, test jobs on push + pull_request"
      contains: "npm run typecheck"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts (plan 01-01)"
      via: "npm ci + npm run lint / npm run typecheck / npm test"
      pattern: "npm (run (lint|typecheck)|test)"
---

<objective>
Ensure the GitHub remote exists (the repo itself is a portfolio artifact — visible green checks matter), then add the advisory CI workflow running lint, typecheck, and test on every push.

Purpose: PLAT-01 / phase success criterion 3 — lint, typecheck, and test run green on push; recruiters browsing the repo see green checks (D-05).
Output: origin remote wired, .github/workflows/ci.yml running three green jobs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify or create the GitHub remote</name>
  <files>(none — git/remote configuration only)</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-07: solo push-to-main workflow; specifics: the GitHub repo is itself a portfolio artifact)
  </read_first>
  <action>
    1. Run `git remote -v`. If an `origin` remote already exists, verify it resolves with `gh repo view --json nameWithOwner,visibility` and skip creation.
    2. If no remote exists: check auth with `gh auth status`; if unauthenticated, raise a dynamic auth-gate checkpoint asking the user to run `gh auth login`, then retry. Create the repo with `gh repo create points-unlocked --public --source=. --remote=origin --push` — public, because the repo with its green checks is part of the portfolio pitch (CONTEXT specifics). Current branch is main; push establishes it as the default branch.
    3. Do NOT configure branch protection, required status checks, or merge rules of any kind — D-06 (advisory CI) and D-07 (push straight to main, no PR ceremony) both forbid them.
  </action>
  <verify>
    <automated>git remote get-url origin && gh repo view --json nameWithOwner -q .nameWithOwner</automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url origin` exits 0 and prints a github.com URL
    - `gh repo view` succeeds against that remote
    - `git ls-remote --heads origin main` shows the main branch exists on the remote
    - No branch protection rules exist on main (`gh api repos/{owner}/{repo}/branches/main/protection` returns 404)
  </acceptance_criteria>
  <done>origin points at a reachable GitHub repo with main pushed and unprotected (push-to-main auto-deploy stays unconditional).</done>
</task>

<task type="auto">
  <name>Task 2: Advisory CI workflow — lint, typecheck, test (D-05, D-06)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - package.json (script names the workflow must invoke: lint, typecheck, test — created in plan 01-01)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-05 workflow scope, D-06 advisory-only, Claude's discretion on Node version/caching/job layout)
  </read_first>
  <action>
    1. Create .github/workflows/ci.yml named "CI", triggered on `push` (all branches — solo dev pushes main, D-07) and `pull_request`. Three separate jobs — `lint`, `typecheck`, `test` — so the repo shows three distinct green checks (portfolio visibility per D-05). Each job: actions/checkout@v4, actions/setup-node@v4 with node-version 22 and `cache: npm`, `npm ci`, then the matching script (`npm run lint`, `npm run typecheck`, `npm test`). Discretion choices (Node 22, npm cache, parallel jobs) per CONTEXT's Claude's Discretion list.
    2. The workflow MUST reference no secrets and no env vars — lint/typecheck/test need none (Vitest smoke test is pure). This is the T-01-03 mitigation: nothing sensitive can appear in public CI logs.
    3. Advisory posture (D-06): add nothing that gates deploys — no `required` checks, no deployment jobs, no Vercel steps. Vercel builds in parallel via its own Git integration (wired in plan 01-04).
    4. After the orchestrator's commit of this plan is pushed, verify the run: `gh run list --workflow=ci.yml --limit 1` shows a run; `gh run watch <id> --exit-status` (or poll `gh run view`) confirms all three jobs conclude `success`.
  </action>
  <verify>
    <automated>gh run list --workflow=ci.yml --limit 1 --json conclusion -q ".[0].conclusion" | findstr success</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists with jobs named lint, typecheck, test
    - The file contains `npm run lint`, `npm run typecheck`, and `npm test` (one per job)
    - The file contains no `secrets.` references and no `env:` blocks with credentials
    - The workflow triggers on push and pull_request
    - The latest CI run on GitHub concludes success for all three jobs
  </acceptance_criteria>
  <done>Every push to the repo produces three visible green checks; a red check never blocks anything (advisory only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public GitHub Actions logs | CI logs on a public repo are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | GitHub Actions logs | mitigate | ci.yml uses zero secrets/env credentials; lint/typecheck/test require none — asserted by acceptance criteria (no `secrets.` reference in file) |
| T-01-SC | Tampering | GitHub Actions dependencies | mitigate | Only first-party actions used (actions/checkout@v4, actions/setup-node@v4); no third-party marketplace actions |
</threat_model>

<verification>
- `git remote get-url origin` resolves; main exists on remote, unprotected
- ci.yml present with three secret-free jobs invoking the plan 01-01 scripts
- Latest workflow run green on all three jobs
</verification>

<success_criteria>
Pushing to main produces three visible green GitHub checks (lint, typecheck, test) that never block deployment — phase success criterion 3's CI half is done.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
