---
phase: 04-core-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/lib/balance-params.ts
  - src/lib/balance-storage.ts
  - tests/balance-params.test.ts
  - tests/balance-storage.test.ts
autonomous: true
requirements: [INPUT-02, INPUT-03]
must_haves:
  truths:
    - "A URL query string like ?ur=90000&mr=50000 decodes to a valid Balances object with hostile values (negative, fractional, unsafe, unknown keys) dropped"
    - "A Balances object encodes back to short-key URL params, round-tripping losslessly for valid values"
    - "Stored localStorage JSON is validated field-by-field on read and discarded wholesale on any failure; a throwing storage never crashes the caller"
    - "The URL-vs-storage precedence decision (URL wins; storage hydrates only when URL is empty) is a pure function verified by unit tests"
  artifacts:
    - path: "src/lib/balance-params.ts"
      provides: "nuqs parser map, short-key codec, createLoader export for server + client"
      exports: ["PARAM_KEY_BY_SLUG", "balanceParsers", "loadBalanceParams", "paramsToBalances", "balancesToParams"]
    - path: "src/lib/balance-storage.ts"
      provides: "Injected-storage read/write with validation + pure precedence function"
      exports: ["STORAGE_KEY", "readStoredBalances", "writeStoredBalances", "resolveInitialBalances"]
    - path: "tests/balance-params.test.ts"
      provides: "INPUT-03 codec round-trip and hostile-input tests"
    - path: "tests/balance-storage.test.ts"
      provides: "INPUT-02 precedence and hostile-storage tests"
  key_links:
    - from: "src/lib/balance-params.ts"
      to: "nuqs/server"
      via: "createLoader + parseAsInteger import (server-safe entry, no 'use client')"
      pattern: "from \"nuqs/server\""
    - from: "src/lib/balance-params.ts"
      to: "@/engine"
      via: "type-only import of Balances + EnterableProgramSlug"
      pattern: "import type .* from \"@/engine\""
---

<objective>
Install the two phase dependencies (nuqs 2.10.1, react-number-format 5.4.5 — both slopcheck [OK], no postinstall scripts) and build the fully unit-tested state-codec layer: the URL param codec (INPUT-03) and the localStorage persistence + precedence module (INPUT-02). These are the pure foundations every UI plan builds on.

Purpose: INPUT-03's shareable URL and INPUT-02's reload persistence both hinge on codecs that reject hostile input. Encoding them as pure, node-testable functions closes the Wave 0 test gaps before any component exists.
Output: two lib modules + two green test suites + both packages installed at exact versions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-core-experience/04-RESEARCH.md
@.planning/phases/04-core-experience/04-PATTERNS.md

<interfaces>
From src/engine/types.ts (consume type-only; do not redefine):
```typescript
export type EnterableProgramSlug =
  | "chase-ur" | "amex-mr" | "capital-one" | "citi-ty"
  | "bilt" | "world-of-hyatt" | "hilton-honors" | "marriott-bonvoy";
export type Balances = Partial<Record<EnterableProgramSlug, number>>;
```

From src/engine/ranking.ts (validation guard to replicate at the URL/storage boundary — engine re-applies it as defense in depth, T-03-09):
```typescript
if (typeof value === "number" && Number.isSafeInteger(value) && value > 0) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install nuqs and react-number-format at exact versions</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependency block; do not disturb existing pins)
    - .planning/phases/04-core-experience/04-RESEARCH.md — "Package Legitimacy Audit" section (both packages [OK]; the earlier slopcheck run installed nothing, so this is the real install)
  </read_first>
  <action>
    Run `npm install nuqs@2.10.1 react-number-format@5.4.5` (exact versions per the RESEARCH version verification of 2026-09-01; both audited [OK] with empty postinstall — no legitimacy checkpoint required). Do not add any other package; react-number-format is consumed later by plan 04-03 but installs now so wave 2 needs no package.json write.
  </action>
  <verify>
    <automated>npm ls nuqs react-number-format && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls nuqs react-number-format` exits 0 and lists nuqs@2.10.1 and react-number-format@5.4.5
    - package.json dependencies contain the entries "nuqs" and "react-number-format"
    - `npm run build` still succeeds (no dependency conflict introduced)
  </acceptance_criteria>
  <done>Both packages resolvable at the pinned versions; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: URL param codec — src/lib/balance-params.ts + tests</name>
  <files>src/lib/balance-params.ts, tests/balance-params.test.ts</files>
  <read_first>
    - .planning/phases/04-core-experience/04-RESEARCH.md — Pattern 1 (copy the nuqs wiring verbatim: `createLoader`, `parseAsInteger` from "nuqs/server", PARAM_KEY_BY_SLUG map)
    - src/engine/types.ts — EnterableProgramSlug + Balances (import type-only from "@/engine")
    - src/engine/ranking.ts — sanitizeBalances guard style (lines ~35-43) to replicate in paramsToBalances
    - src/data/programs.ts — `as const satisfies` idiom precedent
    - tests/engine-ranking.test.ts — test file conventions (relative imports `../src/...`, `_` numeric separators, descriptive `it` strings with concrete numbers, header comment explaining fixture strategy)
  </read_first>
  <behavior>
    - Round-trip: `paramsToBalances(balancesToParams(b))` deep-equals `b` for a full 8-program Balances object of positive safe integers
    - Short-key mapping: `{ ur: 90_000, mr: 50_000 }` → `{ "chase-ur": 90_000, "amex-mr": 50_000 }`; all 8 keys covered: ur, mr, c1, ty, bilt, hyatt, hilton, bonvoy
    - Hostile dropping: values that are null, 0, negative, fractional (1.5), or > Number.MAX_SAFE_INTEGER are absent from the returned Balances (key omitted, not set to 0)
    - `balancesToParams({})` returns an object whose 8 values are all null (nuqs convention: null clears the key from the URL)
    - No "use client" directive and no browser API reference anywhere in balance-params.ts (server-importable)
  </behavior>
  <action>
    Create src/lib/balance-params.ts exporting: (1) `PARAM_KEY_BY_SLUG` — the 8-entry map from RESEARCH Pattern 1 (`"chase-ur"→"ur"`, `"amex-mr"→"mr"`, `"capital-one"→"c1"`, `"citi-ty"→"ty"`, `"bilt"→"bilt"`, `"world-of-hyatt"→"hyatt"`, `"hilton-honors"→"hilton"`, `"marriott-bonvoy"→"bonvoy"`) typed `as const satisfies Record<EnterableProgramSlug, string>`; (2) `balanceParsers` — object mapping each short key to `parseAsInteger` (import from "nuqs/server", never "nuqs" — keeps the module server-safe); (3) `loadBalanceParams = createLoader(balanceParsers)`; (4) `paramsToBalances(params)` — maps short keys back to slugs, keeping only values passing the sanitizeBalances guard (`typeof value === "number" && Number.isSafeInteger(value) && value > 0`); (5) `balancesToParams(balances)` — inverse, emitting `number | null` per short key (null when the slug is absent/invalid) so nuqs `setParams` can clear URL keys. Annotate the guard with a T-03-09 defense-in-depth comment and A3 short-key comment per the repo's decision-ID convention. Create tests/balance-params.test.ts covering every behavior above using relative imports.
  </action>
  <verify>
    <automated>npx vitest run tests/balance-params.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/balance-params.test.ts` exits 0 with tests covering: full round-trip, each of the 8 short keys, and at least 4 hostile-value cases (0, negative, fractional, unsafe integer)
    - `grep -c "\"use client\"" src/lib/balance-params.ts` outputs 0
    - `grep -c "from \"nuqs/server\"" src/lib/balance-params.ts` outputs 1 (no bare "nuqs" import)
    - `npm run typecheck` passes (a misspelled slug in PARAM_KEY_BY_SLUG is a compile error via the `satisfies` constraint)
  </acceptance_criteria>
  <done>Codec round-trips valid balances, drops hostile input, and is importable from both server and client modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: localStorage persistence + precedence — src/lib/balance-storage.ts + tests</name>
  <files>src/lib/balance-storage.ts, tests/balance-storage.test.ts</files>
  <read_first>
    - .planning/phases/04-core-experience/04-RESEARCH.md — Pattern 4 (precedence rules) + Pitfalls 2 and 6
    - src/lib/balance-params.ts (just created — reuse its validation guard approach; do NOT duplicate PARAM_KEY_BY_SLUG here: storage is keyed by canonical slug, not short key)
    - src/engine/valuation.ts — house style for pure helpers (JSDoc naming the requirement served, guard clauses degrading to safe values)
    - tests/engine-ranking.test.ts — test conventions
  </read_first>
  <behavior>
    - `readStoredBalances(fakeStorage)` returns the parsed Balances when JSON is valid and every key is an EnterableProgramSlug with a positive safe integer value
    - Returns null (wholesale discard) when: JSON is malformed, any key is unknown, any value is non-number/negative/fractional/unsafe — no partial salvage
    - Returns null and does not throw when storage.getItem throws (LinkedIn WebView guard, Pitfall 6)
    - `writeStoredBalances(fakeStorage, balances)` writes JSON under "pu:balances:v1" keyed by canonical slug; silently no-ops when storage.setItem throws
    - `resolveInitialBalances(urlBalances, storedBalances)`: URL non-empty → `{ source: "url" }` (never suggests writing storage — A1: a share link must not clobber the visitor's saved balances); URL empty + valid stored → `{ source: "storage", balances }`; both empty → `{ source: "none" }`
  </behavior>
  <action>
    Create src/lib/balance-storage.ts as pure functions with storage I/O injected (parameter typed `Pick<Storage, "getItem">` / `Pick<Storage, "setItem">`) so tests run in the node environment without jsdom. Export: `STORAGE_KEY = "pu:balances:v1"`, `readStoredBalances`, `writeStoredBalances`, and `resolveInitialBalances` returning a discriminated union (`{ source: "url" } | { source: "storage"; balances: Balances } | { source: "none" }`). Validate stored keys against the canonical slug set (derive from PARAM_KEY_BY_SLUG's keys or the EnterableProgramSlug-typed list — one source of truth, no string re-listing). All storage access inside try/catch; never log or rethrow the caught error (T-01-07 style). Annotate A1 (precedence), Pitfall 2 (effects-only usage note in JSDoc), Pitfall 6 (guard) with decision-ID comments. Create tests/balance-storage.test.ts with a fake storage object (plain object implementing getItem/setItem, plus throwing variants) covering every behavior above.
  </action>
  <verify>
    <automated>npx vitest run tests/balance-storage.test.ts && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/balance-storage.test.ts` exits 0 with cases for: valid read, malformed JSON → null, unknown key → null, negative/fractional/unsafe value → null, throwing getItem → null (no throw), throwing setItem → no throw, and all three precedence outcomes
    - `grep -c "pu:balances:v1" src/lib/balance-storage.ts` outputs at least 1
    - src/lib/balance-storage.ts contains no `localStorage` global reference (I/O is injected): `grep -c "localStorage" src/lib/balance-storage.ts` outputs 0
    - Full suite (`npm test`), typecheck, and lint all green
  </acceptance_criteria>
  <done>Storage layer validated hostile-input-proof; precedence is a tested pure function; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → app state | Untrusted, attacker-controllable share-link params cross into engine input |
| localStorage → app state | Tamperable client storage crosses into engine input |
| npm registry → node_modules | Third-party code enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | paramsToBalances (URL boundary) | mitigate | parseAsInteger + safe-integer/positive guard drops hostile values; engine sanitizeBalances re-applies (defense in depth, T-03-09); unit-tested with hostile cases |
| T-04-02 | Tampering | readStoredBalances (storage boundary) | mitigate | try/catch parse + per-field validation against known slugs and positive safe integers; wholesale discard on any failure; unit-tested |
| T-04-03 | Denial of service | storage access in restricted WebViews | mitigate | all storage I/O behind try/catch; silent degrade to URL-only behavior |
| T-04-SC | Tampering | npm installs (nuqs, react-number-format) | mitigate | slopcheck 0.6.1 ran [OK] for both (RESEARCH audit); exact-version pins; `npm view` confirmed empty postinstall for both — no [ASSUMED]/[SUS] packages, no blocking checkpoint required |
</threat_model>

<verification>
- `npm test` — full suite green (was 90 tests pre-phase; now larger)
- `npm run typecheck && npm run lint` — green
- `npm run build` — green after install
</verification>

<success_criteria>
- nuqs@2.10.1 and react-number-format@5.4.5 installed and pinned
- balance-params codec and balance-storage precedence fully unit-tested in node (no jsdom), all green
- Neither lib module references "use client", browser globals, or bare "nuqs" client entry
</success_criteria>

<output>
Create `.planning/phases/04-core-experience/04-01-SUMMARY.md` when done
</output>
