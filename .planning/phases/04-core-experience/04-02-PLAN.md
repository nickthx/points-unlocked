---
phase: 04-core-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/format.ts
  - src/lib/path-display.ts
  - tests/format.test.ts
  - tests/path-display.test.ts
autonomous: true
requirements: [VAL-01, VAL-04, RANK-04]
must_haves:
  truths:
    - "heroDelta returns wowDeltaCents.atMax ?? atMin — the exact conservative figure the ranking sorted on (A2 / Pitfall 4)"
    - "Integer cents format to whole-dollar currency strings ($4,500) and cppX100 formats to one-decimal cents (2.2¢)"
    - "An ISO date formats to 'Sep 1, 2026' via pure string splitting — no Date object, so no timezone off-by-one (Pitfall 5)"
    - "A TransferPath renders as 'via {From} → {To} {ratio}' from real seed routes, and 'Use your {Program} points directly' for direct paths (RANK-04)"
  artifacts:
    - path: "src/lib/format.ts"
      provides: "Display formatters + conservative hero-delta selector + cash-out value helper"
      exports: ["formatDollars", "formatCpp", "formatPoints", "formatVerifiedDate", "heroDelta", "cashOutValueCents"]
    - path: "src/lib/path-display.ts"
      provides: "TransferPath → human-readable path string"
      exports: ["formatTransferPath"]
    - path: "tests/format.test.ts"
      provides: "VAL-01/VAL-04 formatter tests with exact expected strings"
    - path: "tests/path-display.test.ts"
      provides: "RANK-04 path tests against real seed routes"
  key_links:
    - from: "src/lib/format.ts"
      to: "@/engine"
      via: "type-only import of RankedResult/ValueRange for heroDelta"
      pattern: "import type .* from \"@/engine\""
    - from: "tests/path-display.test.ts"
      to: "src/data"
      via: "real seed routes/programs as fixtures (relative import)"
      pattern: "from \"\\.\\./src/data\""
---

<objective>
Build the pure display layer: number/date formatters, the conservative hero-delta selector, the cash-out value helper, and the transfer-path formatter — each unit-tested with exact expected strings against the real dataset.

Purpose: every "hard" number on screen is a pre-computed integer field on RankedResult; this plan is the only sanctioned place UI-adjacent arithmetic lives (the final /100 inside formatters plus the documented cash-out mirror). Centralizing and testing it protects the finance-credibility claim (VAL-01) and the verified stamp (VAL-04).
Output: two lib modules + two green test suites. No new dependencies — runs in wave 1 parallel to plan 04-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-experience/04-RESEARCH.md
@.planning/phases/04-core-experience/04-PATTERNS.md
@.planning/phases/04-core-experience/04-UI-SPEC.md

<interfaces>
From src/engine/types.ts (consume type-only):
```typescript
export interface ValueRange { atMin: number; atMax: number | null; }
export interface TransferPath {
  kind: "direct" | "transfer";
  fromProgramSlug: string;
  routeKey?: string;            // `${from}→${to}` Unicode arrow; absent for "direct"
  requiredSourcePoints: number;
  activeBonus: TransferBonusSeed | null;
}
export interface RankedResult { /* ... */ wowDeltaCents: ValueRange; /* ... */ }
```

From src/data/types.ts:
```typescript
// TransferRouteSeed: fromProgramSlug, toProgramSlug, ratioNumerator, ratioDenominator, ...
// ProgramSeed: slug, name, kind, isUserEnterable, cashOutBaselineCppX100 (null for hotel programs)
```

Seed facts verified in-repo (for exact test expectations):
- amex-mr→hilton-honors route has ratioNumerator: 2, ratioDenominator: 1 → renders "2:1"
- chase-ur→world-of-hyatt uses the route() helper defaults (confirm 1:1 in src/data/transfers.ts before asserting)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Display formatters + heroDelta — src/lib/format.ts + tests</name>
  <files>src/lib/format.ts, tests/format.test.ts</files>
  <read_first>
    - src/engine/valuation.ts — the house style to copy: one exported function per concept, JSDoc naming the finance rule + requirement, guard clauses degrading to a safe value, integer arithmetic until the last step
    - src/engine/ranking.ts — `conservativeWow` (lines ~86-89): heroDelta is the same `atMax ?? atMin` expression over a result's ValueRange
    - .planning/phases/04-core-experience/04-UI-SPEC.md — "Typography" number-rendering rules (binding: Intl.NumberFormat config, cpp format, no Date object)
    - tests/engine-ranking.test.ts — test conventions (relative imports, `_` separators, concrete numbers in `it` strings)
  </read_first>
  <behavior>
    - formatDollars(450_000) === "$4,500"; formatDollars(0) === "$0"; negative cents render with a leading minus per Intl default
    - formatCpp(220) === "2.2¢"; formatCpp(105) === "1.1¢" (toFixed(1) rounding); formatCpp(0) === "0.0¢"
    - formatPoints(90_000) === "90,000"
    - formatVerifiedDate("2026-09-01") === "Sep 1, 2026" (no leading zero on day); formatVerifiedDate("2026-10-14") === "Oct 14, 2026"; implementation contains no `new Date` (Pitfall 5)
    - heroDelta with wowDeltaCents { atMin: 500_000, atMax: 350_000 } returns 350_000 (conservative end); with { atMin: 400_000, atMax: null } returns 400_000 (fixed chart)
    - cashOutValueCents(90_000, 100) === 90_000 (90K points at 1.0¢/pt = $900 in cents); cashOutValueCents(anyPoints, null) === 0; non-finite/negative inputs → 0
  </behavior>
  <action>
    Create src/lib/format.ts with six pure exports. formatDollars: `Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 })` over `cents / 100` (construct the formatter once at module scope). formatCpp: `(cppX100 / 100).toFixed(1) + "¢"`. formatPoints: `Intl.NumberFormat("en-US")`. formatVerifiedDate: split the ISO string on "-", map month number to the three-letter English abbreviation via a const array, emit `"{Mon} {D}, {YYYY}"` with the day parsed to strip a leading zero — annotate with a Pitfall 5 comment (bare ISO dates parse as UTC midnight; no Date object here). heroDelta(result: RankedResult): `result.wowDeltaCents.atMax ?? result.wowDeltaCents.atMin` — annotate A2/Pitfall 4 ("the exact conservative figure the ranking sorted on; never render the optimistic end as the hero"). cashOutValueCents(points: number, cashOutBaselineCppX100: number | null): `Math.round((points * baseline) / 100)` guarded to return 0 on null baseline or non-finite/non-positive inputs — JSDoc must state it mirrors the engine's cash-out definition used inside wowDeltaCents (points × cppX100 ÷ 100 = cents) and exists solely for the "vs. ~$X cashing out" framing copy; it is presentation of an engine-consistent figure, not new math. Create tests/format.test.ts asserting every exact string above.
  </action>
  <verify>
    <automated>npx vitest run tests/format.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/format.test.ts` exits 0, asserting the exact strings "$4,500", "2.2¢", "90,000", "Sep 1, 2026", "Oct 14, 2026"
    - `grep -c "new Date" src/lib/format.ts` outputs 0
    - heroDelta tested for both atMax-present and atMax-null shapes
    - cashOutValueCents tested for null baseline → 0 and the 90_000/100 → 90_000 anchor
  </acceptance_criteria>
  <done>All display formatting flows through tested pure helpers; the hero figure provably matches the ranking key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transfer-path display — src/lib/path-display.ts + tests</name>
  <files>src/lib/path-display.ts, tests/path-display.test.ts</files>
  <read_first>
    - .planning/phases/04-core-experience/04-RESEARCH.md — Pattern 5 (formatTransferPath reference implementation)
    - src/engine/types.ts — TransferPath JSDoc (routeKey `${from}→${to}` Unicode-arrow contract, absent for "direct")
    - src/data/transfers.ts — confirm actual ratioNumerator/ratioDenominator for amex-mr→hilton-honors (2:1) and chase-ur→world-of-hyatt before hardcoding expected strings
    - src/data/programs.ts — exact program `name` values for expected strings
    - tests/engine-ranking.test.ts — test conventions (relative imports, real-dataset fixtures)
  </read_first>
  <behavior>
    - Transfer path with routeKey "amex-mr→hilton-honors" renders "via {amex-mr program name} → {hilton-honors program name} 2:1" (exact names read from src/data/programs.ts)
    - Transfer path with routeKey "chase-ur→world-of-hyatt" renders with the route's actual ratio (expected 1:1 — confirm in seed)
    - Direct path (kind "direct", no routeKey) renders "Use your {program name} points directly"
    - Unknown slug degrades to the raw slug string instead of throwing (guard-clause house style); missing route for a transfer path degrades to "via {From} → {To}" without a ratio rather than throwing
  </behavior>
  <action>
    Create src/lib/path-display.ts exporting `formatTransferPath(path: TransferPath, routes: TransferRouteSeed[], programs: ProgramSeed[]): string` (import types from "@/engine" and "@/data" barrels only — never deep paths; note the transfers.ts filename-collision hazard in a comment). Implementation per RESEARCH Pattern 5: build name lookup from programs; for "direct" return the direct-use sentence; otherwise split routeKey on the Unicode arrow "→", find the route by fromProgramSlug/toProgramSlug, and return `via {From} → {To} {ratioNumerator}:{ratioDenominator}` (UI-SPEC copy contract). Never use non-null assertions on the route lookup — degrade per the behavior block. Create tests/path-display.test.ts using the REAL seed arrays via relative imports (`../src/data`) so a seed rename fails these tests, constructing TransferPath literals with pinned requiredSourcePoints/activeBonus values.
  </action>
  <verify>
    <automated>npx vitest run tests/path-display.test.ts && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/path-display.test.ts` exits 0 with at least: one transfer case asserting an exact "via ... 2:1" string against the real amex-mr→hilton-honors route, one 1:1 case, one direct case, and one degrade case (unknown slug or missing route) proving no throw
    - src/lib/path-display.ts imports only from "@/engine", "@/data" (or their types) — `grep -cE "from \"@/(engine|data)/" src/lib/path-display.ts` outputs 0 (no deep imports)
    - `grep -c "!" ` is not used as a gate, but source contains no `.find(...)!` non-null assertion: `grep -c ")!" src/lib/path-display.ts` outputs 0
    - Full suite, typecheck, lint green
  </acceptance_criteria>
  <done>RANK-04 path strings render from real seed data with graceful degradation; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine output → rendered strings | Values derived from user-influenced balances flow into display strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | formatters over user-influenced numbers | mitigate | guard clauses degrade non-finite/hostile inputs to safe values ("$0", "0.0¢", 0); all output built via Intl.NumberFormat/string templates rendered through JSX auto-escaping downstream |
| T-04-05 | Information disclosure | formatVerifiedDate / path degrade branches | accept | inputs are repo-curated seed fields (Zod-validated at seed boundary); degrade branches emit slugs already public in the shipped dataset — no secrets exist in this layer |
| T-04-06 | Repudiation-adjacent credibility | heroDelta diverging from ranking key | mitigate | heroDelta is the literal `atMax ?? atMin` expression, unit-tested for both shapes (Pitfall 4); plan 04-03 renders cards only through this helper |
</threat_model>

<verification>
- `npm test` — full suite green
- `npm run typecheck && npm run lint` — green
</verification>

<success_criteria>
- All six format.ts exports and formatTransferPath exist, pure, with exact-string unit tests green
- No `new Date` anywhere in format.ts; no non-null assertions in path-display.ts
- Path tests assert against the real seed dataset, not inline route fixtures
</success_criteria>

<output>
Create `.planning/phases/04-core-experience/04-02-SUMMARY.md` when done
</output>
