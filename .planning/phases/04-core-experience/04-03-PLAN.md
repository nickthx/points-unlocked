---
phase: 04-core-experience
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/components/balance-form.tsx
  - src/components/result-card.tsx
  - src/components/almost-there.tsx
autonomous: true
requirements: [INPUT-01, RANK-01, RANK-02, RANK-03, RANK-04, RANK-05, VAL-01, VAL-04]
must_haves:
  truths:
    - "All 8 enterable programs render labeled, thousands-separated numeric inputs with a 44px+ touch target and mobile numeric keypad (INPUT-01)"
    - "A result card leads with the terracotta Fraunces hero delta from heroDelta(), shows cash fare + cpp side by side, the balance-tag chip, the explicit transfer path, verbatim bookingHint, and the Verified stamp (VAL-01/VAL-04/RANK-03/04/05)"
    - "Hotel-point-funded cards use 'Pure travel value' framing; bank-point cards (incl. Bilt) use 'vs. ~$X cashing out' (Pitfall 10)"
    - "Almost-there entries show 'You're {points} {program} points away' in ink, never terracotta (RANK-02)"
  artifacts:
    - path: "src/components/balance-form.tsx"
      provides: "8 NumericFormat inputs wrapping shadcn Input, slug-keyed onChange"
      exports: ["BalanceForm"]
    - path: "src/components/result-card.tsx"
      provides: "Wow card rendering every RankedResult display field"
      exports: ["ResultCard"]
    - path: "src/components/almost-there.tsx"
      provides: "Almost-there section with points-away callouts"
      exports: ["AlmostThere"]
  key_links:
    - from: "src/components/result-card.tsx"
      to: "src/lib/format.ts"
      via: "heroDelta + formatters (card renders, never computes)"
      pattern: "from \"@/lib/format\""
    - from: "src/components/result-card.tsx"
      to: "src/lib/path-display.ts"
      via: "formatTransferPath for the RANK-04 line"
      pattern: "formatTransferPath"
    - from: "src/components/balance-form.tsx"
      to: "react-number-format"
      via: "NumericFormat customInput={Input}"
      pattern: "customInput=\\{Input\\}"
---

<objective>
Build the three presentational components of the guest flow: the 8-program formatted balance form, the wow result card, and the almost-there section. All are props-driven (no state ownership, no nuqs, no storage) so plan 04-04 can compose them in the client island.

Purpose: this is where the phase's success criteria become visible — the delta-led card is the product's wow moment. Every number renders through the tested helpers from plan 04-02; the components do zero arithmetic.
Output: three components, typecheck/lint-clean, composed in wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-core-experience/04-UI-SPEC.md
@.planning/phases/04-core-experience/04-PATTERNS.md
@.planning/phases/04-core-experience/04-RESEARCH.md

<interfaces>
From src/engine/types.ts (all card content is pre-computed — render, never compute):
```typescript
export interface RankedResult {
  redemption: RedemptionSeed;        // title, bookingHint, verifiedAt, cashFareCents, availabilityRating, category...
  chosenPath: TransferPath;          // fromProgramSlug (RANK-03 tag), routeKey, requiredSourcePoints, activeBonus
  alternatePaths: TransferPath[];
  pointsNeeded: { min: number; max: number | null };
  cppX100: ValueRange;
  effectiveCppX100: ValueRange;      // promo-honest figure — the one to display (VAL-05 passthrough)
  wowDeltaCents: ValueRange;         // hero = atMax ?? atMin via heroDelta()
  coverage: number;
  pointsAway: number | null;         // non-null only in almostThere
}
```

From src/lib/format.ts (plan 04-02): formatDollars, formatCpp, formatPoints, formatVerifiedDate, heroDelta, cashOutValueCents
From src/lib/path-display.ts (plan 04-02): formatTransferPath(path, routes, programs)
From src/data (barrel): programs (ProgramSeed[] — .name for labels, .isUserEnterable for the 8, .cashOutBaselineCppX100 null ⇢ hotel framing), routes
From src/engine (barrel, type-only): EnterableProgramSlug, Balances, RankedResult, TransferPath
From src/components/ui: Input (h-8 default — override height via className; merges via cn), Label (htmlFor), Card/CardHeader/CardTitle/CardContent/CardFooter (CardTitle already uses font-heading = Fraunces)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Balance form — src/components/balance-form.tsx</name>
  <files>src/components/balance-form.tsx</files>
  <read_first>
    - .planning/phases/04-core-experience/04-RESEARCH.md — Pattern 3 (NumericFormat + customInput, onValueChange sourceInfo guard)
    - src/components/ui/input.tsx — default classes (h-8) and cn merge behavior
    - src/components/ui/label.tsx — "use client" first-line convention + Label usage
    - src/data/programs.ts — the 8 isUserEnterable entries (order and exact names)
    - .planning/phases/04-core-experience/04-UI-SPEC.md — spacing (44px touch target, sm/md gaps), typography (14px/600 labels), responsive (2-col ≥ sm, single below), copywriting ("Enter what you have. No account, nothing stored on our servers.")
  </read_first>
  <action>
    Create src/components/balance-form.tsx ("use client" as the literal first line). Named export `BalanceForm` with props `{ balances: Balances; onBalanceChange: (slug: EnterableProgramSlug, value: number | null) => void }` — the form owns no state; nuqs stays in the island (plan 04-04). Derive the 8 fields by filtering the `programs` array from "@/data" on `isUserEnterable` (array order; slugs typed via EnterableProgramSlug — do not hardcode a second program list). Each field: shadcn `Label` (htmlFor={slug}, 14px semibold — `text-sm font-semibold`) + `NumericFormat` with `customInput={Input}`, `id={slug}`, `value={balances[slug] ?? ""}`, `thousandSeparator=","`, `allowNegative={false}`, `decimalScale={0}`, `inputMode="numeric"`, `placeholder="0"`, and `onValueChange={({ floatValue }, sourceInfo) => ...}` that returns early unless `sourceInfo.source === "event"` (ignore prop-driven echoes) and calls `onBalanceChange(slug, floatValue && floatValue > 0 ? Math.floor(floatValue) : null)`. Override the Input height to meet the 44px touch target: `className="h-11"` (UI-SPEC exception). Layout: `grid grid-cols-1 sm:grid-cols-2` with `gap-4` (md token), 8px label-to-input gap. Include the helper line "Enter what you have. No account, nothing stored on our servers." in `text-ink/70`. No terracotta anywhere in this component (accent reserved list). Semicolon style per app-authored convention; kebab-case filename; cn from "@/lib/utils" for conditional classes.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isUserEnterable" src/components/balance-form.tsx` outputs at least 1 (program list derived from data, not hardcoded)
    - `grep -c "customInput={Input}" src/components/balance-form.tsx` outputs 1
    - `grep -cE "inputMode=.numeric." src/components/balance-form.tsx` outputs 1 and `grep -c "allowNegative={false}" src/components/balance-form.tsx` outputs 1
    - `grep -c "h-11" src/components/balance-form.tsx` outputs at least 1 (44px touch target)
    - First line of the file is `"use client";`
    - typecheck + lint green
  </acceptance_criteria>
  <done>8 labeled, formatted, mobile-keypad inputs render from the data-driven program list with the island-owned change contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wow result card — src/components/result-card.tsx</name>
  <files>src/components/result-card.tsx</files>
  <read_first>
    - .planning/phases/04-core-experience/04-UI-SPEC.md — color reserved list, typography table (48px display hero, 28px Fraunces titles, 14px metadata, 16px body), copywriting contract (framing lines, bonus badge, verified stamp — copy verbatim)
    - .planning/phases/04-core-experience/04-PATTERNS.md — result-card section (field → UI mapping) + design-token section (no new color values)
    - src/components/ui/card.tsx — Card/CardHeader/CardTitle/CardContent/CardFooter exports and size prop
    - src/lib/format.ts and src/lib/path-display.ts (plan 04-02 outputs — exact signatures)
    - src/app/page.tsx — current token vocabulary example (font-display/text-display/text-ink/text-terracotta usage)
  </read_first>
  <action>
    Create src/components/result-card.tsx (server-compatible pure presentational component — no "use client" needed; it renders inside the island regardless). Named export `ResultCard` with props `{ result: RankedResult; programs: ProgramSeed[]; routes: TransferRouteSeed[] }`. Render, top to bottom (layout discretion within UI-SPEC tokens, content mandatory):
    (1) Title: `result.redemption.title` via CardTitle (Fraunces via font-heading, ~28px — `text-[1.75rem]` or existing heading scale).
    (2) Hero: `formatDollars(heroDelta(result))` in `font-display text-display text-terracotta` — the ONLY terracotta number (A2 comment: this is the ranking key).
    (3) Framing line beneath the hero (Pitfall 10): look up the source program by `result.chosenPath.fromProgramSlug` in `programs`; if `cashOutBaselineCppX100 === null` render exactly "Pure travel value — these points have no cash-out option"; else render "vs. ~{formatDollars(cashOutValueCents(result.chosenPath.requiredSourcePoints, baseline))} cashing out". Ink/muted, 16px.
    (4) Side-by-side valuation block (VAL-01): cash fare `formatDollars(result.redemption.cashFareCents)` labeled "Cash fare" and cpp `formatCpp(result.effectiveCppX100.atMax ?? result.effectiveCppX100.atMin)` labeled "Value per point" — the conservative end, consistent with the hero. 16px body, ink.
    (5) Balance-tag chip (RANK-03): "Uses {formatPoints(result.chosenPath.requiredSourcePoints)} {source program name} points" — 14px, ink/muted chip (e.g. `bg-ink/5 text-ink/70 rounded-full px-2 py-1`), NOT terracotta.
    (6) Path line (RANK-04): `formatTransferPath(result.chosenPath, routes, programs)` — 14px ink/muted.
    (7) Bonus badge (VAL-05 surfacing) only when `result.chosenPath.activeBonus !== null`: "+{activeBonus.bonusPercent}% transfer bonus through {formatVerifiedDate(activeBonus.endDate)}" — terracotta (sanctioned accent use #3).
    (8) Booking guidance (RANK-05): `result.redemption.bookingHint` rendered verbatim, 16px body — the seed field contains literal \n separators, so render with `whitespace-pre-line`.
    (9) Verified stamp (VAL-04): "Verified {formatVerifiedDate(result.redemption.verifiedAt)}" — 14px ink/muted; `verifiedAt` is non-null for engine output (A5 fail-closed filter) but guard with a conditional render rather than a non-null assertion.
    Zero arithmetic in this file — every number arrives through a plan 04-02 helper. No new color values; spacing from the UI-SPEC scale (p-4 mobile / p-6 desktop card padding).
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "heroDelta" src/components/result-card.tsx` outputs at least 1 and `grep -cE "wowDeltaCents\.(atMin|atMax)" src/components/result-card.tsx` outputs 0 (hero only via the tested helper)
    - `grep -c "Pure travel value" src/components/result-card.tsx` outputs 1 and `grep -c "cashing out" src/components/result-card.tsx` outputs 1 (both framings present)
    - `grep -c "bookingHint" src/components/result-card.tsx` outputs at least 1 and `grep -c "whitespace-pre-line" src/components/result-card.tsx` outputs at least 1
    - `grep -c "formatTransferPath" src/components/result-card.tsx` outputs at least 1; `grep -c "Verified" src/components/result-card.tsx` outputs at least 1
    - `grep -cE "text-terracotta" src/components/result-card.tsx` outputs exactly 2 (hero + bonus badge — nothing else)
    - `grep -c "dangerouslySetInnerHTML" src/components/result-card.tsx` outputs 0
    - typecheck + lint green
  </acceptance_criteria>
  <done>The wow card renders every required field from pre-computed engine data with the accent discipline intact.</done>
</task>

<task type="auto">
  <name>Task 3: Almost-there section — src/components/almost-there.tsx</name>
  <files>src/components/almost-there.tsx</files>
  <read_first>
    - .planning/phases/04-core-experience/04-UI-SPEC.md — almost-there callout copy (verbatim template), section heading "Almost there", accent restrictions
    - .planning/phases/04-core-experience/04-PATTERNS.md — almost-there section (pointsAway denomination proof from engine tests)
    - src/components/result-card.tsx (just created — match its card structure conventions in condensed form)
  </read_first>
  <action>
    Create src/components/almost-there.tsx. Named export `AlmostThere` with props `{ results: RankedResult[]; programs: ProgramSeed[]; routes: TransferRouteSeed[] }`. Render null when `results.length === 0` (section absent, not an empty shell). Section heading "Almost there" in Fraunces 28px ink (`font-heading`), 48px (2xl token) top margin for the section break. For each result (engine array order — never re-sort), render a condensed card: `redemption.title` (Fraunces heading), the callout "You're {formatPoints(result.pointsAway ?? 0)} {source program name} points away" as the lead line in ink semibold (NOT terracotta — accent reserved; guard the null with `?? 0` plus a conditional so a null pointsAway row renders no callout rather than "You're 0"), the path line via formatTransferPath, and the potential delta as secondary ink text "worth {formatDollars(heroDelta(result))} over cash-out" in 16px ink/70 (explicitly not display-size, not terracotta — the drama stays in Bookable now). Source program name looked up from `result.chosenPath.fromProgramSlug` (pointsAway is denominated in that currency — engine contract). Cards `gap-6` (lg token) apart.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "points away" src/components/almost-there.tsx` outputs at least 1
    - `grep -c "terracotta" src/components/almost-there.tsx` outputs 0
    - `grep -cE "\.sort\(" src/components/almost-there.tsx` outputs 0 (engine order preserved)
    - `grep -c "Almost there" src/components/almost-there.tsx` outputs at least 1
    - typecheck + lint + full test suite green
  </acceptance_criteria>
  <done>Near-miss redemptions render with points-away callouts in engine order, accent-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine output (user-influenced balances) → DOM | Rendered values derive from untrusted URL/storage input via the engine |
| Curated seed strings → DOM | bookingHint/title/notes render as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering (XSS) | all three components | mitigate | every string renders through JSX auto-escaping; `dangerouslySetInnerHTML` prohibited and grep-gated in Task 2; `whitespace-pre-line` renders \n without HTML injection |
| T-04-08 | Tampering | NumericFormat input boundary | mitigate | allowNegative false, decimalScale 0, Math.floor + >0 guard in onChange; codec + engine re-sanitize downstream (T-04-01 chain) |
| T-04-09 | Spoofing (credibility) | framing copy on hotel currencies | mitigate | framing branches on `cashOutBaselineCppX100 === null` from the dataset (Pitfall 10) — misleading "vs. cashing out" copy never shows for no-cash-out programs |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm test` — green
- Grep gates from each task's acceptance criteria
</verification>

<success_criteria>
- Three props-driven components exist, typecheck/lint-clean, doing zero arithmetic and zero re-sorting
- Terracotta appears exactly twice in result-card (hero, bonus badge) and nowhere in balance-form/almost-there
- All copywriting matches the UI-SPEC contract verbatim
</success_criteria>

<output>
Create `.planning/phases/04-core-experience/04-03-SUMMARY.md` when done
</output>
